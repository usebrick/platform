{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://usebrick.dev/schemas/v1/calibration-tool-authority-publication-lock.schema.json",
  "title": "CalibrationToolAuthorityPublicationLockV1",
  "description": "Fixed-path lock for a tool-authority index create or replace transaction.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "lockId", "intendedTransactionId", "operation", "expectedCurrentState", "nextIndexSha256", "artifactSetSha256", "recoveryNonce", "lockSha256"],
  "properties": {
    "version": { "const": "v10.3-tool-authority-publication-lock-v1" },
    "lockId": { "$ref": "#/$defs/sha256" },
    "intendedTransactionId": { "$ref": "#/$defs/sha256" },
    "operation": { "enum": ["create", "replace"] },
    "expectedCurrentState": { "$ref": "#/$defs/expectedCurrentState" },
    "nextIndexSha256": { "$ref": "#/$defs/sha256" },
    "artifactSetSha256": { "$ref": "#/$defs/sha256" },
    "recoveryNonce": { "$ref": "#/$defs/sha256" },
    "lockSha256": { "$ref": "#/$defs/sha256" }
  },
  "$defs": {
    "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "expectedCurrentState": {
      "oneOf": [
        { "type": "object", "additionalProperties": false, "required": ["kind"], "properties": { "kind": { "const": "absent" } } },
        { "type": "object", "additionalProperties": false, "required": ["kind", "indexSha256"], "properties": { "kind": { "const": "existing" }, "indexSha256": { "$ref": "#/$defs/sha256" } } }
      ]
    }
  }
}
