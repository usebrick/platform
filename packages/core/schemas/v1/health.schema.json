{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://usebrick.dev/schemas/v1/health.schema.json",
  "title": "Repository Structure \u2014 Health",
  "description": "Repository health snapshot from a completed or partial slopbrick scan. The canonical four fields are aiSlopScore (0\u2013100, lower is better), engineeringHygiene (0\u2013100, higher is better), security (0\u2013100, higher is better), and repositoryHealth (0\u2013100, higher is better). Repository Health is 0.4 \u00d7 (100 \u2212 aiSlopScore) + 0.3 \u00d7 engineeringHygiene + 0.2 \u00d7 security + 0.1 \u00d7 testQuality. Scores are rounded to nearest integer in this health snapshot; JSON/SARIF retain full precision and human renderers show one decimal. An empty or not-applicable scan is represented by the score-free scan-report envelope, not by this health artifact.",
  "type": "object",
  "required": [
    "version",
    "generatedAt",
    "workspace",
    "aiSlopScore",
    "engineeringHygiene",
    "security",
    "repositoryHealth",
    "issueCounts"
  ],
  "properties": {
    "version": {
      "description": "Health schema version. Currently '5'.",
      "const": "5"
    },
    "generatedAt": {
      "type": "string",
      "format": "date-time"
    },
    "workspace": {
      "type": "string",
      "minLength": 1
    },
    "aiSlopScore": {
      "description": "v0.21.0: AI-slop score (0-100, higher = more slop, lower = cleaner). The raw amount of AI slop signatures detected (0 = no AI slop, 100 = max AI slop). In v0.15\u20130.20.1 this was inverted (100 = clean); the v0.21.0 flip matches the natural reading of the name (users read 'AI Slop Score: 100' as '100% slop').",
      "type": "integer",
      "minimum": 0,
      "maximum": 100
    },
    "engineeringHygiene": {
      "description": "Canonical engineering hygiene score (0-100, higher is better): 100 minus the mean bounded burden across effective arch, logic, layout, visual, component, and test categories.",
      "type": "integer",
      "minimum": 0,
      "maximum": 100
    },
    "security": {
      "description": "v0.15.0: security posture score (0-100, higher is better). Dependency vulnerabilities, secret leaks, OWASP anti-patterns.",
      "type": "integer",
      "minimum": 0,
      "maximum": 100
    },
    "repositoryHealth": {
      "description": "Canonical repository health composite (0-100, higher is better): 0.4 × (100 − aiSlopScore) + 0.3 × engineeringHygiene + 0.2 × security + 0.1 × testQuality. The score is rounded to nearest integer in health.json.",
      "type": "integer",
      "minimum": 0,
      "maximum": 100
    },
    "issueCounts": {
      "description": "Number of issues per severity level.",
      "type": "object",
      "required": [
        "high",
        "medium",
        "low"
      ],
      "properties": {
        "high": {
          "type": "integer",
          "minimum": 0
        },
        "medium": {
          "type": "integer",
          "minimum": 0
        },
        "low": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "slopIndex": {
      "description": "DEPRECATED (v0.15.0). Legacy composite (0-100, lower is better). Kept optional for backward compat with v0.14 dashboards; will be removed in v0.16.0. New readers should ignore.",
      "type": "integer",
      "minimum": 0,
      "maximum": 100
    },
    "categoryScores": {
      "description": "DEPRECATED (v0.15.0). Legacy per-category breakdown. Kept optional for backward compat; will be removed in v0.16.0.",
      "type": "object",
      "additionalProperties": {
        "type": "integer",
        "minimum": 0,
        "maximum": 100
      }
    },
    "constitutionDrift": {
      "description": "Number of constitution violations detected in this scan.",
      "type": "integer",
      "minimum": 0
    },
    "topOffenseIds": {
      "description": "Top 3 most-firing rule IDs in this scan, sorted by issue count desc.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z]+/[a-z-]+$"
      },
      "maxItems": 3
    },
    "scanDurationMs": {
      "description": "How long the scan took in milliseconds.",
      "type": "integer",
      "minimum": 0
    },
    "scoreBasis": {
      "description": "Provenance for headline scores: the analysed-file denominator and effective issue set used by the scanner.",
      "type": "object",
      "required": ["denominator", "analyzedFiles", "issueSet", "suppressedIssueCount", "parseErrorCount"],
      "properties": {
        "denominator": { "type": "integer", "minimum": 0 },
        "analyzedFiles": { "type": "integer", "minimum": 0 },
        "issueSet": { "const": "effective" },
        "suppressedIssueCount": { "type": "integer", "minimum": 0 },
        "parseErrorCount": { "type": "integer", "minimum": 0 }
      }
    },
    "completionStatus": {
      "description": "Outcome of the source scan. Optional so historical health snapshots remain valid.",
      "type": "string",
      "enum": ["complete", "empty", "partial"]
    },
    "scoreValidity": {
      "description": "Whether headline numeric scores are safe to use for gating. `incomplete` means only a subset of requested files was analysed; `not-applicable` means no files were selected.",
      "type": "string",
      "enum": ["valid", "incomplete", "not-applicable"]
    },
    "requested": { "type": "integer", "minimum": 0 },
    "analyzed": { "type": "integer", "minimum": 0 },
    "failed": { "type": "integer", "minimum": 0 },
    "skipped": { "type": "integer", "minimum": 0 },
    "scanAccounting": {
      "description": "Additive outcome accounting for every selected file. Optional for compatibility with historical snapshots.",
      "type": "object",
      "required": ["selected", "analyzed", "zeroFinding", "incrementalCached", "parseFailed", "timedOut", "crashed", "internalFailed"],
      "properties": {
        "selected": { "type": "integer", "minimum": 0 },
        "analyzed": { "type": "integer", "minimum": 0 },
        "zeroFinding": { "type": "integer", "minimum": 0 },
        "incrementalCached": { "type": "integer", "minimum": 0 },
        "parseFailed": { "type": "integer", "minimum": 0 },
        "timedOut": { "type": "integer", "minimum": 0 },
        "crashed": { "type": "integer", "minimum": 0 },
        "internalFailed": { "type": "integer", "minimum": 0 }
      }
    },
    "selectionAccounting": {
      "description": "Aggregate-only accounting for candidates observed by discovery or explicit directory expansion. It does not claim counts for paths outside that observable population.",
      "type": "object",
      "required": ["observedCandidates", "selected", "excluded"],
      "properties": {
        "observedCandidates": { "type": "integer", "minimum": 0 },
        "selected": { "type": "integer", "minimum": 0 },
        "excluded": {
          "type": "object",
          "required": ["configExclude", "unsupportedFileType", "extensionlessDuplicate", "outsideWorkspace", "gitScope"],
          "properties": {
            "configExclude": { "type": "integer", "minimum": 0 },
            "unsupportedFileType": { "type": "integer", "minimum": 0 },
            "extensionlessDuplicate": { "type": "integer", "minimum": 0 },
            "outsideWorkspace": { "type": "integer", "minimum": 0 },
            "gitScope": { "type": "integer", "minimum": 0 }
          }
        }
      }
    },
    "compositeScore": {
      "description": "v0.18.2: project-level Bayesian aggregate of the per-file composite scores (see worker.ts:98). Informational addition; does not affect the four headline scores. The `mean` is the headline 'is this codebase AI?' signal; `tier` is derived from the mean using Jaeschke 1994 JAMA thresholds. Optional for backward compat with v0.18.1 and earlier readers.",
      "type": "object",
      "required": ["mean", "max", "tier", "fileCount"],
      "properties": {
        "mean": {
          "description": "Mean of per-file probabilities across all files that fired at least one rule. In [0,1].",
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "max": {
          "description": "Highest per-file probability in the scan. In [0,1].",
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "tier": {
          "description": "Confidence tier of the mean, per Jaeschke 1994 JAMA thresholds: <0.10 LIKELY_HUMAN, <0.50 INCONCLUSIVE, <0.90 LIKELY_AI, else VERY_LIKELY_AI.",
          "type": "string",
          "enum": ["LIKELY_HUMAN", "INCONCLUSIVE", "LIKELY_AI", "VERY_LIKELY_AI"]
        },
        "fileCount": {
          "description": "Number of files that contributed a composite score.",
          "type": "integer",
          "minimum": 0
        }
      }
    }
  }
}
