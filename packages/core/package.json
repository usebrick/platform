{
  "name": "@usebrick/core",
  "version": "0.2.0",
  "private": true,
  "description": "Shared .slop-audit/ memory artifact reader/writer + Repository Memory Platform types. The contract every usebrick.dev tool depends on. Private to the monorepo — not published to npm until the schema is stable.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "schemas",
    "README.md"
  ],
  "scripts": {
    "prebuild": "pnpm codegen && pnpm test:contract && tsup",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "codegen": "tsx scripts/codegen-types.ts",
    "test:contract": "tsx scripts/verify-codegen-fresh.ts",
    "validate:schema": "tsx scripts/validate-schemas.ts"
  },
  "keywords": [
    "usebrick",
    "repository-memory",
    "slopbrick",
    "slop-audit",
    "schema",
    "inventory",
    "constitution"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vitest/coverage-v8": "^2.1.9",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "json-schema-to-typescript": "^15.0.4",
    "tsup": "^8.5.1",
    "tsx": "^4.19.2",
    "typescript": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
