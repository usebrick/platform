# v0.45.0 Gate 0 evidence

**Recorded:** 2026-07-09  
**Branch:** `codex/v0.45-recovery`  
**HEAD:** `0cebba53c` (`docs(slopbrick): record post-reconciliation blockers`)

This is a containment and classification record. It does not authorize a
version bump, tag, push, deployment, or publication.

## G0-01 — dirty-tree inventory

The worktree is intentionally dirty and is **not** `main`. `git status
--short --branch` reports 17 modified tracked files and 52 untracked files.
The complete machine-readable inventory is the output of `git status` at this
commit; the classes below identify what may be carried forward.

| Class | Current items | Decision |
| --- | --- | --- |
| Keep/harden | CLI scan/calibrate/program/types, worker, registry, calibrator, and their tests; calibration README/plan/catalog edits | Preserve as pre-release work; review under Gates 1–3 before any release commit. |
| Accidental/revert candidate | `src/engine/parser-kotlin.ts` comment-only deletion; `pnpm-workspace.yaml` generated `allowBuilds` placeholders; `packages/website/src/data/version.json` date-only change | Do not ship. Revert or separately justify in the next scoped cleanup commit. |
| Release metadata requiring decision | `packages/slopbrick/package.json` v0.45.0 description; `CHANGELOG.md` v0.45/v0.44 narrative | Keep as evidence of the unresolved state, but remove unsupported ML/corpus/shipped claims before a release branch. |
| Research-only | `packages/slopbrick/scripts/cal/*`, `models/ai-baseline-v0.45.json`, `TODO.md`, master/ML/rule-review documents, and `/tmp/cal-results-v45` | Preserve and label exploratory; never feed signal updates or product inference until Gates 3–6 pass. |
| Review evidence | `.superpowers/sdd/*` briefs, reports, reviews, and diffs | Preserve for audit; these files are not package/runtime artifacts. |

No unrelated user edits were staged or altered by this audit.

## G0-02 — evidence hashes

The current exploratory artifacts were hashed in place. The two directory
digests are SHA-256 over sorted per-file SHA-256 records, so they cover every
chunk output without committing raw third-party code.

| Artifact | Count/size | SHA-256 |
| --- | ---: | --- |
| `/tmp/cal-results-v45/pos` | 592 files | `132930a99cbaab3e9982618104b052b4203a223f7e7946aacebd653b88e5d3ef` |
| `/tmp/cal-results-v45-neg/neg` | 730 files | `178f0b8d19e973861330e71ca92a401d9aa755fec435396588becbc48a671012` |
| `v10.2a-empirical.md` | 17,009 bytes | `4349883d020b2882c123b4645a22b3ebaa186b4a3e12fabaad8079b4aa230006` |
| `features.jsonl` | 46,562,133 bytes | `49efd26c842441b05c0bc59427f809d39ce81923f644b44b8b4944b108d348f6` |
| `models/ai-baseline-v0.45.json` | 3,246 bytes | `286c6c869abf51dee1e0eaa58a0d12c23562b6becac57a7645dfac2c53962a30` |

These hashes identify evidence only. The artifacts remain invalid for release
calibration because the continuation plan records incomplete, polarity-biased
coverage and unverified labels.

## G0-03 — branch and release containment

The recovery branch exists (`codex/v0.45-recovery`), and the reviewed recovery
commits are separated by concern through `0cebba53c`. The branch has not been
merged or pushed by this audit. No tag, GitHub release, npm publication, or
production deployment was performed. The dirty worktree remains a deliberate
release guardrail.

## Decisions still required before Gate 0 can close

1. Choose the next public train: v0.44.0 trust restoration (recommended) or
   explicitly record an intentional v0.44.0 skip.
2. Freeze whether Dart, Ruby, PHP, and C# are in the next release scope.
3. Adopt a maintained Node LTS policy (the current Node 20 claim is EOL) and
   decide whether calibration Python remains research-only.
4. Resolve the accidental diffs and rewrite unsupported package/changelog
   claims; add an `[Unreleased]` section before release metadata is staged.

Until those decisions and clean-up are evidenced, Gate 0 remains open and no
release action is permitted.
