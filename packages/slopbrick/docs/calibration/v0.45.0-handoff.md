# v0.45.0 Handoff Document

> **Post-handoff audit update (2026-07-09): execution superseded.** Do not resume from “finish Step 5 + Step 6”, the priority list, the release checklist, or the 1–2 day estimate below. A repository-wide audit found upstream score, scan, corpus-provenance, coverage, metric, dataset, package, and release-lineage blockers. The authoritative continuation is [`v0.45.0-continuation-plan.md`](./v0.45.0-continuation-plan.md). Existing chunk reports, feature JSONL, and model files are exploratory artifacts only and must not drive signal changes, model shipment, or a release.

**Date:** 2026-07-09
**From:** AI session that started the v0.45.0 plan execution
**To:** Next person / next AI session continuing the work
**Historical status at handoff:** v0.45.0 plan ~80% executed, NOT yet released

**Current status:** reset to gate-based planning; not release-ready

**Reader shortcut — latest current-state evidence (2026-07-14):** use the
dated sections at the end of this file beginning with `Bounded validity,
threshold, and toolchain follow-up`, then read the appended `MCP stdio and
corpus authority follow-up`, `Read-only genesis composition diagnostic`, and
`Synthetic clean commit-boundary consumer diagnostic`, and the final full
package and health-wire matrix section. Those sections supersede the older
dated checkpoints above (including earlier 306/3,519 and 751/455 snapshots).
The current evidence is 307
SlopBrick test files/3,540 tests, Core 23/185, Engine 5/59, Website 38/38 plus
16/16 Playwright/axe, a complete workspace self-scan at 227/227, and a dirty
tree of 757 status entries/456 untracked files. It is still not corpus
admission or release evidence.

The external review-artifact validator also passes on the canonical v10.3
workspace. That confirms pinned checkout and review-file integrity only; the
12-source register remains pending/quarantine, the admission/manifests/
selection/runs directories are empty, and authority remains `0 AI / 0 human`.

The local Task 0 bootstrap probe is now green (`ready=true`) with Node 24.15.0,
Corepack 0.34.6, pnpm 9.15.0, Git 2.50.1, Python 3.14.4, and disposable
`pyarrow==25.0.0`; it accessed no candidate bytes and remains
`authorityEligible=false`. This closes only the tool bootstrap diagnostic.

The independent dependency-migration rereview in
`.superpowers/sdd/dependency-remediation-rereview.md` is **APPROVE** for its
bounded scope: the frozen offline install, both audits, Website typecheck,
38-test unit suite, Astro build, dual Vite graph, and ZIP dependency paths are
green. This does not approve a commit, corpus admission, release, or
deployment; clean commit-bound Task 6 receipts remain required.

Five files are already staged for the non-blocking beacon and large-JSON
boundary. Their focused run passes 2 tests; the corpus integration test is
intentionally skipped because no admitted corpus exists. The staged changes
prove that a hanging opted-in telemetry endpoint cannot keep a completed scan
alive and that JSON output above 128 KiB is flushed before a policy exit. They
still require explicit retention or splitting during clean-boundary review.

The recursive build was also rerun against the current bytes. Website builds
four static pages; Core codegen completes; `test:contract` then exits 1 on the
tracked observation/health changes plus 71 untracked schema/generated pairs.
This confirms the existing clean-boundary blocker rather than a new build or
typecheck regression.

The current dirty same-tarball Task 6 pair is also green: one packed artifact
(`dd04265be64829f03ccc210ce7bc40948c3489710d31b2d194a69e6c7363ef3f`) passes
9/9 on Node 22.22.3 and 9/9 on Node 24.15.0. Both receipts bind the same
commit, dirty status, 738-entry status snapshot, and status SHA
`a95e3b1706c722fac5ea6daf3fe318a9df47f75cdb5d8cc0d2c6ebe0530147d9`. This is
diagnostic dirty-worktree evidence for that 738-entry snapshot only.
Subsequent docs/config/catalog changes moved the historical snapshot to 744 entries
with status SHA `e1f2cef3147106f57b402372b94889461cad523e265f619f74f63e1286d7a28c`;
a clean commit-bound rerun remains required for release.

## Current execution override — 2026-07-13

The latest bounded verification supersedes the pending-rerun wording below.
SlopBrick passes 285 test files (5 skipped), 3,316 tests (9 skipped) with one
Vitest worker and a 2 GiB heap cap; Core, Engine, and Website tests pass
127/127, 59/59, and 37/37; recursive typechecks pass with zero errors; Core
schema fixtures validate; and the fresh SlopBrick build passes. The recursive
build is not green because Core's codegen-freshness guard detects uncommitted
generated `packages/core/src/generated/calibration-observation.ts` and
`packages/core/src/generated/health.ts` changes. No generated bytes were
discarded or staged.

The current same-tarball packed-consumer pair uses
`/tmp/slopbrick-task6-current-final-20260713/slopbrick-0.44.0.tgz` (SHA-256
`61d3b9e9c3a8d8d01b19425431f3dca56d395e30b719461f1c0e34ad9c398846`) and
passes 9/9 on Node 22.22.3 plus 9/9 on Node 24.15.0 with strict offline
installation. Both receipts agree on dirty builder commit
`81e79b9ec47f46f70575ae650beccd3a278b60e5`, 179 status entries, and status
SHA-256 `0142d40f2cca9afa8f713d7e5bf4ed9fc1f9e5e6895f73771fa2764e2574fe6f`.
This is dirty-worktree evidence only; no commit, tag, push, publication, or
deployment has occurred.
Those receipts are bound to their 179-entry snapshot. The current worktree is
now 613 status entries with status SHA
`f5b4404c0a72c4b81766767ad93d06687d35a81b45f0538aeb9473cd72e051df` after the
score-contract, provenance, CLI regression, workflow, website accessibility,
v10.3 run-artifact, and current verification slices;
no receipt has been rebound.

**Task 1B authority override — 2026-07-13:** the temporal and dedicated
adjudicator contracts are now consumed by the real SlopBrick admission path.
Temporal validation rejects orphan/malformed receipts and maps validated
attestations to explicit source IDs. Dedicated disagreement assignment,
adjudicator decision, and receipt artifacts are bound into optional per-source
ledger JSONL/hash/ID sections; the exact-two blinded peer receipt remains
unchanged. Core normalizes hash-sorted prior IDs to canonical peer order before
checking the graph. After the reduction refactor Core is green at 19 files/172
tests, and the SlopBrick admission-review/source-census/temporal matrix is
green at 30/30 under one
worker. The final RAM-bounded serial SlopBrick Vitest rerun is green at 293
files/3,406 tests with 5 skipped files and 9 skipped tests. This is still
diagnostic/fail-closed (`ready=false`,
`authorityEligible=false`, `eligibleUnits=0`): no corpus unit is admitted and
no calibration, release, publish, or deployment state changed. The top-level
review intentionally requires a complete decision ledger even when the direct
temporal helper can validate a standalone chain.

The current dirty snapshot is now 624 status entries with SHA-256
`8aeaff54a6612c3ddad819bc52e1c0b8a61cc45ad4edd6480741bcc644ba6c0a`; it is not
the earlier 179-entry Task 6 receipt and is not a clean-boundary release
receipt.

This closes only the named contract slice, not Task 1B or the release. The
next gate is reduction and reconciliation: consolidate repeated validators,
classify generated artifacts, prune/split historical evidence into reviewable
commits, then rerun recursive gates. The known Core codegen-freshness failure
on uncommitted generated observation/health files remains a dirty-worktree
limitation, not a clean-release pass.

**Reduction/reconciliation checkpoint — 2026-07-13:** the shared-primitives
pass is complete for this dirty boundary. Seven admission validators now share
the pure Core guard module; the inventory fell from 48 repeated definitions to
seven shared primitives plus 11 specialized wrappers. Core is 19 files/172
tests, schema fixtures validate, SlopBrick typecheck passes, and the artifact
classification is recorded in `artifact-classification.md`. Old contradictory
Task 1B sections are marked historical/superseded; no files were deleted or
moved. The branch is still unreleased, with no corpus admission or remote
mutation. The reduction snapshot was 627 status entries with SHA-256
`a0b7502b5dd57367216ba6bfb75c66f4f6b56056b68c5ebe0877bb31b77a0e48`.
Task 2A has since started at a bounded Core contract boundary; its runtime and
recursive gates remain open. Core's generated observation/health freshness
guard is still the known dirty-worktree limitation.

**Task 2A Core overlap-contract checkpoint — 2026-07-14:** the independent
review in `.superpowers/sdd/task-2-contracts-review-final.md` approves the
first four-contract Core slice. It adds the normalizer registry,
overlap-universe row/summary, and frozen overlap-policy schemas with generated
peers, fixtures, semantic validators, exact rational similarity/size helpers,
and canonical stream checks. Review fixes reject contradictory
registered-unassigned labels, locator control bytes, omitted/substituted
normalizer registries, and the wrong heap bound; malformed unknown rows now
fail closed without throwing. Bounded one-worker gates pass: focused 1/6,
full Core 20/178, typecheck, schema validation, and diff-check. The SlopBrick
normalizers/reader, disk-bounded ledger, resource receipt,
persistence/recovery, CLI, and real-corpus acceptance remain open. No corpus
or remote release state changed.

**Task 2A external-sort/shard fixture checkpoint — 2026-07-14:** the
independent review in `.superpowers/sdd/task-2-external-sort-review-final.md`
approves the fixture/diagnostic writer only. Canonical duplicate-preserving
JSONL sorting, contained no-clobber `generation_local` shard paths, lowered
shard/open-file/work budget enforcement, incomplete receipts, and unknown-file
preservation pass in 1 file/6 tests; SlopBrick typecheck and diff-check pass.
This is not the production spill/merge path: incremental verification,
heap/RSS/unit/wall telemetry, Core shard/resource schemas, adjacency/clusters,
checkpoint/recovery, CLI, and 452,382-unit acceptance remain open; no corpus or
remote release state changed.

**Task 2A SlopBrick normalizer/universe fixture checkpoint — 2026-07-14:**
the independent review in
`.superpowers/sdd/task-2-normalizers-universe-review-final.md` approves the
bounded diagnostic slice only. Strict UTF-8 normalization, explicit
covered/unsupported/unreadable outcomes, runtime implementation/fixture-hash
allow-listing, deterministic full SHA-256 five-token shingles, and canonical
chunked JSONL validation pass in 2 files/10 tests; SlopBrick typecheck and
diff-check pass. The reader intentionally buffers fixture input and is not the
452,382-unit production path. Incremental verification, resource limits,
malformed-language fixtures, collision evidence, external sort, shards,
persistence/CLI, and real-corpus acceptance remain open; no corpus or remote
release state changed.

**Task 2A exact-similarity fixture checkpoint — 2026-07-14:** the independent
review in `.superpowers/sdd/task-2-similarity-review-final.md` approves the
pure fixture slice only. Same-language prefix candidates, global exact-hash
duplicates, integer size filters, exact inclusive rational 0.80 confirmation,
both polarity bindings/sides, explicit AI↔human cross-side derivation, and
duplicate-shingle cardinality pass in 1 file/8 tests; the Core overlap
contract remains 1 file/6 and SlopBrick typecheck passes. Incremental stream
verification, external sort/shards, adjacency/clusters, resource receipts,
persistence/recovery, CLI, and 452,382-unit acceptance remain open; no corpus
or remote release state changed.

**Task 2A artifact-contract/runtime checkpoint — 2026-07-14:** the independent
review in `.superpowers/sdd/task-2-artifact-runtime-review-final.md` approves
the nine overlap artifact schemas, generated peers, nine valid/nine invalid
fixtures, and pure semantic validators. Core passes 21 files/181 tests;
focused artifact tests pass 3/3; typecheck, schema validation, and diff-check
pass under one worker and a 2 GiB heap cap. Production incremental overlap,
telemetry, persistence/recovery, publication, CLI, and real-corpus acceptance
remain open; no corpus or remote release state changed.

**Task 2A incremental stream checkpoint — 2026-07-14:** the independent review
in `.superpowers/sdd/task-2-incremental-reader-review-final.md` approves the
bounded single-consumer `AsyncIterable` universe reader. It strictly decodes
UTF-8, hashes exact incoming bytes, retains only the current line and scalar
counters, requires canonical JSONL/final newline, enforces the 32 MiB
unit/content bound, validates Core row/registry/order/count/status/unresolved/
hash contracts, and fails closed on early consumer termination. The review fix
permits registered-language `unreadable` rows (strict UTF-8 failure can retain
the language normalizer) while rejecting contradictory `unsupported` bindings.
Focused overlap-universe/stream tests pass 2 files/9 tests; SlopBrick typecheck
and diff-check pass. Production spill/merge, shards, telemetry,
persistence/recovery, CLI, and real-corpus acceptance remain open; no corpus or
remote release state changed.

**Task 2A bounded overlap computation checkpoint — 2026-07-14:** the
independent review in `.superpowers/sdd/task-2-overlap-builder-review-final.md`
approves the computation-only builder. Canonical stream hashing and
normalizer/status binding, bounded multi-pass postings/pair merges,
deduplicated candidate pairs, exact/inclusive-near edges, generation-local
edge/adjacency/cluster shards, resource accounting, and output
rehash/schema-checks are covered by 5 files/25 focused tests; SlopBrick
typecheck and diff-check pass. Checkpoint/resume, publication/recovery, CLI,
cross-artifact range/relationship verification, and real 452,382-row acceptance
remain open; no corpus or remote release state changed.

**Task 2A overlap authority publication/CLI checkpoint — 2026-07-14:** the
bounded local authority slice is independently approved. Core now carries the
four overlap generation/current/lock/transaction contracts with generated
types, semantic validators, and self-hash helpers (22 files/183 tests plus
schema validation). SlopBrick journals and fsyncs transaction/output
boundaries, materializes descriptor and primary leaves before intent, promotes
immutable hash-named generations before the current pointer, and recovers only
with matching lock/transaction bindings, generation-number chain, complete
tree, and current-pointer proof. The three overlap authority CLI commands are
wired; publication/CLI tests pass 2 files/14 tests under one worker and a 2 GiB
heap cap (40 tests across the seven-file overlap focus), with typecheck and
diff-check green.

This is not a release or corpus-admission approval. The real 452,382-unit run,
external tool receipt authority, complete fault/two-writer matrix,
checkpoint/resume, cross-artifact relationship verifier, clean recursive gate,
commit-bound evidence, and release operations remain open. Keep the staged
authority output local and fixture-bound; do not promote corpus labels or
publish a package from this checkpoint.

The website refresh passes Astro typecheck with zero errors (three existing
hints), builds four static pages, passes 38/38 unit tests and 16/16 strict
Playwright/axe checks with no serious or critical violations, and now has real
`/docs`, `/changelog`, and 404 routes. The workflow contract suite passes 4/4
and all workflow action references are pinned to reviewed full SHAs. These are
local candidate checks only: repository SHA-policy/ruleset enablement,
deployed-candidate performance/proof, and Cloudflare ownership/live-SHA
verification remain open.

The latest built-CLI self-scans are complete/valid and parse-error free with no
failed thresholds: package `.` 208/208 with 119 active and 274 suppressed
instances (AI Slop 4.6849, Repository Health 98.1111), and platform root `../..`
439/439 with 212 active and 550 suppressed (AI Slop 4.3307, Repository Health
94.8741). These are dirty-byte diagnostic receipts, not release evidence. The manual review is
closed for diagnostic triage; compression-profile and self-referential
rule/example findings remain visible, contextualized, and non-release evidence.
Corpus admission remains at Task 0 Steps 1–2:
the 329-source register has no admission manifests/runs and no new public
repos were pulled; v10.3 calibration and release approval remain blocked by
provenance, clean-boundary, and owner-authorization gates.

The finding-level evidence contract is now independently approved: 95 focused
tests cover typed exact/omitted spans, JSON preservation, MCP redaction and
boundedness, recursive details-drop status, and the public MCP documentation.
This improves finding usefulness without claiming v10.3 cohort provenance;
the rule-level calibration cohort/source remains explicitly unavailable. The
new optional per-rule calibration evidence contract is also approved: 8
SlopBrick calibration files/31 tests plus 3 Core contract tests preserve
sanitized rule counts through observations without exposing paths or snippets.

The Gate 1 heatmap output follow-up is also green: valid and empty/partial
`--heatmap --json <path>` requests write the requested score-free JSON envelope
without quiet-mode leakage. The focused heatmap/renderer/CI/watch matrix is
20/20; the broader output and manual keyboard-copy matrix remains open.

The Gate 3 report boundary is now explicit and fail-closed: a verified scan can
write four deterministic, path-free unavailable-only receipts through
`cal:report` (exit 1), bound to the run and upstream artifact hashes. The new
bounded point-estimate reducer, deterministic macro slice, and seeded
family-cluster bootstrap slice are covered by eleven focused tests. The
implemented synthetic runtime matrix is complete at 28 calibration files/553
tests, including the full select→run:init→verify→scan→verify→report chain;
the report path still does not consume admission-backed metrics or admit the
corpus, so metric promotion, coverage promotion, and smoke evidence remain
open.

The human report path now has explicit `AI-specific signals` and `Engineering
findings` lanes, filters `severity: off`, and explains empty lanes. Its TDD
slice was red 3/3 before implementation and green 3/3 afterward; the full
report suite is 160/160 and independent review found no P0-P2 issues. This is
presentation evidence only; it supports diagnostic triage but does not
authorize release or calibration use.

The follow-up self-scan review also found and fixed a score-message
contradiction: active AI-specific findings at a 4.7 score no longer render
`[NO SLOP]` or “no detectable AI slop”; pretty, composite, verdict, and brief
views now say `LOW` / “low amount of AI slop.” The regression, rebuilt golden
parity, full report suite, full package suite, typecheck, and independent review
are green. Compression-cohort and self-referential root-context findings remain
non-release context pending provenance and clean-boundary gates.

The legacy corpus is now physically centralized under
`/Users/cheng/corpus-expansion`: 224,903 positive/AI and 227,479 negative/human
filelist paths have zero exact overlap. The baseline trees contain 5,833 and
52,280 immediate regular source files, of which the maintained selected lists
contain 5,809 and 52,280; generated `.slop-audit` artifacts are quarantined and
no selected file depends on `/Users/cheng/ai-slop-baseline`.
The centralization receipt is
`/Users/cheng/corpus-expansion/v10.3/review/corpus-centralization-receipt-2026-07-13.json`.
This changes storage/provenance routing only; it does not promote labels or
start admission. The v10.3 positive/negative JSONL inventories were rebuilt
from the canonical paths (224,903 and 227,479 indexed, zero missing), and the
review validator remains green.

**Task 1A recovery-hardening checkpoint — 2026-07-13:** the current source
passes SlopBrick typecheck and four focused Task 1A files at `54/54` tests with
one worker and a bounded heap. The implementation now covers deterministic CAS
transaction/reservation identity, canonical observation and cleanup proofs,
verified-context acquisition/tool-authority joins, generation-zero acquisition
recovery, local authority publication/recovery, and the canonical-layout
recovery CLI. The fresh independent re-review approves this bounded Task 1A
slice. Lower-priority journal-temporary byte checking and materialization-only
authority replay remain plan-open; the plan ledger remains `96/178`
continuation and `2/76` admission items, and Gate 4 remains `verified_ai=0` /
`verified_human=0` with no admission artifacts or calibration run. No commit,
push, tag, release, npm publish, or deployment has occurred.

**Task 1B bounded register/review/census checkpoint — 2026-07-13:** an
independent superseding review approves the bounded slice recorded in
`.superpowers/sdd/task-1B-review.md`. The new Core contracts validate a
329-entry generation-0 register and exact review ID equality, hash-bind every
review to its register row, and enforce explicit baseline/repository partition
conservation (`58,089 + 394,293 = 452,382`). The non-persisting SlopBrick
`source:census` path requires the branded evidence context, returns zero/empty
source projections for unbranded or invalid inputs, keeps candidate claims in
quarantine, and reports final `eligibleUnits=0`. Its CLI realpath-checks root
relative inputs and rejects symlink escapes; aggregate rows cannot inflate the
additive total.

Core schema validation/typecheck/focused review tests and SlopBrick typecheck
plus census/path tests (`5/5`) are green with bounded resources. This does not
complete Task 1B: register-growth transactions, blind/temporal reviews,
record/decision ledgers, overlap/privacy/lineage, and the real Gate 4 census
remain open. The Core repository-level codegen freshness gate still detects the
pre-existing tracked generated observation/health changes. No corpus or remote
release state changed.

**Task 1B register-generation authority checkpoint — 2026-07-13:** the
bounded Core contract slice is independently approved in
`.superpowers/sdd/task-1B-register-authority-rereview.md` after the fix wave in
`.superpowers/sdd/task-1B-register-authority-fix-report.md`. It binds delta
source-generation hashes to the transaction and receipt, requires a completed
receipt-bearing phase with matching metadata, synchronizes the canonical Core
schema list, and enforces source/register path roles plus transaction-wide
collision checks. Focused authority/review tests pass `9/9`, and the full Core
suite passes `138/138`; schema validation, typecheck, codegen, and diff-check
also pass. The independent review limits approval to the Core contract:
runtime publication/recovery, source proposal/approval, blind/temporal
authority, record/decision, overlap/privacy/lineage, corpus admission, and
release gates remain open. The ledger remains `96/178` continuation and `2/76`
admission items, Gate 4 remains `verified_ai=0` / `verified_human=0`, and no
corpus or remote release state changed.

**Task 1B register-publication runtime checkpoint — 2026-07-13:** the
independent re-review in `.superpowers/sdd/task-1B-register-publication-rereview.md`
approves the bounded local filesystem runtime. The implementation report is
`.superpowers/sdd/task-1B-register-publication-report.md`. Focused register
tests pass `12/12`; the combined acquisition/register/path suite passes
`34/34`; Core passes `138/138`; SlopBrick typecheck and diff-check pass. The
runtime journals every durable boundary, uses lock/transaction-bound one- or
two-source publication, rejects symlink/path and byte/hash mutation,
revalidates current/register/receipt/tool bindings during recovery, supports
lock-only recovery, and preserves unknown transaction files. The CLI exposes
`register:publish-round` and `register:recover`.

This approval is local-runtime-only. The transaction-lock check-then-rename
multi-writer race is a documented P2 follow-up; source proposal/approval,
blind/temporal authorities, record/decision ledgers, overlap/privacy/lineage,
the real corpus census, Gate 4/5, calibration metrics, and release operations
remain open. The plan ledger stays `96/178` continuation and `2/76` admission
items. No corpus mutation, repository pull, commit, push, tag, release, npm
publish, or deployment occurred.

**Task 1B Core authority + bounded SlopBrick diagnostic checkpoint —
2026-07-13:** the independent approval is recorded in
`.superpowers/sdd/task-1B-core-authority-rereview.md`, with evidence in
`.superpowers/sdd/task-1B-core-authority-report.md`. The current hardening
slice adds strict Core joins for source
generation, independent blind source-review roles, rights evidence, temporal
receipts, records/decisions/samples/ledgers, canonical materialization IDs,
artifact bytes, timestamps, URLs, and SHA-256 decision references. Generated
fixed tuples are concrete and no longer widen to `unknown`. The new pure
SlopBrick `reviewAdmissionSources` boundary requires a branded verified
context, rejects malformed runtime input without throwing, delegates register
and review-set validation to Core, and keeps all candidate/structured inputs
quarantined with `eligibleUnits=0`; `source:census` follows the same
fail-closed path.

The bounded evidence is Core codegen/schema validation plus `29/29` focused
authority tests, SlopBrick review/census `9/9`, and the already approved
register-publication runtime `34/34` with SlopBrick typecheck green. This
checkpoint does not advance the admission ledger: full Task 1B acquisition,
structured record/decision authority, overlap/privacy/lineage, witness and
manifest work, the real census, Gate 4/5, and release remain open. The
schema-valid source-register fixture is shape-only; exact 329-entry semantic
validation remains in the in-memory contract fixture, and census
represented/unrepresented fields are register diagnostics rather than
admitted-record counts. No corpus/network/remote release state changed.

**Task 1B acquisition-round contract checkpoint — 2026-07-13:** the
independent bounded review in `.superpowers/sdd/task-1B-acquisition-round-rereview.md`
approves the offline Core contract, with evidence in
`.superpowers/sdd/task-1B-acquisition-round-report.md`. The new schemas and
generated types cover round authorization, approved source acquisition, source
receipt, round receipt, lock, and recoverable transaction. Validation joins
sources by the round authorization's explicit `sourceAuthorizationIds` order,
accepts permuted persisted arrays, and compares lock/transaction child tuples
structurally after JSON reload. It records distinct `toolReceiptId` and
`toolReceiptSha256` values plus round orchestrator ID/hash and child profile /
tool-receipt projections.

The contract rejects unsafe URLs and IPs, unrelated origins, peers outside the
resolved set, unapproved redirects, release byte-cap overruns, recovery
nonce/cap mutations, and staged child/orchestrator receipt mismatches. Core
passes 17 files / 163 tests, the focused acquisition/schema run passes 6/6,
schema validation/typecheck/codegen/diff-check pass, and adversarial probes
fail closed. The Core contract freshness command still reports only the
pre-existing tracked generated observation/health drift. This checkpoint is
offline contract evidence, not runtime acquisition or corpus admission:
network/filesystem crash proof, remaining Task 1B authorities, the real
census, Gate 4/5, and release gates remain open. The ledger stays `96/178` and
`2/76`, with no corpus mutation, repository pull, commit, push, tag, release,
npm publish, or deployment.

**Gate 4 census-prep audit — 2026-07-13:** the validator and admission
bootstrap probes pass, but the bootstrap is `authorityEligible=false`; no
candidate bytes were read. The 224,903 AI-positive and 227,479 human-negative
rows are quarantine/discovery records, not eligible counts. All 317 repository
reviews and all 12 source-register entries remain pending/quarantine. The
controlled HumanEval audit has 100/100 pairs but zero eligible after its source
contract; EvalPlus has zero eligible files. The truthful reviewed baseline is
therefore `verified_ai=0`, `verified_human=0`, with no admission directory,
census, witness, ledger, manifest, selection, run, or metric receipt present.
Tasks 1A–3B must build and review the offline authority before a deficit can be
computed or any public repository can be pulled; this audit made no network or
corpus mutation.

A bounded Task 1A Core contract slice follows the audit: five admission policy,
witness, tool-profile, invocation-intent, and tool-receipt schemas plus
generated peers and fail-closed validators pass 6/6 focused tests, Core
typecheck, and schema validation. It is not the complete Task 1A vertical path;
the evidence CAS/context brand, authority publication, `evidence:verify` CLI,
remaining schemas, and independent review are still open.

The incremental-cache policy is now explicitly fail-closed and covered by 9/9
contract tests: cached files do not enter ordinary or project-level findings,
partial runs score only rescanned files, persisted health/cache bytes stay
unchanged, and a full scan is required for project-wide parity. The rebuilt
packed-hook check is also green on both supported Node lines, including a
real-commit missing-binary failure case.

The score/provenance implementation slice is independently approved for the
incremental/partial/PR path. Successful per-file and project effective issue
projections drive headline and secondary scores; cache-only clone evidence
remains visible only in the audit envelope; `--rule` and `--include-rule`
duplicate opt-ins are covered; and failed results cannot feed business-logic
enrichment. The canonical AI score is additive over per-file log burdens with
a fixed cumulative scale, so clean files cannot dilute existing evidence and
harmful evidence cannot improve Repository Health. The public score contract is
versioned as `v2`, with constitution drift kept as a separate diagnostic
boundary. This does not close the
clean-boundary, corpus-admission, calibration, or release gates.

## Historical verification checkpoint — 2026-07-12 (superseded)

The current branch contains Gate 2 remediation slices for parser-backed comment
ratios, AST-proven CORS detection, project-level identical-block coordination,
effective diagnostic filtering, Unicode source positions, lexical security
boundaries, and nonblocking machine output. Focused serial tests and package
typechecks are green. The last serialized recursive gate before the latest
Task 6 harness and finding-context UX edits passed 3,151 tests in 264 files
(5 files skipped; 9 tests skipped), plus recursive lint/typecheck and build.
A later bounded serial SlopBrick rerun after the default-off renderer fix
passed 3,157 tests in 265 files (5 files skipped; 9 tests skipped). Focused
tests for the subsequent pretty-detail/resource-budget edits are green; a
fresh recursive rerun after all current edits remains pending. This is
evidence of scanner repair and Task 6 bounded verification, not a release pass.

Registry/release facts are deliberately split: the checkout is an unreleased
119-rule/27-category candidate, while the latest verified npm release is
v0.43.0 with 103 rules/22 categories. Historical v10.1 metadata covers 103
rules and distinguishes 576,750 analyzed files from 581,550 sampled paths.
Website facts now expose both candidate and published tracks and fail the
prebuild on registry/catalog/signal drift. The separate runtime/release-truth
Task 5 slice is focused-verified (runtime/packed-consumer 7/7); immutable
release-asset Task 5 is complete; and immutable Task 6 is bounded-contract
approved with isolated Node 22/24 packed receipts. The durable receipts are
marked dirty and currently carry different tarball hashes because no commit was
created; a same-tarball paired run remains temporary dirty-worktree evidence.
Corpus admission, tagging,
pushing, publishing, and deployment remain blocked by their explicit gates.

## Historical execution checkpoint — 2026-07-12 (superseded by the 2026-07-13 override)

Release-materialization Task 4C is complete at `04697806e`. Independent
specification, code-quality, and archive-security rereviews returned APPROVE
after all lifecycle and publication findings were fixed.

The focused Task 4 matrix passed 425/425 on Node 20.19.6, 22.22.3, and 24.15.0.
SlopBrick passed 2,956 tests with 9 skipped. Core 116, website 37, engine 59,
and SlopBrick 2,956 repository tests all passed.

Typecheck, build, and frozen install are green. Test repairs are `93cbc36eb`;
generated website package metadata is `81e79b9ec`.

The historical pre-remediation audit recorded 17 production and 20 full-graph
advisories, with no path through `yauzl`, `yazl`, `pend`, or `buffer-crc32`.
The bounded 2026-07-12 migration now makes both moderate-level audits return
zero advisories on the dirty bytes; independent migration review and a clean
commit remain open.

The historical partial staged precommit self-scan scored 7.2/100 with one low
compression-profile finding. The current package/root self-scans are now
complete and recorded below, but remain diagnostic rather than release proof.

The remaining Task 6 release prerequisite is a user-authorized clean builder
commit and commit-bound receipt rerun. No push, tag, release, publication, or
deployment has occurred, and the corpus remains provenance-gated. Detailed evidence is in
[`v0.45.0-execution-evidence.md`](./v0.45.0-execution-evidence.md).

The last bounded recursive verification (2026-07-12), before the latest
harness/UX edits, is recorded in that evidence:
core 116/116, engine 59/59, website 37/37, and SlopBrick 3,151 passed tests
(9 skipped), with 264 files passed and 5 skipped; recursive lint, typecheck,
test, and build were green on the then-current dirty bytes. Focused tests for
the newer edits are green; a fresh recursive rerun remains pending. The package
self-scan is complete/valid at 204/204 files with 114 active findings (113
low compression-profile and one isolated heaps-deviation) and 266 suppressed
findings; the broader root
self-scan is 435/435 with 208 active findings and 545 suppressed after the
security lexical-boundary fix. The remaining manual UX concern is the
compression-profile flood and mixed self-hosting fixtures. These results keep
the manual self-scan UX gate open; they do not authorize corpus admission or
release actions. Task 5 is complete as an isolated, non-release artifact slice;
Task 6 bounded implementation/review is complete, but its clean commit-bound
release gate remains open.

The runtime/release-workflow slice is now that bounded artifact: package engines,
doctor, docs, CI, PR review, and publish workflow agree on Node 22/24 and the
exact-artifact/OIDC boundary. Its focused runtime and packed-consumer tests are
7/7; immutable release-asset Task 5 is separately complete with 16 focused
tests, package typecheck/build, and independent approval.

## Recovery execution update (2026-07-09)

The bounded scanner-recovery tranche has now been implemented and independently
reviewed through the Dart contracts: source/package artifact parity, stable
config and monorepo discovery, explicit empty/partial scan outcomes,
current-data CI gating, and four default-off Dart rules. The exact commits,
focused test evidence, and fresh self-scan results are recorded in
[`v0.45.0-execution-evidence.md`](./v0.45.0-execution-evidence.md).

Fresh scans are no longer false zero-file successes:

- `packages/slopbrick/src`: 328 requested/analyzed, 0 failed, 0 skipped; exit 1
  because `meanSlop` failed (AI slop 25, hygiene 99.9006, security 100,
  repository health 75).
- repository root: 388 requested/analyzed, 0 failed, 0 skipped; exit 1 because
  `meanSlop` failed (AI slop 25, hygiene 99.7989, security 83.3333,
  repository health 75).

This evidence confirms discovery and completion accounting only. It does not
close Gate 0 or any broader score, MCP/core/engine, calibration/provenance,
site/operations, or release gate, and it does not authorize tagging, pushing,
publishing, or deployment.

Gate 0 containment evidence is now recorded in
[`v0.45.0-gate0-evidence.md`](./v0.45.0-gate0-evidence.md). The recovery branch
is `codex/v0.45-recovery`; exploratory calibration artifacts are hashed and
classified there. Release identity, language scope, Node LTS policy, and the
remaining accidental/release-metadata diffs are still unresolved.

---

## TL;DR

We **did not release v0.45.0**. We did most of the prep work (corpus, calibration data, training script, release notes, version bump) but stopped before tagging/publishing because:

1. The v10.2a calibration had a 42% chunk error rate on the pos side
   (timeout on 50MB minified JS / complex Java files)
2. The logistic regression baseline F1 was 0.0 (as expected — per-rule
   stats are too coarse; per-file training data exists but training
   script not yet updated to use it). Post-audit, that per-file data is
   also invalid because it drops zero-fire files and inherits unverified labels.
3. The user said "write a handoff" → stop and document

~~The plan is correct; the execution got 80% there. Pick up from
"finish Step 5 (real per-file training) + Step 6 (release)".~~

**Superseded:** start at Gate 0 of the continuation plan. Do not train or release from the current artifacts.

---

## What's done

### Code
- `package.json` bumped to v0.45.0 with updated description (119 rules, 27 categories, v5 corpus 452k files, opt-in ML baseline)
- `CHANGELOG.md` v0.45.0 entry written (4 Dart rules + corpus v5 + calibration v10.2a)
- `parser-kotlin.ts` still has an uncommitted one-line deletion; classify it during Gate 0 rather than assuming it was reverted
- `models/ai-baseline-v0.45.json` (3.2 KB aggregate-rule artifact) — produced by the training script; not a product model

### New scripts in `scripts/cal/`
- `train-ai-baseline.py` (12.5K) — Logistic regression training on v10.2a report. Reads markdown, parses per-rule stats, builds 50-dim feature vector, exports ONNX (if skl2onnx available) or JSON (fallback). Currently trains on per-rule stats only; per-file training in v0.46.0.
- `extract-per-file-features.py` (4K) — Extracts per-file feature vectors from the v10.2a chunk JSONs. Output: `/tmp/cal-results-v45/features.jsonl` (154,272 samples, 53k pos + 101k neg).
- `merge-v45.sh` (2K) — Combines pos (`/tmp/cal-results-v45/pos/`) and neg (`/tmp/cal-results-v45-neg/neg/`) JSONs into a single directory and runs the merge script. Used after bg_1 (neg) finishes.

### Corpus (v5 builder, `/Users/cheng/corpus-expansion/`)
- **v5 builder prototype** — `/Users/cheng/corpus-expansion/build-filelists-v5.sh` produced the current lists, but is superseded by the manifest-based v10.3 design.
- **Total corpus: 452,382 files** (227,479 neg + 224,903 pos)
- 22 new repos cloned in this session:
  - **Negative (human-written):** dotnet/runtime, dotnet/aspnetcore, dotnet/efcore, vapor, Alamofire, kubernetes, moby, prometheus, terraform, openproject, chatwoot, JetBrains/kotlin, laravel/framework, php-src, lua-language-server, postgres, requests, numpy, vuejs/core, sveltejs/svelte, withastro/astro
  - **Placed in the positive arm without adequate AI-authorship provenance:** kotlinx.coroutines, square/picasso, jekyll/jekyll, redis, dotnet/orleans, android/nowinandroid, samples (Flutter). Quarantine these labels pending file-level evidence.
  - **Spree, mastodon, forem**: cloned initially as "AI Ruby" candidates, then moved to `negative/` because they're human-written SaaS apps
- Corpus balance after v5 (imbalanced langs need v0.48.0 work):
  - .rb 0% pos (only jekyll = 88 files, mostly TS)
  - .kt 8% pos (kotlin compiler + nowinandroid)
  - .php 5% pos (laravel/framework)
  - .lua 5% pos (only local-language-server)

### Calibration data (v10.2a)
- **Location:** `/tmp/cal-results-v45/v10.2a-empirical.md`
- **Corrected post-audit accounting:**
  - expected: 375 positive + 380 negative = 755 chunks
  - positive: 216 outputs present = 126 valid + 90 error markers; 159 missing; 75,600 scanned files; 33.61% coverage
  - negative: 349 outputs present = 295 valid + 54 error markers; 31 missing; 177,000 scanned files; 77.81% coverage
  - total: 421 valid + 144 recorded failures + 190 missing; 252,600 scanned files; 55.84% file coverage
  - the polarity-dependent loss is missing-not-at-random, so v10.2a cannot support release verdicts
- **Historical pooled-fire distribution** (not statistical precision):
  - ≥65% (USEFUL): 10 (was 5 in v10.2 PASS D) — +5 promotion candidates
  - 50-65% (WEAK): 12 (was 11)
  - 33-50% (inverted-ish): 33 (was 26)
  - <33% (INVERTED): 49 (was 57) — -8 fewer inversions
- **Top 10 rules by the historical pooled-fire ratio:**
  - rust/unwrap-in-production 90.1%
  - ai/text-like-ratio 87.5% (corpus artifact — 7 pos, 0 neg)
  - kt/global-coroutine-scope 87.5%
  - kt/coroutine-cancellation-missing 75.7%
  - docs/stale-function-reference 73.0%
  - logic/zombie-state 68.4%
  - security/missing-auth-check 67.6%
  - go/struct-tag-inconsistency 66.7%
  - java/thread-sleep-in-loop 66.7%
  - logic/math-console-log-storm 65.5%

### Per-file training data (v0.46.0 foundation)
- **Location:** `/tmp/cal-results-v45/features.jsonl`
- **154,272 samples** (53k pos, 101k neg)
- Format: `{"file": "...", "pos": true|false, "features": [0, 1, 0, ...]}`
- Features: 15 rule-fire counts + 35 placeholder zeros (for v0.46.0 to expand)
- **Post-audit validity:** not a training dataset. It includes only files with at least one issue (70.14% of scanned positive files versus 57.20% of negatives), drops zero-fire files, and inherits the invalid corpus labels.

---

## Historical handoff checklist (superseded by the 2026-07-13 execution override)

The checklist below records the state at the original handoff. It is retained
for provenance, but the current status is defined by the dated verification
override at the top of this document and the continuation plan.

### Step 5 (training) — only partially done
- Logistic regression trained on per-rule stats: F1 = 0.0 (as expected; too few positive examples)
- **Post-audit correction:** do not update the trainer to consume this JSONL. Rebuild provenance, complete file accounting, real features, and repository-family splits first.

### Step 6 (release) — not done
- Have NOT tagged `v0.45.0`
- Have NOT run `pnpm generate:rules --check` (to verify catalog drift)
- Have NOT run `pnpm typecheck` (last typecheck was earlier today, may have stale state)
- Have NOT run the pre-push hook (`scripts/pre-push`): typecheck + full test + build
- Have NOT run the self-scan: `node bin/slopbrick.js scan --workspace src`
- Have NOT created the GitHub release: `gh release create v0.45.0`
- Have NOT reconciled the corrected 31 missing negative chunks or 159 missing positive chunks

### Post-audit blockers before any release
- settle version lineage and release scope
- repair score/CLI/worker/MCP/core/language contracts
- replace chunk-level loss with complete per-file accounting and bisection
- rebuild the corpus from provenance-backed records
- replace pooled-count ratios with denominator-aware statistics
- run and approve smoke, canary, and full v10.3
- complete package, consumer, documentation, website, CI/CD, and publication gates

---

## What to do next — superseded

The original training, timeout-only retry, destructive wipe, `git add -A`, tag, and release sequence has been removed because it would turn biased exploratory output into a public release.

Continue at **Gate 0** in [`v0.45.0-continuation-plan.md`](./v0.45.0-continuation-plan.md): preserve evidence, branch safely, settle version/scope, repair scanner contracts, rebuild provenance, implement lossless v10.3 accounting/statistics, then run smoke → canary → full calibration.

**Current override:** recovery has advanced through release-materialization Task
4C. Resume at Task 5 while the release and provenance gates remain closed.

---

## File locations cheat sheet

| File | Path |
|------|------|
| Calibration report (markdown) | `/tmp/cal-results-v45/v10.2a-empirical.md` |
| Per-rule calibration (raw) | `/tmp/cal-results-v45/pos/*.json`, `/tmp/cal-results-v45-neg/neg/*.json` |
| Per-file features (JSONL) | `/tmp/cal-results-v45/features.jsonl` (154k samples) |
| Trained model (JSON, no ONNX) | `/Users/cheng/platform/packages/slopbrick/models/ai-baseline-v0.45.json` |
| v5 corpus builder | `/Users/cheng/corpus-expansion/build-filelists-v5.sh` |
| v5 corpus filelists | `/Users/cheng/corpus-expansion/filelists/*.txt` |
| Master plan | `/Users/cheng/platform/packages/slopbrick/docs/calibration/master-plan-v0.45.md` |
| Plan validation (research review) | `/Users/cheng/platform/packages/slopbrick/docs/calibration/plan-validation-2026-07-09.md` |
| ML integration analysis | `/Users/cheng/platform/packages/slopbrick/docs/calibration/ml-integration-analysis.md` |
| Rule quality review | `/Users/cheng/platform/packages/slopbrick/docs/calibration/rule-quality-review.md` |
| v10.2 plan (historical/superseded) | `/Users/cheng/platform/packages/slopbrick/docs/calibration/v10.2-plan.md` |
| Rules literature + recommendations | `/Users/cheng/platform/packages/slopbrick/docs/calibration/rules-literature-and-recommendations.md` |
| This handoff doc | `/Users/cheng/platform/packages/slopbrick/docs/calibration/v0.45.0-handoff.md` |

## Background processes

- None currently running (bg_1 and bg_2 both completed)

## Git state

- `package.json` version: 0.45.0 (uncommitted)
- `CHANGELOG.md`: v0.45.0 entry added (uncommitted)
- `parser-kotlin.ts`: one-line uncommitted deletion remains; likely accidental, to classify in Gate 0
- New scripts: `train-ai-baseline.py`, `extract-per-file-features.py`, `merge-v45.sh` (uncommitted)
- New directory: `models/` (uncommitted)

---

## How to pick this up now

1. Read the linked continuation plan.
2. Treat this handoff as an artifact inventory, not an execution guide.
3. Preserve v10.2a, feature, and model files as exploratory evidence.
4. Resume at Task 5; do not train, change signals, push, tag, or release.

## Post-audit assessment

- corpus construction ideas are reusable, but labels and sampling need provenance review
- v10.2a is invalid for verdict decisions because coverage is incomplete and polarity-biased
- the pooled ratio is not deployment precision
- the feature JSONL and model are invalid for product evaluation
- the scanner and product contracts have upstream blockers
- release identity is unresolved because v0.44.0 was never verified as public

**Current conclusion: no v0.45 artifact is ready to ship as-is. Completion requires the gate sequence, not a longer timeout plus model retraining.**

## Historical current verification update — 2026-07-14 (superseded)

The latest implementation slice is independently approved for the bounded
fixture/local overlap authority boundary. Publication now has a 20-test
fault/CAS matrix covering six recoverable durable hooks (including the real
`cleanup-fsynced` boundary), a post-completion `lock-unlinked` case, and a
deterministic two-writer stale-parent race. The CLI boundary has 5 tests,
including child-process publish→verify, contention status, and
library-created pending transaction→CLI recovery→verify. The combined
publication/CLI focus passes **25 tests**; the complete seven-file overlap
focus passes **51 tests** under one worker and a 2 GiB heap cap.

The post-hardening full SlopBrick suite passes **301 files / 3,462 tests**
(5 skipped files / 9 skipped tests), and recursive typecheck passes with zero
errors. The recursive build remains intentionally blocked by Core's
codegen-freshness guard for uncommitted generated observation/health peers.
This is still a dirty-worktree verification, not a clean release receipt:
real-corpus acceptance, external tool-authority indexing, checkpoint/resume,
corpus admission, commit-bound evidence, packaging/release, and deployment
remain open. No corpus bytes or remote state changed.

## Historical Task 1B structured record/decision consumption checkpoint — superseded 2026-07-13

> Historical snapshot only. The superseding temporal/adjudicator checkpoint
> below is the current authority; do not use the deferred-work statements in
> this section as the present execution state.

The bounded structured-consumption slice is independently approved in
`.superpowers/sdd/task-1B-structured-records-rereview.md`, with evidence in
`.superpowers/sdd/task-1B-structured-records-report.md`. SlopBrick now consumes
records, review samples, blind assignments, decisions, receipts, and source
decision ledgers as one pure offline graph. Inventory candidate units are not
silently counted as admitted records: represented units come from the record
stream and the remainder is explicit unrepresented inventory. Orphan bytes,
partial/alias-conflicting graphs, duplicate or cross-source ledgers,
unledgered objects, missing record/source-review decision joins, missing source
rights evidence, and non-canonical decision JSONL fail closed.

Fresh evidence: SlopBrick focused review/census **22/22**, Core focused
authority **13/13**, full Core **17 files / 165 tests**, both typechecks, schema
validation, and `git diff --check`. This remains diagnostic-only
(`ready=false`, `authorityEligible=false`, `eligibleUnits=0`). The current
two-decision receipt contract cannot represent a one-decision third
adjudicator, so disagreement bundles remain rejected until a dedicated
adjudicator receipt contract exists. Evidence-ID resolution against the
verified context/index and temporal-attestation consumption remain deferred.
The plan ledger remains `96/178` continuation and `2/76` admission items;
Gate 4 is still quarantine-only (`verified_ai=0`, `verified_human=0`). The
current worktree snapshot is 613 status entries with SHA-256
`f5b4404c0a72c4b81766767ad93d06687d35a81b45f0538aeb9473cd72e051df`.

## Historical current verification update — 2026-07-14 (superseded)

The bounded authority verifier now checks cross-envelope provenance rather than
only individual file hashes. `verifyOverlapArtifactRelations` validates the
index, resource receipt, and ledger schemas/self-hashes, joins their universe,
policy, normalizer, and tool-receipt authorities, checks index/ledger and
coverage/count/completion relationships, and binds each envelope to its
generation receipt. Publication, recovery, and read-only verification invoke
the same immutable-tree check.

The latest evidence is publication **24/24**, CLI **5/5**, combined
publication/CLI **29/29**, and seven-file overlap **55/55**. The full
SlopBrick package gate is **301 files / 3,466 tests** (5 skipped files / 9
skipped tests), Core is **22 files / 183 tests**, schema validation and
recursive typecheck are green, and the package build succeeds with the known
non-fatal Zod DTS warnings. `git diff --check` is clean.

This is still a bounded fixture/local slice, not corpus or release approval.
The recursive build remains blocked by uncommitted Core generated observation/
health peers; the 452,382-unit corpus, external tool receipt, checkpoint/
resume, admission, clean commit-bound evidence, release, and deployment gates
remain open. No corpus or remote state changed.

The post-gate worktree census is **757 status entries / 456 untracked files**
with approximately **48,784 untracked lines**; the tracked diff against `main`
is **460 files, +62,175/-6,139 lines**. Current status-list SHA-256:
`7e1175279aa03f5dec68d66f6fed278bf2bbe40587326e76d998f60a7f338e50`.

`HEAD` is `81e79b9ec`, 260 commits ahead of `main`; the current uncommitted
tracked delta against `HEAD` is **301 files, +18,859/-4,735 lines**. The
460-file figure is cumulative branch-vs-`main` history, so a clean-boundary
review must assess the dirty delta plus untracked files rather than treating
the entire branch as one new patch.

## Scope interpretation and clean-boundary audit — 2026-07-14

The branch size overstates visible product change. Against `main`, the recovery
branch is 292 files and +43,812/-1,900 lines: approximately +10,124 runtime
source lines, +16,120 tests/fixtures, +15,822 documentation/plans, and +320
schema/generated peers. SlopBrick itself is approximately +9,723 runtime,
+14,500 tests, and +12,774 documentation. This is mostly proof and recovery
infrastructure. The baseline scanner was not declared unusable; the audit
found release-trust gaps around score/effective-set semantics, incomplete
scan/accounting behavior, claims, corpus provenance, and package receipts.

The current read-only dependency audit reports zero moderate advisories for
both production and full graphs, but the migration remains uncommitted. The
ignored Node 22/24 Task 6 receipts are not a valid pair: they use different
tarballs/receipt shapes and one is an older dirty snapshot. The live worktree
has 716 status entries (5 staged, 445 untracked). The next gate is therefore a
scoped, explicitly authorized clean Task 5+6/dependency boundary, preserving
unrelated files, followed by recursive gates and a same-tarball Node 22/24
receipt rerun. This does not authorize push, tag, publication, deployment, or
corpus mutation.

## Gate 1/2 bounded follow-up — 2026-07-14

The score review added an assembly-level matrix (`score-contract-matrix.e2e`:
6/6) and randomized score invariants (`score-contract-properties`: 3/3,
150 cases) for effective-set separation, bounds, monotonicity, and renderer
parity. Valid Markdown and HTML reports now include detailed accounting when
the scan producer supplies it; selection-only reports show unknown detailed
counts as `n/a` (`renderer-contract`: 10/10). These are synthetic and local
correctness gains. Historical wording in this checkpoint that listed health-wire
as wholly open is superseded by the final 2026-07-14 bounded health-wire matrix;
the broader all-output/partial-watch UX contract remains open, alongside
runScan/subcommand, finding-evidence, corpus, clean-builder, and release gates.

## Current package gate refresh — 2026-07-14

The post-fix rebuilt SlopBrick suite is green at **303 files / 3,477 tests**
(5 skipped files / 9 skipped tests) under one Vitest worker and a 2 GiB heap
cap. Core, Engine, SlopBrick, and Website typechecks are green with zero
errors; Website reports only three existing hints. `git diff --check` is
clean. This remains dirty-worktree evidence: the Core generated observation/
health freshness guard still blocks the recursive build, and the clean
Task 5+6/dependency receipt boundary, real corpus admission, release, and
deployment remain open. No remote or corpus state changed.

At this earlier package-gate checkpoint the dirty snapshot was **729 status
entries** (448 untracked); the later bounded validity/toolchain follow-up near
the end of this handoff supersedes that count. Neither snapshot is a
clean-commit or release receipt.

## Historical workspace-binary self-scan correction — 2026-07-14 (superseded)

The bare `slopbrick` self-scan command was found to resolve a stale packed
`node_modules/slopbrick` created by the Task 6 consumer test rather than the
current workspace build. The authoritative rerun uses
`corepack pnpm --filter slopbrick exec -- node ./bin/slopbrick.js ...` and is
complete/valid at 227/227 analyzed files, 157 active of 532 total issues,
AI Slop 17.1909, hygiene 99.4702, security 100.0000, and Repository Health
92.9647. It has zero scan failures and exits 1 only because findings are
present. The stale packed result is discarded; the plan and AGENTS instructions
now require the explicit workspace bin. No corpus or remote state changed.

## Human-report evidence parity — 2026-07-14

The bounded report-parity follow-up is complete for HTML: issue rows now show
escaped exact snippets and source spans when typed evidence is available, or an
explicit omission reason when it is not. HTML passes **15/15** focused tests;
the combined CLI/MCP/report parity slice passes **268 tests**. This closes a
renderer contract gap only. Most rules still do not emit exact spans, so this
does not claim universal finding evidence, v10.3 provenance, or release
readiness. Typecheck/build and `git diff --check` remain green; the recursive
Core codegen freshness gate, clean-builder receipt, corpus admission, release,
and deployment gates remain open.

## Bounded validity, threshold, and toolchain follow-up — 2026-07-14

DB/docs empty and partial scans now fail closed with score-free, explicit
`not-applicable` envelopes; a docs-only workspace can still receive a measured
docs score. Complete scan JSON now agrees with the CLI gate on
`failedThresholds`, fixing the self-scan mismatch where stderr named
`meanSlop` but JSON reported no failed threshold. Core freshness verification
now includes staged/untracked schema/type files and schema/generated-peer
parity. Website dev startup regenerates its product facts, and release/review
workflow jobs inherit the exact repository pnpm version rather than floating
on major `9`.

Verification is refreshed at **307 SlopBrick test files / 3,540 tests**, Core
**23/185**, Engine **5/59**, Website **38/38** unit tests, and **16/16**
Playwright/axe checks. Typechecks and builds are green aside from the
intentional Core dirty codegen freshness boundary (with the known non-fatal
Zod declaration warnings). The workspace self-scan is **227/227** complete,
**157/531** active/total findings (**374 default-off suppressed**), with
`failedThresholds=["meanSlop"]`; it is
diagnostic dirty-worktree evidence only. The current worktree is **757 status
entries** (**456 untracked**). No corpus, commit, push, release, publish, or
deployment state changed.

## Scope interpretation and next gate order — 2026-07-14

The large diff should be read as release-trust work, not as proof that the
scanner was fundamentally broken. The current tracked diff against `main` is
**460 files, +62,175/-6,139 lines**: approximately **17,736** SlopBrick docs,
**1,684** root docs/specs, **20,904** test/fixture lines, **12,375** SlopBrick
runtime lines, **1,896** Core schema/runtime/test lines, **429** Engine lines,
**380** Website lines, **3,730** tooling/CI/release lines, and **3,041** other
package lines. The current dirty snapshot has **757 status entries**,
including **456 untracked files** (approximately **48,784 lines**), mostly
the uncommitted v10.3 admission contracts, generated peers, and their tests.
Its reproducibility hash is status SHA-256
`7e1175279aa03f5dec68d66f6fed278bf2bbe40587326e76d998f60a7f338e50`.
The default-on scan surface moved far less than the raw line count, but the
release evidence surface grew substantially. Earlier 292-file/43,812-line
figures in this handoff are historical checkpoints, not current state.

The implementation is now frozen except for defects that block an existing
gate. The next work is gate execution, in order: an explicitly authorized
clean recursive builder/dependency receipt; a same-tarball Node 22/24
consumer receipt and independent migration review; evidence/tool-authority
approval and real v10.3 corpus admission; calibration and UX review; then the
documented commit/tag/GitHub-release/OIDC publication path. No further broad
subsystem should be added before those gates produce evidence.

The only post-freeze code correction in this checkpoint is a narrow MCP stdio
transport fix: a final valid JSON-RPC request without a trailing newline is
now flushed before shutdown. The regression was observed red before the fix
and is green afterward; the six MCP files pass **110/110** and SlopBrick
typecheck remains green. This is a protocol-boundary correction, not a new
feature tranche.

## Corpus authority audit refresh — 2026-07-14

The read-only corpus check confirms **317/317** pinned checkouts resolve under
`/Users/cheng/corpus-expansion/` with matching recorded HEADs. The declared
inventories remain **224,903 AI rows** (SHA-256
`4511193d10dc39ac7589047e945c531d20752b3344b1d22e96a9d348bc7d34f0`) and
**227,479 human rows** (SHA-256
`85ef9452e96977ec21578a8f87026d374ac510daad20fd34af8d6d363b38c215`).
No new repository pull is justified. Authority remains
`verified_ai=0`/`verified_human=0` because the 329-entry generation-0
admission register, source reviews, evidence/tool/acquisition receipts,
complete ledgers, and witness are absent. AI-positive/human-negative remains
the declared polarity only; no admission manifest or flat receipt should be
created until those authorities exist. Corpus bytes were not changed.

The read-only `cal:admission:genesis` diagnostic composes the real frozen
inputs into 329 quarantined source reviews and conserves all 452,382 selected
units, while explicitly reporting `authorityEligible=false` and
`persisted=false`. It does not create `review/admission` artifacts. Task 4
Step 1 therefore remains an external, authorized publication gate rather than
a completed calibration claim.

The final RAM-bounded SlopBrick package rerun passes **306 test files / 3,519
tests** (5 skipped files / 9 skipped tests) at one worker, including the
genesis contract and MCP transport regression. It exits 0 after regenerating
the 119-rule registry/catalog; the known non-fatal Zod declaration warnings
remain build noise only. The genesis adapter also now fails closed when a
repository's declared polarity disagrees with the inventory arm or when
normalized repository byte totals do not conserve the selected summary; its
focused contract is 4/4 and the real-corpus diagnostic remains
zero-authority.

## Synthetic clean commit-boundary consumer diagnostic — 2026-07-14

A disposable copy of the current platform bytes was committed only under
`/tmp/platform-clean-cVXytA` at synthetic commit
`f47afa0847ad0c8a3b0c8a507f3ee32e8a691946`, excluding the unrelated root
artifacts `TODO.md`, `.astro/types.d.ts`, and `src/env.d.ts`. It had zero
status entries and status SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

The recursive typechecks passed with zero errors (Website retained three
non-blocking hints), and the full recursive build passed for all four buildable
packages. One offline
`slopbrick@0.44.0` tarball passed the packed-consumer suite **9/9** on both
Node 22.22.3 and Node 24.15.0, and the paired-receipt assertion passed with
tarball SHA-256
`84a5c5115b59b1566611fcdb4c41891eb630a8d25a7259f8af92e866717bf0fc`.
This is diagnostic proof that the dirty-boundary guard protects a buildable
snapshot; it is not the owner-authorized branch commit or release evidence.

The complete JSON genesis diagnostic was captured outside the repository at
`/tmp/v103-genesis-diagnostic-20260714.json` (530,426 bytes,
SHA-256 `aec85e3901069d6dc1aba23b782b387e4ff0c4965160455e82c734bccd48be87`).
The independent Core validator returned `ok=true` for all 329 reviews and
452,382 quarantined units; no admission artifacts or corpus bytes were written.

## Task 2A terminal checkpoint/resume and sidecar-integrity follow-up — 2026-07-14

The overlap builder now writes four canonical, no-clobber, fsynced local
checkpoints when a task-scoped invocation intent is supplied. The phases are
`postings`, `candidate_pairs`, `exact_edges`, and `clusters`; each checkpoint
binds the universe, normalizer registry, policy, input/output shard hashes,
and continuation cursor. A terminal `clusters` checkpoint resumes without
reading the universe stream or source bytes. Intent mismatches, altered
checkpoint bytes, substituted shards, and non-terminal resume requests fail
closed. Terminal recovery now validates Core index, ledger, and resource
receipt shapes/self-hashes and their authority/completion joins before it
returns a result; this closes a real local-sidecar tamper gap.

The checkpoint/resume suite is **4/4** green, the overlap/builder/scale/
publication/CLI/resume and health-structure focus is **90/90**, SlopBrick
typecheck passes, and `git diff --check` is clean. Only terminal resume is
implemented; phase continuation from an earlier checkpoint remains open. The
full RAM-bounded package gate is now **306 files / 3,519 tests** (5 skipped
files / 9 skipped tests), with no corpus, commit, push, release, publish, or
deployment mutation.

## Disposable source-bound self-scan refresh — 2026-07-14

The rebuilt workspace binary was rerun in a disposable copy with project
memory and telemetry disabled, one worker, and no repository mutation. The
JSON artifact is `/tmp/slopbrick-self-scan-current-20260714-final.json`
(585,972 bytes, SHA-256
`20cb0bcad4b02925e974c7f61585ef683e0b798b5e62be680e51099203f8f623`). It
analyzed **227/227** files with zero failure/skip/parse/timeout/crash/internal
outcomes, **157 active / 531 total** findings, **374 default-off suppressed**,
AI Slop **17.1909**, hygiene **99.4702**, security **100.0000**, health
**92.9647**, and `failedThresholds=["meanSlop"]`. Exit 1 is the expected
policy result, not a crash; this remains diagnostic rather than release
evidence. The brief terminal output is useful and internally coherent. The
headline now names the metric (`Repo's AI-slop score is low`) so the
lower-is-cleaner direction is not ambiguous; the focused UX regression passes.

The follow-up scan after that correction retains the same valid 227/227
coverage, 157 active / 531 total findings, and expected meanSlop threshold
failure. The new disposable JSON receipt is
`/tmp/slopbrick-self-scan-current-20260714-ux.json` with SHA-256
`4e52627940f6d4d5a6419387ed42a26fa866f15895804723cee56fdecaf30593`.

## Genesis blocker-count diagnostic refresh — 2026-07-14

The read-only `cal:admission:genesis` output now includes deterministic,
overlapping blocker counts. Receipt:
`/tmp/v103-genesis-diagnostic-20260714-blockers.json`, 531,205 bytes,
SHA-256
`727263f7806c8039ec2afce4079d48778d8cb5965a6b7eef81be69eaa7d683a3`.
It records **329** reviewed sources and **452,382** quarantined additive units,
with **0 candidate sources / 0 candidate units / 0 eligible units**;
`authorityEligible=false` and `persisted=false` remain explicit. The counts
are `authorship_unproven` 323/452,382, `evidence_unresolved` 327/394,293,
`family_unknown` 318/394,293, `license_scope_ambiguous` 10/58,089,
`materialization_unverified` 3/0, `review_incomplete` 329/452,382,
`source_bytes_unbound` 1/58,089, `source_inventory_open` 1/0, and
`source_wide_quarantine` 329/452,382 (source reviews / additive units).
Counts overlap and exclude the aggregate view from additive totals. Focused
genesis coverage is **5/5** and typecheck is green. No corpus bytes or
admission/selection/run/remote state changed; promotion remains unsupported.
A second read-only run with the same frozen inputs and timestamp produced the
identical diagnostic SHA-256, confirming byte-level reproducibility.

## Final full package and health-wire matrix gate — 2026-07-14

The latest RAM-capped serial SlopBrick package gate passes **307 test files /
3,540 tests** (5 skipped files / 9 skipped tests), including the genesis,
persisted-health, heatmap, and doctor validity coverage. Core remains **23/185**
and Engine **5/59**; recursive typecheck remains green. The focused validity and
renderer matrix is **121/121**. The package build has only the known non-fatal
Zod declaration warnings. This is still dirty-worktree evidence: the clean
codegen boundary, real 452,382-unit admission/calibration, and release gates
remain open. No corpus or remote state changed.

## Historical synthetic clean-boundary consumer rerun — 2026-07-14 (superseded by corrected v3 below)

The current source bytes were copied into the disposable snapshot
`/tmp/platform-clean-current-AAX30H`, omitting only the unrelated root files
`TODO.md`, `.astro/types.d.ts`, and `src/env.d.ts`. It was committed locally at
synthetic commit `0118fdeb02fe712b482729f1ea612aa8496989a0` and has zero status
entries with status SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
The snapshot predates later documentation-only plan/evidence edits, so its
implementation commit is not byte-identical to today's full documentation.
The dedicated recursive typecheck log is
`/tmp/platform-clean-current-AAX30H-typecheck.log` (SHA-256
`77a8ba5d727e74962cdb7fb9f4dd04f7db7e699e54f3e03eb09e82b86e17b93f`).

That clean snapshot passes recursive serial typecheck (zero errors and the same
three non-blocking Website hints), recursive build, and recursive serial tests:
**307 SlopBrick files / 3,540 tests passed**, with 5 skipped files / 9 skipped
tests. The build emits only the known non-fatal Zod declaration warnings.

The exact packed `slopbrick@0.44.0` artifact is
`/tmp/task6-clean-current-AAX30H/slopbrick-0.44.0.tgz` with SHA-256
`c048fe0ac0e4d7fb1a988e7899c3a6765b87f8cc6e09c13d63521ce548beb826`.
The packed-consumer contract passes **9/9** on Node 22.22.3 and **9/9** on
Node 24.15.0 using strict offline installation and local-process-only CLI
execution. The current receipt directory is
`/tmp/task6-clean-current-AAX30H/receipts-v2/`; the external packer is recorded
as Node 24.15.0, npm 11.12.1, and pnpm 10.34.5. Receipt hashes:

- Node 22: `0858f8f9125fc3303071ffcd9e10dac83900004174f964c673e1bbbacef12919`
- Node 24: `f393f8dc7ca5fee719fd2561c9b48ff249c3ae837529b03ced355936675dacf5`

Both receipts bind the same tarball, synthetic commit, clean status, and
successful install, ESM import, CJS require, materialize, select, and verify
stages. This is diagnostic clean-snapshot evidence only. It is not the
owner-authorized branch commit, release receipt, npm publication, or deployment;
the real clean boundary and corpus admission/calibration gates remain open.

## Partial-watch heatmap validity matrix — 2026-07-14

The watch integration suite now passes **45/45** under one worker with the
built SlopBrick CLI present; without that binary the suite is explicitly
skipped rather than reporting vacuous passes. Fourteen new
cases cover partial scans (one valid file plus one parse failure) for both the
global `scan --watch` flag and the `watch` subcommand across human, JSON, SARIF,
HTML, JSON-file, HTML-file, and quiet heatmap modes. Every mode preserves the
partial/incomplete discriminator and accounting, omits all five canonical
numeric headline fields from both streamed output and file-output streams, and
remains stable after debounce. Manual keyboard-copy/terminal UX is still the
only remaining part of this output-contract slice.

## Corrected packed-consumer provenance rerun — 2026-07-14

The clean-boundary diagnostic was rerun after correcting the packed-consumer
test harness so an externally packed tarball preserves the packer's provenance
instead of overwriting it with the consumer install runtime. The disposable
snapshot is synthetic commit
`79d44f58d8e3da7c87588cf9d7d78e42ce021642`, with zero status entries and the
empty-status SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

The artifact `/tmp/task6-clean-current-AAX30H-v3/slopbrick-0.44.0.tgz` has
SHA-256 `c048fe0ac0e4d7fb1a988e7899c3a6765b87f8cc6e09c13d63521ce548beb826`.
The packed-consumer contract passes **9/9** on Node 22.22.3 and **9/9** on
Node 24.15.0 under strict offline installation and local-process-only CLI
execution. The corrected receipts record the external packer as Node 24.15.0,
npm 11.12.1, pnpm 9.15.0; consumer install pnpm 10.34.5 remains separately
recorded in `stages.install.pnpmVersion`.

- Node 22 receipt SHA-256: `7197b4860be4f1cd8457ecea274186a5cf7b6be68cc16287b838509f3feb5541`
- Node 24 receipt SHA-256: `ebb31a46401f6f55f9f17bc4918fe93700a32493701990c2625c46cbb45ca8b2`

This strengthens the diagnostic only; it does not authorize commit, release,
publication, deployment, or corpus admission.

## Live package-local self-scan cleanup — 2026-07-14

The correct workspace command is
`corepack pnpm --filter slopbrick exec -- node ./bin/slopbrick.js`; a bare
`slopbrick` command can resolve the stale packed consumer copy in the root
`node_modules`. The package-local binary completed **227/227** files with zero
parse, timeout, crash, internal, or skipped outcomes. Removing eight concrete
dead-code leftovers reduced the result to **147 active / 519 total** findings,
**372** default-off suppressed findings, AI Slop **16.877047461130914**,
Engineering Hygiene **99.7**, Security **100**, and Repository Health **93.1**.
Exit **1** is the configured `meanSlop > 15` policy result, not a runtime
failure. The remaining 140 findings are repetitive-compression diagnostics in
the new calibration modules and must remain visible until admitted-corpus
calibration proves their disposition.

The post-cleanup JSON receipt is
`/tmp/slopbrick-selfscan-clean-20260714/report.json`, SHA-256
`720d103e6acd40a64da5d5844ecf7c5b1d3b20fed74f185d785ba99a893302bb`.
It records `scoreValidity=valid`, `completionStatus=complete`, and only the
expected `meanSlop` threshold failure.

Focused cleanup tests pass **81/81**; the full serial SlopBrick gate passes
**307 files / 3,540 tests**, with 5 files / 9 tests intentionally skipped.
Package build, recursive typecheck, and diff checks also pass. No corpus,
commit, push, release, publication, or deployment state changed.

## Corpus row-versus-content overlap audit — 2026-07-14

The inventory totals are unique source-keyed rows, not deduplicated content
files: 224,903 AI `sourceId`s and 227,479 human `sourceId`s, with zero
cross-polarity source-ID overlap. The same quarantined inventories contain
4,405 cross-polarity `normalizedPath` overlaps and **3,522** cross-polarity
`contentSha256` overlaps (unique hash counts: 190,310 AI and 217,837 human).
Within-polarity duplicate hash keys are 18,691 AI and 3,135 human.

This does not promote or reject any file, but it is a concrete leakage/family
review blocker. “Zero exact-path overlap” refers only to source-keyed path
identity; it does not mean zero content overlap. Overlap clustering, rights,
and label review must precede any admission or calibration claim. No corpus
bytes changed.
The recursive-canonical diagnostic receipt is
`/tmp/v103-corpus-overlap-diagnostic-20260714.json`, SHA-256
`afb1550087ff5a2644da4f25d84e082c4666307c1fef6ef5f637527748ac305b`;
it is non-persisting and does not promote labels.

## Owner-authorized commit and bounded corpus resolution — 2026-07-14

The owner authorized a local commit and corpus resolution, with no push, tag,
release, npm publication, deployment, or remote mutation. The intended boundary
was committed at `8de6cfb712620e5fd554f73e677ac1a9d6cb7947`
(`feat: complete v0.45 calibration and release hardening`): 754 intended paths,
`+67,843/-4,736`. Unrelated untracked `TODO.md`, `.astro/`, and `src/` were
excluded. After the commit, recursive typecheck/build and the Core
contract/codegen freshness gate pass; Core is **23 files / 185 tests**, and the
SlopBrick package build passes with only known non-fatal Zod declaration
warnings. The branch is 265 commits ahead of `main`; the four follow-up commits
contain only plan/evidence documentation.

One bounded Mendeley Data acquisition was made to resolve coverage without
pretending that labels are provenance: `HumanVSAI_CodeDataset`, DOI
`10.17632/kjh95n54f8.1`, archive **2,251,807 bytes**, SHA-256
`c6cb156a8fa627c9228b7798ea7d25be9327a4d1f72f40b16ddae3e6d807e0c4`. Static
inspection records **10,000 rows** (5,000 AI / 5,000 Human), four languages,
1,780 prompt-summary rows, three shared problem IDs, and zero exact
cross-polarity code hashes. Candidate code was never executed.

The persisted audit is
`/Users/cheng/corpus-expansion/v10.3/review/mendeley-humanvsai-audit-2026-07-14.json`
(file SHA-256
`dee8a555f886be193718ac77333d8ef3fd0ad853e5e26b80bf6fc19cad68c655`, embedded
self-hash `83cc5f0b00de398f8a514243ea6416d072481303e7a5c88025b001c1fa3e7941`);
the proposal receipt is
`/Users/cheng/corpus-expansion/v10.3/review/acquisition-proposal-mendeley-humanvsai-2026-07-14.json`
(self-hash `7cf431b353569cef62d578fab4fce5c7c94ecffa6c0e055e239a3b51cbf85414`).
The source register remains unchanged at its 12-entry draft. Because the
dataset lacks row-level generator/output evidence, a complete CodeNet rights
chain, immutable projection, lineage/overlap/privacy ledgers, and two reviewer
decisions, it remains quarantine-only: `verified_ai=0`, `verified_human=0`,
`eligible_gold=0`, with no manifest, selection, run, or calibration output.

The genesis and overlap diagnostics are now copied under
`/Users/cheng/corpus-expansion/v10.3/review/admission/genesis-diagnostic-2026-07-14/`
and remain `authorityEligible=false`; their hashes are
`727263f7806c8039ec2afce4079d48778d8cb5965a6b7eef81be69eaa7d683a3` and
`afb1550087ff5a2644da4f25d84e082c4666307c1fef6ef5f637527748ac305b`.
The Mendeley projection is now materialized below. Next: recover
generator/rights/lineage evidence, publish an acquisition-bound register delta,
and rerun the deterministic census. Do not pull more repositories until that
census proves a measured deficit.

## Quarantine projection materialization — 2026-07-14

The Mendeley CSV is now materialized as a deterministic quarantine projection
at
`/Users/cheng/corpus-expansion/v10.3/sources/benchmarks/humanvsai-code-dataset/projection-v1/`.
It contains 10,000 independently hashable regular files plus a canonical JSONL
manifest, preserving each exact UTF-8 `Sample_Code` field with no added
newline. Code bytes total 6,195,562; manifest SHA-256 is
`588afb3fe94fdde5958ee4aeac9a5ce3b0680cff61d329ec91998819206c6eab`; the
path/hash/size tree SHA-256 is
`261dc463129e50be4ed7e573b83d67b168828afc03ad4c37a7b35f9a0ace6452`.
The projection receipt's canonical self-hash is
`80d9ad37ee5c89cba806482280646c6332087b0b2e82150a1af2e59924e64abf`.

The projection preserves `AI -> verified_ai` and `Human -> verified_human`,
stable source-row record IDs, problem IDs, language, source claim, byte count,
and content/materialization hashes. A streaming reread matched all 10,000
manifest rows/files with no orphan or duplicate path. This closes only the
materialization diagnostic: generator evidence, upstream rights,
family/lineage, privacy/quality, and independent review remain unresolved.
The receipt therefore keeps `verified_ai=0`, `verified_human=0`,
`eligible_gold=0`, and `promotionAllowed=false`; no manifest, selection, run,
or calibration output was created.

## Second bounded source: OSS-forge HumanVsAICode — 2026-07-14

The final second source in this bounded round is the official Hugging Face
`OSS-forge/HumanVsAICode` dataset at revision
`03cac5dd2096c8901cb7a74e2ecbd26624ada497`, with a declared `cc-by-sa-4.0`
license. Its Python and Java JSONL payloads total **1,156,626,118 bytes** and
match LFS SHA-256 values
`f1347d75e05bdd4ddf4a7538ded47c3499027b014c2cd27898c0ef4735a5308d` and
`fe845d53204f4a8fe1e04d7a826163a253e2404f80171845c482c6005e384c20`.
Together with Mendeley, the acquisition round totals **1,158,877,925 bytes**;
no third source is being pulled.

The static audit is
`/Users/cheng/corpus-expansion/v10.3/review/ossforge-humanvsaicode-audit-2026-07-14.json`
(file SHA-256
`21848613f87f5d4b1b596dc3bb61a868bf5abdc44c64793a16be0f516a5caa0a`, embedded
self-hash `82241981700f836c2cf10921e1630a954c3e333bb5166af149f349bf9611ee65`).
It parsed **507,045** rows (285,249 Python / 221,796 Java), while the card says
507,044. Every row has `hm_index`, docstring, human code, and named ChatGPT,
DeepSeek, and Qwen fields; 38,683 DeepSeek and 3,209 Qwen values are blank.
Exact human/AI hash overlaps are 2/20/38 by model, with cross-AI overlaps too.

The source is still quarantine-only. It has no row-level generation timestamp,
prompt/output hash ledger, exact model revision, upstream repository path/commit,
or per-record license chain; the human side is a lossy CodeSearchNet/HMCorp
reconstruction. One checkout/two languages also cannot satisfy the smoke
diversity gate. The proposal receipt is
`/Users/cheng/corpus-expansion/v10.3/review/acquisition-proposal-ossforge-humanvsaicode-2026-07-14.json`
(self-hash `e76a14dea3691a5b301dab1db20f1e3f1b222e492f0e3c987d294f453d0faf39`).
The source register remains unchanged and authoritative counts remain
`verified_ai=0`, `verified_human=0`, `eligible_gold=0`; no manifest, selection,
run, or calibration output exists. The blocker is now provenance/rights and
lineage, not corpus volume.

## Bounded acquisition-round diagnostic receipt — 2026-07-14

The cumulative round receipt is
`/Users/cheng/corpus-expansion/v10.3/review/acquisition-round-diagnostic-2026-07-14.json`
(file SHA-256
`ed6865550f8750594b62f22613498be73bc8c5b65ee2e339e46424db13d35ac9`, embedded
self-hash `f711406daa4819bf9f92d685b7bbe769a901c723354bbee7daa6e0aecf916d7b`).
It proves the two-source/5-GiB bound was respected: two sources totaling
1,158,877,925 bytes, both static-only and both non-promotable. The 12-entry
source register is byte-identical before and after (SHA-256
`1bed3c36f105413411b52a62b8a8543115da5c11c3c4c25ecc8025dfb0ba5deb`). The
round ends with `verified_ai=0`, `verified_human=0`, `eligible_gold=0`, and no
manifest, selection, run, or calibration output. The next work is provenance,
rights, lineage, and admission—not another download.
