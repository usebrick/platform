# v0.45.0 Recovery Execution Evidence

**Recorded:** 2026-07-09
**Scope:** recovery tasks 0–5 plus explicitly recorded v10.3 implementation
slices; this is not release evidence.

This report records the fresh verification available after the scanner recovery
work. It does not promote calibration results, close the broader continuation
plan, or authorize a version tag, publish, or deployment.

**Latest-evidence pointer — 2026-07-16:** the dated clean-gate, provenance,
and packed-consumer sections in the current continuation plan and handoff
supersede the historical sections below. The hardened clean workspace gate
passes Core **33/241**, Website **11/47**, Engine **5/59**, and SlopBrick
**333 files / 3,749 tests** with **5 skipped files / 9 skipped tests**. The
current clean packed artifact is bound to package commit
`a7d3e0c18dd8b10a4f70094ad2b3b634c92ae7ab`, SHA-256
`f8ec4417e0fd38e15d6ffbca852b6f0c44ad5bcb413f6a36ab976f8902a06ef7`, and
paired Node 22/24 receipts pass **9/9** each. The read-only corpus census
remains **329/329** reviewed sources, **452,382** quarantined units, and **0
eligible** units. This report remains diagnostic and does not authorize
corpus admission, calibration, release, publish, or deployment.

## External corpus review-artifact refresh — 2026-07-14

The read-only validator at
`/Users/cheng/corpus-expansion/v10.3/review/validate-review-artifacts.mjs`
exits 0 (`v10.3 corpus review artifacts: valid`). It verifies the pinned
checkouts and review JSON/JSONL artifacts, but does not promote labels. The
inventories contain **224,903 declared AI** and **227,479 declared human**
rows. In this historical 2026-07-14 snapshot, the 12-source register was
pending/quarantine and the canonical `admission/`, `manifests/`, `selection/`,
and `runs/` directories were empty. That snapshot is superseded by the current
329-entry source register; authority is still `verified_ai=0` and
`verified_human=0`.

## Task 0 admission-tool bootstrap refresh — 2026-07-14

The dependency-free bootstrap probe returns exit 0 with `ready=true` when run
against Python 3.14.4 plus a disposable `/tmp` target containing
`pyarrow==25.0.0`. Node 24.15.0, Corepack 0.34.6, pnpm 9.15.0, Git 2.50.1,
the Node API probe, and the `node18` tsup target check pass. Candidate bytes
were not accessed, network policy remains denied, and `authorityEligible` is
false. This is local diagnostic evidence only; it does not close the
affected-package gate or promote corpus authority.

## Dependency migration independent rereview — 2026-07-14

`.superpowers/sdd/dependency-remediation-rereview.md` independently approves
the bounded uncommitted migration scope. The reviewed package manifests,
lockfile, Vitest filesystem allowance, Astro 7 compatibility change, and ZIP
dependency paths are internally consistent. Frozen offline install, production
and full audits (zero advisories), Website typecheck (zero errors/hints), the
38-test Website unit suite, and the four-page Astro build all pass. The review
does not replace the owner-authorized clean commit or fresh commit-bound Node
22/24 Task 6 receipts.

## Recursive build boundary refresh — 2026-07-14

`NODE_OPTIONS=--max-old-space-size=2048 corepack pnpm -r build` was rerun on
the current bytes. Website prebuild/build still succeeds and emits four static
pages. Core codegen completes, then `test:contract` exits 1 because the
freshness guard correctly sees the tracked observation/health changes and the
71 untracked schema/generated admission pairs. No new compile or package-build
failure was observed; this remains the intentional clean-boundary blocker.

## Current dirty same-tarball Task 6 pair — 2026-07-14

The current workspace was packed once at
`/tmp/slopbrick-task6-current-20260714/slopbrick-0.44.0.tgz` with SHA-256
`dd04265be64829f03ccc210ce7bc40948c3489710d31b2d194a69e6c7363ef3f`. The
offline packed-consumer test passed **9/9** on Node **22.22.3** and **9/9** on
Node **24.15.0**, including the pair assertion. Both receipts agree on commit
`81e79b9ec47f46f70575ae650beccd3a278b60e5`, dirty status, 738 status entries,
and status SHA-256
`a95e3b1706c722fac5ea6daf3fe318a9df47f75cdb5d8cc0d2c6ebe0530147d9`. This
closes the same-tarball consumer contract for that 738-entry dirty snapshot
only. That historical tree was 744 entries with status SHA
`e1f2cef3147106f57b402372b94889461cad523e265f619f74f63e1286d7a28c`, so it is
not current clean-commit, npm-release, or OIDC publication evidence.

## Current verification override — 2026-07-13

The current dirty worktree is 613 status entries with status SHA
`f5b4404c0a72c4b81766767ad93d06687d35a81b45f0538aeb9473cd72e051df`; the
receipt-bound Task 6 pair remains intentionally bound to its earlier 179-entry
snapshot and has not been rebound. A fresh SlopBrick build passes. The RAM-
bounded serial full suite passes 285 files (5 skipped), 3,316 tests (9
skipped); Core, Engine, and Website suites pass 127/127, 59/59, and 37/37;
recursive typecheck has zero errors; and Core schema fixtures validate. The
recursive build remains blocked by the Core codegen-freshness guard for
uncommitted generated `packages/core/src/generated/calibration-observation.ts`
and `packages/core/src/generated/health.ts`.

**Task 1B temporal/adjudicator checkpoint — 2026-07-13:** Core now rejects
orphan/malformed temporal receipts, resolves temporal targets through an
explicit source map, and validates a dedicated disagreement assignment,
adjudicator decision, and receipt without expanding the exact-two peer
receipt. Optional adjudicator JSONL/hash/ID sections are consumed by the
SlopBrick per-source decision ledger; prior IDs are normalized from hash order
to canonical peer order. After the reduction refactor the Core suite is 19
files/172 tests and the bounded SlopBrick admission-review/source-census/
temporal matrix is 30/30 with one
worker. This remains diagnostic-only and fail-closed (`ready=false`,
`eligibleUnits=0`); no corpus, calibration, release, or deployment state
changed. After rebuilding Core and SlopBrick from the final source boundary,
the RAM-bounded serial Vitest rerun is green at 293 files/3,406 tests with 5
skipped files and 9 skipped tests; the earlier stale-source-map failure was
therefore an artifact freshness race, not a test failure. The current dirty snapshot is
624 status entries with SHA-256
`8aeaff54a6612c3ddad819bc52e1c0b8a61cc45ad4edd6480741bcc644ba6c0a`; it is
not a clean-boundary receipt. The next required action is the scope-control
reduction pass before Task 2A, not another feature tranche.

**Reduction/reconciliation checkpoint — 2026-07-13:** the pass is now complete
for the dirty source boundary. Seven Core admission validators share one pure
primitive module (48 repeated guard definitions reduced to seven shared
primitives plus 11 specialized wrappers), with four focused regression tests.
Core typecheck and bounded suite are green at 19 files/172 tests; schema
fixtures, SlopBrick typecheck, and diff-check are green. The artifact
classification records the intentional schema/generated/fixture tranche and
the historical/research partitions; contradictory Task 1B sections are marked
superseded. No files were deleted, moved, or acquired. The reduction snapshot was
627 status entries with SHA-256
`a0b7502b5dd57367216ba6bfb75c66f4f6b56056b68c5ebe0877bb31b77a0e48`. Task 2A
has since started at a bounded Core contract boundary; the Core codegen
freshness failure remains a dirty-worktree limitation.

**Task 2A Core overlap-contract checkpoint — 2026-07-14:** the independent
review in `.superpowers/sdd/task-2-contracts-review-final.md` approves the
first four-contract Core slice. Four schemas, generated peers, index entries,
fixtures, semantic validators, exact rational Jaccard/size helpers, and
canonical stream checks are green. The review fixes reject contradictory
registered-unassigned labels, locator control bytes, omitted/substituted
normalizer registries, and the wrong heap bound; malformed unknown rows now
return `ok=false` instead of throwing. Bounded one-worker gates pass: focused
overlap 1/6, full Core 20/178, typecheck, schema validation, and diff-check.
The SlopBrick reader/normalizers, disk-bounded ledger, resource receipt,
persistence/CLI, and real-corpus acceptance remain open; no corpus or remote
release state changed.

**Task 2A exact-similarity fixture checkpoint — 2026-07-14:** the independent
review in `.superpowers/sdd/task-2-similarity-review-final.md` approves the
pure fixture slice only. Same-language prefix candidates, global exact-hash
duplicates, integer size filters, exact inclusive rational 0.80 confirmation,
both polarity bindings/sides, explicit AI↔human cross-side derivation, and
duplicate-shingle cardinality pass in 1 file/8 tests; the Core overlap
contract remains 1 file/6 and SlopBrick typecheck passes. Incremental stream
verification, external sort/shards, adjacency/clusters, resource receipts,
persistence/recovery, CLI, and 452,382-unit acceptance remain open; no corpus
or remote release state changed.

**Task 2A external-sort/shard fixture checkpoint — 2026-07-14:** the
independent review in `.superpowers/sdd/task-2-external-sort-review-final.md`
approves the fixture/diagnostic writer only. Canonical duplicate-preserving
JSONL sorting, contained no-clobber `generation_local` shard paths, lowered
shard/open-file/work budget enforcement, incomplete receipts, and unknown-file
preservation pass in 1 file/6 tests; SlopBrick typecheck and diff-check pass.
This is not the production spill/merge path: incremental verification,
heap/RSS/unit/wall telemetry, Core shard/resource schemas, adjacency/clusters,
checkpoint/recovery, CLI, and 452,382-unit acceptance remain open; no corpus or
remote release state changed.

**Task 2A artifact-contract/runtime checkpoint — 2026-07-14:** the independent
review in `.superpowers/sdd/task-2-artifact-runtime-review-final.md` approves
the nine overlap artifact schemas, generated peers, nine valid/nine invalid
fixtures, and pure semantic validators. Core passes 21 files/181 tests;
focused artifact tests pass 3/3; typecheck, schema validation, and diff-check
pass under one worker and a 2 GiB heap cap. Production incremental overlap,
telemetry, persistence/recovery, publication, CLI, and real-corpus acceptance
remain open; no corpus or remote release state changed.

**Task 2A incremental stream checkpoint — 2026-07-14:** the independent review
in `.superpowers/sdd/task-2-incremental-reader-review-final.md` approves the
bounded single-consumer `AsyncIterable` universe reader. Strict UTF-8,
exact-byte hashing, canonical JSONL/final-newline, 32 MiB unit/content limits,
Core row/registry/order/count/status/unresolved/hash checks, and early-return
failure are covered. The review fix permits registered-language `unreadable`
rows while still rejecting contradictory `unsupported` bindings. The focused
overlap-universe/stream suite passes 2 files/9 tests; SlopBrick typecheck and
diff-check pass. Production spill/merge, shards, telemetry,
persistence/recovery, CLI, and real-corpus acceptance remain open; no corpus or
remote release state changed.

**Task 2A bounded overlap computation checkpoint — 2026-07-14:** the
independent review in `.superpowers/sdd/task-2-overlap-builder-review-final.md`
approves the computation-only builder. It binds the canonical stream hash and
normalizer/status authority, uses bounded multi-pass postings/pair merges,
deduplicates candidate pairs, emits exact and inclusive-near edges plus
generation-local edge/adjacency/cluster shards, and rehashes/schema-checks
outputs. The focused overlap suite passes 5 files/25 tests; SlopBrick
typecheck and diff-check pass. Checkpoint/resume, publication/recovery, CLI,
cross-artifact range/relationship verification, and real 452,382-row acceptance
remain open; no corpus or remote release state changed.

**Task 2A SlopBrick normalizer/universe fixture checkpoint — 2026-07-14:**
the independent review in
`.superpowers/sdd/task-2-normalizers-universe-review-final.md` approves the
bounded diagnostic slice only. Strict UTF-8 normalization, explicit
covered/unsupported/unreadable outcomes, runtime implementation/fixture-hash
allow-listing, deterministic full SHA-256 five-token shingles, and canonical
chunked JSONL validation pass in 2 files/10 tests; SlopBrick typecheck and
diff-check pass. The reader intentionally buffers fixture input and is not the
452,382-unit production path. Incremental verification, resource limits,
malformed-language fixtures, collision evidence, external sort, shards,
persistence/CLI, and real-corpus acceptance remain open; no corpus or remote
release state changed.

**Task 2A overlap authority publication/CLI checkpoint — 2026-07-14:** the
bounded local authority implementation is independently approved for this
fixture/local slice. Core's four
overlap generation/current/lock/transaction contracts have generated peers,
semantic validators, and self-hash helpers; Core passes **22 files / 183
tests** plus schema validation. SlopBrick stages descriptor and primary leaves
before transaction intent, fsyncs journal/output boundaries, promotes immutable
hash-named generations and then the current pointer, and rejects symlinks,
orphan/substituted leaves, unknown cleanup paths, broken generation chains, and
current-pointer mismatches during recovery. The CLI commands
`authority:overlap`, `authority:overlap:recover`, and
`authority:overlap:verify` are wired. The publication/CLI focused gate passes
an initial **2 files / 14 tests**; the follow-on status/fault update is
recorded immediately below. SlopBrick typecheck and diff-check are green under
one worker and a 2 GiB heap cap.

This is fixture/local authority evidence only. It does not establish the real
452,382-unit corpus run, external tool-authority receipt indexing, full
fault/two-writer coverage, checkpoint/resume, cross-artifact relationship
verification, corpus admission, clean recursive release gates, commit-bound
receipts, or release readiness. No corpus bytes or remote release state changed.

**Task 2A fault/CAS and CLI-E2E update — 2026-07-14:** the follow-on bounded
publication test now covers six recoverable durable hooks (including the real
`cleanup-fsynced` boundary), the post-completion `lock-unlinked` boundary, and
one deterministic two-writer stale-parent race; it passes **20/20** and was
repeated three times for race stability. Contention and post-completion status
are explicit and machine-readable. The CLI test passes **5/5** for read-only
and contention boundaries plus child-process publish→verify and
library-created pending transaction→CLI recovery→verify. The combined
publication/CLI focus passes **25/25** and the seven-file overlap focus passes
**51/51** under one worker and a 2 GiB heap cap. The full SlopBrick suite passes
**301 files / 3,462 tests** (5 skipped files / 9 skipped tests), while recursive
typecheck passes with zero errors. Recursive build remains blocked only by the
known Core codegen-freshness guard on uncommitted generated observation/health
peers. These are dirty-worktree gates; real-corpus, external tool authority,
corpus admission, clean commit-bound receipts, packaging, and release remain
open.

The affected score/provenance suite passes 12 files/234 tests, and the
standalone `suggest`/`badge` validity regressions pass in the 111-test
scan-completion file plus 7 badge/color checks, including stale-health and
empty-Git-subset cases. The score/provenance suite includes the
monotonic Repository Health regression, per-file additive score formula,
incremental-cache accounting, project-rule filtering, PR/diff projection,
duplicate-rule CLI opt-ins, failed-result business-logic exclusion, renderer
contracts, and the source/built CLI/MCP golden. Independent review approved the
explicit successful-file/project effective issue projection: cache-only clone
evidence remains in the broad audit envelope but cannot enter headline,
secondary, or PR scoring.

The read-only Gate 4 corpus-prep audit (2026-07-13) is intentionally not a
census receipt: the review validator passes, but the bootstrap reports
`authorityEligible=false` and does not access candidate bytes. Centralized
inventories contain 224,903 declared-AI rows and 227,479 declared-human rows;
all remain quarantine/discovery evidence. All 317 repository inventory records
are pending review, all 12 source-register entries are quarantine/pending, the
controlled HumanEval 100/100 pair audit yields zero eligible pairs, and EvalPlus
yields zero eligible files. The truthful reviewed baseline is
`verified_ai=0`, `verified_human=0`; admission/census/witness/ledger/manifest/
selection/run/metric artifacts are absent. Tasks 1A–3B are the next safe gate;
no public repository was pulled and no corpus or network state changed.

A bounded Task 1A Core contract slice then passed 6/6 focused tests, Core
typecheck, and schema validation. It adds the five durable policy,
witness-policy, tool-profile, invocation-intent, and tool-receipt schemas with
generated peers and fail-closed self-hash/action/network validators. The
codegen guard still reports only the two pre-existing dirty observation/health
generated peers. CAS/context branding, authority publication, the
`evidence:verify` CLI, remaining admission schemas, and independent review are
not implemented by this slice and remain open.

The bounded heatmap output contract now routes valid and not-applicable/partial
`--heatmap --json <path>` requests through the shared dispatcher, writes the
requested file without quiet-mode leakage, and preserves score-free validity
envelopes. The heatmap/renderer/CI/watch focused matrix passes 20/20; the
broader all-output and manual keyboard-copy matrix remains open.

The follow-on projection contract omits canonical score aggregates and opt-in
score explanations from incomplete JSON/SARIF/MCP outputs while retaining
non-gating diagnostics; its focused checks pass 44 tests plus 28 selected
score-explanation/partial tests. The lossless calibration-evidence contract
adds sanitized, deterministic per-rule counts through observations; its
focused checks pass 8 SlopBrick calibration files/31 tests plus 3 Core
contract tests. Neither slice creates a v10.3 cohort or release evidence.

The latest locally available built-CLI diagnostic self-scan receipts are
complete/valid with zero failures or skips and no failed thresholds. From the
package workspace (`.`), 208/208 files were analyzed (119 active, 274
suppressed; AI Slop 4.6849; Engineering Hygiene 99.9501; Security 100;
Repository Health 98.1111). From the platform root (`../..`), 439/439 were
analyzed (212 active, 550 suppressed; AI Slop 4.3307; Engineering Hygiene
99.7989; Security 83.3333; Repository Health 94.8741). These dirty-byte
receipts are diagnostic UX/completion evidence only; they are not v10.3
corpus, calibration, or release evidence.

The canonical AI score now uses per-file log burdens followed by fixed-scale
additive accumulation. Clean files cannot dilute existing evidence, and
adding unsuppressed harmful evidence cannot improve Repository Health. The
public score contract is versioned as `v2`; constitution drift remains a
separate diagnostic boundary rather than silently entering headline scoring. No
corpus admission manifest, calibration run, clean commit-bound receipt, tag,
push, publication, or deployment was created by this verification.

The website refresh also passes Astro typecheck with zero errors (three
existing hints), a four-page static build, 37/37 unit tests, and 13/13 strict
Playwright/axe checks with no serious or critical violations. The workflow
contract suite passes 4/4 and all current workflow action references are pinned
to reviewed full SHAs; repository SHA-policy/ruleset settings and Cloudflare
ownership/live-deployment proof remain open operations gates. The local strict
accessibility gate is therefore closed for the candidate; deployed performance
and production proof remain open.

The v10.3 execution slice now also has bounded worker and durable-attempt
evidence: the original focused worker/durable slice passed 24 files/523 tests;
the current full v10.3 calibration glob passes 28 files/553 tests.
`workerCount` is validated and caps in-flight scans,
output order is stable, and completed attempts replay only when the canonical
run-manifest hash matches. `run:init` and post-scan `verify` are now covered;
  independent registry/signal/config revalidation, bounded point-estimate
  metrics, actual admission-backed metrics/report production,
  scanner/feature-schema hash binding, and admission-backed v10.3.2 remain open
  and are not claimed here. The explicit `scan --resume` boundary now refuses
  existing final artifacts, requires an expected completed chunk, and fails
closed on stale/corrupt attempts before scanner invocation.

The scanner/feature hash follow-up was audited and intentionally left open: no
versioned authoritative scanner artifact or runtime feature-schema byte set
exists yet, while the worker may select generated `dist` code or a source
fallback. Hashing either current representation would create false provenance;
the schema/CLI migration must define the canonical bytes first. No code or
corpus state changed in that audit.

The status-only `cal:report` boundary is now implemented and focused-tested:
it repeats the frozen-input, checkout, observation, failure, and coverage
verification before writing deterministic, path-free `rule-metrics.json`,
`language-metrics.json`, `report.md`, and `logs/report.jsonl` receipts marked
`status: unavailable`. The receipts carry only upstream artifact hashes and an
explicit `eligible-cohort-unavailable` or `coverage-gate-not-promotable`
reason; they contain no fabricated metrics or corpus-eligibility claim, exit
1 for the unavailable result, and refuse existing outputs. Actual metrics,
review, and admission-backed v10.3.2 remain open. The writer now publishes
sequentially and removes files created by a failed write, so an I/O error does
not leave a partial derived-artifact bundle.

The bounded pure metrics reducer is now independently exercised 11/11. It takes
an explicit verified-AI/verified-human eligible-file census and complete rule
catalog, rejects mixed runs and schema-incompatible terminal metadata, and
emits deterministic file-level point estimates and Wilson 95% intervals
(recall, FPR, smoothed LR+, balanced/prior PPV, zero-fire, and ineligible
states). The same pure module now provides deterministic equal-weight
family-cluster and language macro point estimates with pooled audit counts,
diagnostic F1, and seeded family-cluster percentile intervals for LR+,
balanced PPV, and F1 (`cluster-bootstrap-percentile-v1`, capped at 10,000
replicates). Admission-backed report wiring and eligibility remain open; this
slice does not promote corpus labels or create release evidence.

The deterministic Gate 3 smoke extension runs the complete 100-AI/100-human
metric fixture twice and compares the normalized reducer output byte-for-byte;
the focused smoke/metrics/edge matrix is 21/21. It also covers unequal arms,
zero-fire and zero-cell rates, and execution paths containing spaces. A
follow-on 8/8 slice validates persisted observation/failure/coverage bytes
through AJV and Core runtime validators, snapshots telemetry/flywheel/baseline/
`AGENTS.md` sidecars, and runs the bundled worker through the checkout boundary
with `SLOP_AUDIT_CACHE=0`. The added full cross-command slice passes 1/1 and
the 28-file/553-test calibration glob covers the synthetic runtime matrix.
Broader worker/checkout production parity remains open.

## Latest bounded continuation slices — 2026-07-13

The v10.3 control plane now has a no-clobber `run:init`/`init` command and a
post-scan `cal:verify` stage. Initialization derives a path-free canonical
`run-manifest.json`, binds the frozen selection and checkout-map digest, checks
that every selected repository is uniquely covered, and refuses existing final
artifacts. Verification rechecks the manifest, selection, checkout binding,
observations, failures, coverage, and frozen hashes. The focused calibration
suite is now 28 files/553 tests; `cal:report` now adds unavailable-only derived
artifact receipts, while actual metrics, scanner/feature hash binding, and
corpus admission remain gated. The report log is a status-only receipt, and
the explicit resume boundary is implemented; neither is a substitute for the
remaining full calibration gates.

Gate 1's bounded matrix now covers empty, tiny, large-clean, AI-only,
hygiene-only, backend-only, and mixed isolation. The matrix passes 4/4 and the
matrix-plus-metrics run passes 50/50; no production defect was demonstrated in
this slice, so SCORE-01/SCORE-02 remain open.

Gate 2's fail-closed renderer slice treats both `scoreValidity=incomplete` and
legacy `completionStatus=partial` as non-gating. Incomplete JSON omits the four
canonical headline fields, the project-level `compositeScore`, and opt-in
`scoreExplanation`; SARIF omits score aggregates from `properties`; MCP
persisted-health suggestions omit score aggregates; and human output includes
detailed accounting when available. Compatibility health snapshots retain
diagnostic numeric fields pending the final wire/schema decision.

### Historical Task 1B structured record/decision consumption — superseded 2026-07-13

> Historical snapshot only. The temporal/adjudicator checkpoint later in this
> evidence file supersedes the deferred-work statements below.

Independent review approves the pure offline structured slice in
`.superpowers/sdd/task-1B-structured-records-rereview.md`; implementation
details are in `.superpowers/sdd/task-1B-structured-records-report.md`.
SlopBrick consumes records, samples, assignments, decisions, receipts, and
source ledgers as one fail-closed graph. Record-derived representation now
separates represented units from inventory candidates and exposes the
unrepresented remainder. Orphan bytes, partial/alias-conflicting graphs,
duplicate/cross-source/unledgered objects, missing decision joins, missing
source-rights evidence, non-canonical decision JSONL, temporal adjudication,
and incomplete eligibility-bearing record decision sets are rejected.

Fresh checks: SlopBrick review/census **22/22**, Core focused authority
**13/13**, full Core **17 files / 165 tests**, Core/SlopBrick typechecks, Core
schema validation, and `git diff --check`. The result remains
`ready=false`, `authorityEligible=false`, `eligibleUnits=0`; it does not read
or mutate corpus bytes and does not advance Gate 4. The exact-two receipt
contract cannot represent a one-decision third adjudicator, so disagreement
bundles remain fail-closed until a dedicated adjudicator receipt contract is
implemented. Evidence-ID resolution against the verified context/index and
temporal-attestation consumption remain deferred. The current worktree snapshot
is 613 entries with SHA-256
`f5b4404c0a72c4b81766767ad93d06687d35a81b45f0538aeb9473cd72e051df`.

## Verified implementation tasks

| Task | Evidence | Result |
| --- | --- | --- |
| 0 — source/ESM/CommonJS parity | Commit `e8b7a2d4f`; source help, `tsc`, `tsup`, package-worker and artifact tests | APPROVE |
| 1 — config/monorepo discovery | Commit `83e1d2894`; 14 focused tests and `tsc` | APPROVE |
| 2 — empty/partial outcomes | Commits `160e149bc`, `9dbb02afe`, `e388b4a0a`; final review approved | APPROVE |
| 3 — authoritative CI outcome | Commits `f94805af8`, `f5d87528c`, `0da9150f5`; 26 focused tests and `tsc` | APPROVE |
| 4 — Dart rule contracts | Commit `158ee8011`; `vitest run tests/engine/rule-hints.test.ts tests/engine/signal-strength-guardrails.test.ts tests/rules/dart` — 3 files, 17 tests passed; `tsc --noEmit` passed; independent review approved | APPROVE |

The four Dart rules remain `defaultOff: true`, `DORMANT`, and zero-valued in
signal metadata. No calibration evidence is claimed for them.

## Historical fresh self-scan evidence — superseded 2026-07-14

Commands were run against the built CLI with one worker thread, JSON output,
telemetry disabled, and color disabled. Both scans returned exit code **1**
because the current `meanSlop` threshold failed. Both reports were complete:
no files failed or were skipped.

| Workspace | Status | Requested | Analyzed | Failed | Skipped | AI slop | Hygiene | Security | Repository health | Failed thresholds |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `packages/slopbrick/src` | complete | 328 | 328 | 0 | 0 | 25 | 99.9006 | 100 | 75 | `meanSlop` |
| repository root | complete | 388 | 388 | 0 | 0 | 25 | 99.7989 | 83.3333 | 75 | `meanSlop` |

These scans establish discovery and completion accounting, not rule
correctness or release quality. Findings still require the stratified manual
correctness/usefulness review specified by Gate 2, and the failing threshold is
intentionally recorded rather than hidden.

## Remaining gates

Gate 0 (scope/version decision), the score and broader CLI/MCP/core/engine
contracts, v10.3 calibration/provenance, full-suite environment repair, site
and operations hardening, and release-candidate/publication gates remain open.
The historical v10.2a artifacts and current model files remain exploratory and
must not drive signal changes or a release.

## Post-reconciliation verification (2026-07-09)

Follow-up commit `ea9c7c1d1` corrected the Dart signal metadata `aiSpecific`
drift; its review is APPROVE. The follow-up guardrail run covered four files
and 19 tests, all passing.

A full Vitest rerun was then started and stopped after exposing additional
non-green gates:

- structural-clone performance exceeded its 2,000 ms budget (2,748.9 ms)
- calibration-db corpus fixture scanned 0 files where more than 50 were expected
- category-separation exceeded the 60-second timeout
- parser-kotlin setup remained blocked by the local native-addon/install environment

Follow-up verification after the pinned dependency repair changed the status:

- structural-clone now completes within budget (1,971 ms in the combined run;
  533 ms isolated)
- category-separation passes with 1.93 points of best-bucket separation
- parser-kotlin passes after compiling the Node 24 native addon (5/5 tests)
- the DB calibration fixture still scans 0 files and remains an open corpus
  provenance/availability blocker
- stricter core schema validation required updating an invalid SlopBrick test
  fingerprint fixture; the structure test now passes (37/37)

The DB corpus blocker and remaining Gate 0 cleanup/calibration/release gates
still prevent a full-suite or release claim.

## Verification update — 2026-07-10

Using the repository-pinned `pnpm@9.15.0`, recursive typecheck passed for
core, engine, SlopBrick, and website. The SlopBrick build emits the v10.3 CJS
and ESM calibration control-plane entries. Its serial full suite passed:
247 test files and 2,418 tests; five fixture/integration files and nine tests
were intentionally skipped.

The default parallel recursive test invocation remains non-green under local
resource contention: worker-pool handshake timing assertions and the
structural-clone 2-second performance budget failed only in that concurrent
run, while both focused tests and the serial full suite passed. This is not a
release pass for the default parallel CI configuration; it is evidence that
the remaining issue is test-execution resource sensitivity rather than a
reproduced functional failure.

## v10.3 immutable release materialization — Task 1 (2026-07-11)

Release-materialization Task 1 is implemented in commit `ca72d0b15`
(`feat(core): model immutable calibration release assets`). This opens the
single private Core `0.3.0` calibration-schema tranche and adds the optional
`release_archive` manifest/check-out binding without changing
`STRUCTURE_SCHEMA_VERSION` (`5`) or the existing Git-tree fixture bytes.

TDD and review evidence:

- pre-change focused baseline: 21/21 passed;
- test-only RED: six expected missing-feature failures, followed by a separate
  RED that caught the absent 4,096-character runtime boundary;
- specification review found two identity-test gaps; mutation runs proved the
  corrected tests fail when repository ID or commit is omitted from checkout
  identity, when repositories are keyed by commit, or when duplicate
  repository-ID rejection is removed;
- code-quality review found three contract defects; a second test-only RED
  produced eight failures for non-string coercion, `.` root segments, and the
  inconsistent 512-character source-ID ceiling;
- final specification and code-quality rereviews both returned APPROVE with no
  remaining Critical, Important, or Minor findings.

Fresh pre-commit verification:

- exact Core codegen and generated-code freshness gate: pass;
- exact Core schema fixture validation: pass;
- focused Core contracts: 67/67 pass;
- full Core suite: 116/116 pass;
- Core typecheck and exact build (including prebuild/codegen/contracts): pass;
- SlopBrick typecheck: pass;
- tracked and staged diff checks: pass;
- existing v10.3.0 Git fixture SHA-256 remains
  `89d9eaf198702f2e28bc6270efedc0d1a08d73988ab4c61c79ff0704a7724225`;
- frozen admission/release plan hashes remain
  `233ede7262dd408318230f32ab4cc5d2103caf8b5662ae4fb228088531f70e75`
  and `0115a2c73dcfd0a086c70b1e3fe6519558bf7c34ff4942d97d52c3dfa92c585d`
  respectively.

The commit hook's staged self-scan completed 5/5 files with no parse failures:
AI Slop 7.2 (threshold 15 passed), engineering hygiene 100, security 100, and
repository health 97.1. It reported local `.slopbrick/` persistence, but the
post-commit status showed no tracked or untracked `.slopbrick/` change. This
small staged scan is diagnostic Task 1 evidence, not a whole-project self-scan,
corpus-quality, calibration, or release pass.

No external corpus byte, source register, admission decision, selection, run,
signal/verdict, remote, tag, publication, or deployment changed in Task 1.

## v10.3 immutable release materialization — Task 2 (2026-07-11)

Release-materialization Task 2 is implemented in commit `704abef0e`
(`feat(slopbrick): bind release assets into corpus selection`). File identity
now conditionally binds the normalized release-archive kind, byte digest,
root prefix, and extraction policy while preserving the exact pre-existing Git
payload. Selection records copy only the local checkout binding
`{kind, assetSha256, extractionPolicy}`; URLs, acquisition metadata, archive
size/format, checkout/cache paths, and host details are not serialized there.

TDD, review, and compatibility evidence:

- pre-change Git selection and run-manifest baselines passed;
- test-only RED produced four intended feature failures for missing release
  identity, selection binding, mixed-manifest propagation, and ledger drift;
- the frozen Git file ID remains
  `sbf_16358504671821d0e9643d831764052abd7cfadf3d1cc989074612cd94d64b58`;
- frozen Git-only JSONL, canonical-ledger, and pretty-ledger SHA-256 values
  remain `caefdfa7467d74e09193875d6f29ed213ac2958069a1f3dc22ada7c4b12ee52f`,
  `b01883c955f357fb04b6a87ed104bd4ed36c86d297d48a6d4d34a81451f97a18`,
  and `92c2407b9b2d61f844661b80b5e86c0765d95a4f78f53ea549daaf9edb18e430`;
- identity mutations cover digest, policy, root prefix, commit, repository,
  family, and normalized path; a URL-only change preserves `fileId` while
  changing the manifest hash;
- mixed Git/release order is deterministic, removed/changed selection bindings
  fail ledger verification, and a release binding under method `v10.3.0` is
  rejected;
- independent specification and code-quality reviewers both returned APPROVE
  with no findings;
- the exact focused boundary suite passed 16/16, including the spawned CLI
  refusal cases, and SlopBrick typecheck passed;
- staged diff checks passed.

The commit hook scanned the two changed production files with no active issues
or parse failures: AI Slop 0.0 (threshold 15 passed), engineering hygiene 100,
security 100, and repository health 100. This is a scoped staged diagnostic,
not a whole-project self-scan or corpus/calibration/release verdict.

No external corpus byte, source register, admission decision, real selection,
run, signal/verdict, remote, tag, publication, or deployment changed in Task 2.

## v10.3 immutable release materialization — Task 3 plan correction (2026-07-11)

Task 3 RED/design review found that the original release plan's atomic-rename
language conflicted with its no-overwrite invariant: POSIX rename may replace
an existing digest target. Current official Node, POSIX, IANA, and RFC research
also required distinct raw-header inspection, explicit abort/deadline behavior,
complete per-hop address/peer validation, and constant redacted errors.

The release plan was corrected from SHA-256
`0115a2c73dcfd0a086c70b1e3fe6519558bf7c34ff4942d97d52c3dfa92c585d`
to `7a20d89c35dcea0d9255e21d3e9cdadf89404c229a7c2dcb474b6bbacac7cb41`.
Task 3 now uses Node built-ins only, publishes by same-directory
`link`-if-absent plus winner rehash, preserves invalid existing entries,
examines uncollapsed headers, disables connection reuse, validates every DNS
answer and the connected peer on every hop, and specifies 30-second idle and
five-minute whole-operation aborts. Package and lockfile changes remain owned
by Task 4's exact ZIP dependencies.

The targeted independent rereview in
`.superpowers/sdd/v103-release-task3-plan-rereview.md` returned APPROVE/READY;
its zero-context diff confirmed that Tasks 1-2 and 4-8 were otherwise
byte-unchanged. The admission plan remains frozen at
`233ede7262dd408318230f32ab4cc5d2103caf8b5662ae4fb228088531f70e75`.

Follow-on OWASP/SSRF review found that arbitrary RFC 6052 network-specific
NAT64 prefixes cannot be distinguished safely from native global IPv6 without
trusted-prefix or network-sandbox authority. It also found that a leaf-only
cache check does not prevent a writable-parent swap and that silently omitting
POSIX no-follow primitives would overstate native Windows safety. The plan was
therefore tightened first to IPv4-only acquisition at candidate SHA
`2fea604dd10c3a1bb91cd68ebd9ca9fdbb1f9901e8b609e099f204bca25b099e`
and then to the final reviewed SHA
`3c115b75b63544c8fa281fb32532be25ed5d0a71b0afad7571163829668e93c5`.
The final contract adds a pre-existing canonical POSIX cache root with a fully
trusted ancestor chain, fail-closed unsupported-platform behavior, a 5-GiB
absolute ceiling, explicit certificate/TLS/header/parser controls, and honest
cooperative filesystem-deadline wording. Native IPv6 and reparse-safe Windows
support now require later explicit designs rather than silent fallbacks.

Independent targeted verdicts are persisted at
`.superpowers/sdd/v103-release-task3-ipv4-plan-rereview.md` and
`.superpowers/sdd/v103-release-task3-cache-plan-rereview.md`; both returned
APPROVE/READY and confirmed Task 3-only plan scope.

No production implementation, external corpus byte, source register,
admission decision, selection, run, signal/verdict, remote, tag, publication,
or deployment changed in this plan correction.

## v10.3 immutable release materialization — Task 3 implementation (2026-07-11)

Release-materialization Task 3 is implemented in commit `14998b539`
(`feat(slopbrick): acquire pinned calibration assets safely`) against final
release-plan SHA
`3c115b75b63544c8fa281fb32532be25ed5d0a71b0afad7571163829668e93c5`.
It adds a Node-built-in-only, default-denied HTTPS acquisition boundary and a
fake-transport adversarial suite; package and lockfile bytes are unchanged.

Final fail-closed behavior includes:

- exact lower-case SHA-256 and 1-byte-through-5-GiB size authority;
- a pre-existing canonical POSIX cache root with a `0700`-style leaf, fully
  trusted ancestor chain, no-follow/nonblocking opens, and BigInt pathname/
  handle device-inode checks before and after hashing;
- IPv4-only all-answer DNS validation, IANA non-public exclusions, exact-host
  allow-listing, connected-peer binding, manual five-hop redirects, and native
  IPv6/unsupported-platform denial;
- explicit certificate verification, TLS 1.2 minimum, strict HTTP parser,
  16-KiB headers, identity encoding, raw duplicate-header validation, and no
  pooled connection;
- exact streaming byte/digest enforcement, random mode-`0600` `wx` temporary
  files, sync/close, hard-link-if-absent publication, winner/publication rehash,
  and invocation-owned cleanup; and
- 30-second idle plus five-minute cooperative end-to-end deadlines whose code
  remains authoritative across success and rejection paths. OS filesystem
  calls that never return and same-effective-UID/root attackers remain explicit
  operating-system boundaries rather than hidden guarantees.

TDD and review evidence:

- corrected initial RED: 137 tests, 136 intended functional failures and one
  constant-message non-disclosure baseline pass;
- each review-discovered defect received a focused RED before its fix, including
  overwrite races, duplicate framing, NAT64/IPv6 ambiguity, TLS/parser limits,
  cache ancestry/platform primitives, pathname inode swaps, deadline-phase
  cleanup, and rejected-operation deadline precedence;
- final focused suite: 184/184 pass;
- combined Task 2-3 identity/selection/run/acquisition boundary: 200/200 pass
  with no leaked-FileHandle warning after correcting an artificial test mock;
- SlopBrick typecheck and staged diff checks: pass;
- independent specification review: APPROVE;
- independent OWASP A01-A10 review: APPROVE with zero remaining findings; and
- final production/test SHA-256 values:
  `9c2678505e9adc6ca47781521e11103c412f70215c8b4014c1aa3564d6d30854`
  and `c93a076be69053891793e902660c7abbcd8d2228eaef680a3c67d23ae56f4004`.

The first commit attempt was correctly blocked by the staged AI Slop threshold
(18.7 > 15). Review found one real documentation-density problem and one
`dead/unused-import` false positive on a used type-only import. Behavior-neutral
remediation added security-boundary rationale and an equivalent local type
shape. A manual staged rerun then passed at 7.2 with one low compression-profile
issue; the unchanged staged bytes scored 0.0 with no active issue in the commit
hook. That unexplained state-sensitive score drift, irrelevant visual-axis
wording, suppressed-count mixing, repeated baseline-migration warning, and
`.slopbrick/` write side effect
are now explicit open Gate 2 self-scan UX/correctness requirements. The final
hook result (AI Slop 0.0, engineering 100, security 100, repository health 100,
zero parse errors) is only a scoped commit diagnostic, not whole-repository,
corpus, calibration, or release evidence.

No external corpus byte, source register, admission decision, real selection,
run, signal/verdict, remote, tag, publication, or deployment changed in Task 3.

## v10.3 immutable release materialization — Task 4 plan correction (2026-07-11)

The Task 4 implementation plan is now independently approved at release-plan
SHA-256
`08d30d9dca5b177764c5c631d98d570d75b2702a556438eaf8c10dadf292c26f`.
The targeted verdict and primary-source evidence are persisted at
`.superpowers/sdd/v103-release-task4-plan-rereview.md`. This supersedes the
Task 4 portion of the prior `3c115b75...` plan without changing the historical
Task 3 implementation basis recorded above. The admission plan remains
byte-unchanged at
`233ede7262dd408318230f32ab4cc5d2103caf8b5662ae4fb228088531f70e75`.

The correction was necessary because dependency/API inspection and adversarial
review found real design defects rather than implementation failures: whole-
directory rename could replace a winner, library-decoded names and central-only
metadata were not a complete raw ZIP authority, descriptor ownership and final
archive verification contradicted each other, receipt/resource bounds were not
fully computable, and cleanup/publication durability was underspecified.

The approved plan now freezes:

- an ASCII-only `safe-zip-v1` raw EOCD/ZIP64-v1/local/central grammar, methods
  0/8, signed descriptors, exact extra fields, CRC/size/input-consumption
  checks, and maximum-plus-one resource tests;
- a borrowed positional yauzl reader over one verified owning archive
  `FileHandle`; the installed low-level Promise helper is explicitly forbidden
  after a live probe showed it passes numeric arguments where an `Entry` is
  expected;
- early per-file and aggregate output budgets, deterministic exact receipt and
  reference bytes, private generation trees, bottom-up file/directory sync,
  hard-link-if-absent stable reference publication, complete `EEXIST` winner
  reuse verification, and identity-safe invocation-owned cleanup;
- one archive descriptor retained through extraction and publication/reuse,
  rehashed and restated immediately before success, closed exactly once, and
  followed by a post-close pathname identity check; and
- three implementation/review boundaries: Task 4A shared trusted-POSIX-cache
  refactor, Task 4B raw ZIP/CRC/receipt/reference foundation, and Task 4C
  extraction/publication/reuse.

Dependency and environment evidence before implementation:

- exact graph: runtime `yauzl@3.4.0 -> pend@1.2.0`; development
  `yazl@3.3.1 -> buffer-crc32@1.0.0`, `@types/yauzl@3.4.0`, and
  `@types/yazl@3.3.1`;
- frozen install and SlopBrick typecheck pass after relinking the workspace to
  the pnpm 9.15.0-compatible store;
- `yauzl@3.4.0` is outside CVE-2026-31988's affected `3.2.0` range;
- production audit remains red with 17 pre-existing website-path advisories
  (3 low, 10 moderate, 4 high), and the complete graph remains red with 20
  pre-existing advisories (4 low, 11 moderate, 4 high, 1 critical); no finding
  traverses the exact ZIP delta; and
- Node 22 and 24 directory-`FileHandle.sync()` probes pass. Node 20 is already
  end-of-life; Task 4 must still test the package's current Node-20-compatible
  promise, while the release program separately moves support and packed proof
  to Node 22/24.

The intended EvalPlus ZIPs were inspected read-only and fit the frozen profile:
both contain 330 ZIP entries, use host system 3, methods 0/8, zero flags,
printable ASCII names, no comments, contiguous non-overlapping ranges, and only
the accepted `0x5455`/`0x7875` extra fields. Their identities remain exactly
the hashes and sizes recorded in the Task 4 rereview. This is compatibility
evidence, not license, provenance, label, review, admission, or promotion.

Two independent reviewers approved the exact final plan hash: a full
specification pass and a filesystem/archive-security pass. `git diff --check`
and markdown fence parity pass. Task 4 production code is not yet implemented;
the next execution slice is Task 4A.

No external corpus byte, source register, admission decision, selection, run,
signal/verdict, remote, tag, publication, or deployment changed in this plan
correction.

## v10.3 immutable release materialization — Task 4A implementation (2026-07-11)

Release-materialization Task 4A is implemented in commit `521c0e888`
(`refactor(slopbrick): share trusted POSIX cache guards`) against release-plan
SHA
`08d30d9dca5b177764c5c631d98d570d75b2702a556438eaf8c10dadf292c26f`.
The exact review is persisted at
`.superpowers/sdd/v103-release-task4a-review.md`.

The slice adds one shared module for the already-proven POSIX capability,
canonical ancestor, BigInt identity, and bounded positional descriptor-hash
primitives. `artifact-download.ts` delegates to those primitives while keeping
its exported Artifact dependency types, stable error domain, relative cache
input behavior, missing/invalid distinction, and network/publication behavior.
The shared hash does not own or close a borrowed descriptor; the downloader's
path-verification wrapper still closes exactly the handle it opens. A deadline
observed immediately after open now closes that acquired handle before the
authoritative deadline escapes.

TDD and review evidence:

- pre-refactor acquisition baseline: 184/184 pass;
- RED: the new direct suite failed before collection because the shared module
  did not exist;
- GREEN: 43/43 direct shared-boundary tests and the byte-unchanged 184/184
  acquisition tests, 227/227 combined;
- SlopBrick typecheck, build, and diff checks pass; the build retains non-fatal
  pre-existing Zod/Rollup declaration warnings and is not warning-clean;
- independent specification review: APPROVE;
- independent code/filesystem-security review: APPROVE; and
- final source hashes: artifact adapter
  `5d7fe26c5671c6a679d3702eea8a10e35590d7f5a5d99a524d8fc9d507bff693`,
  shared module
  `8ede7164fcfbf535b53924f788f5fd159e16c1e871c388e0581ae36b7e2c1f21`,
  unchanged acquisition tests
  `c93a076be69053891793e902660c7abbcd8d2228eaef680a3c67d23ae56f4004`,
  and direct tests
  `25d67ee157b2bc86772a0124d9acc788ba56a78c3bb81adb5897ce3c6200fbbd`.

The staged hook selected only the two production files and passed at AI Slop
7.2/100, engineering 100, security 100, and repository health 97.1 with two
low compression-profile findings and zero parse errors. It also repeated the
known baseline-migration warning, wrote ignored `.slopbrick/` state, and scored
a visual axis for non-visual code; this scoped hook pass does not close the
self-scan correctness/UX gate.

No dependency or lockfile byte was committed in Task 4A. No external corpus
byte, source register, label, admission decision, selection, run, signal/
verdict, remote, tag, publication, or deployment changed. The next execution
slice is Task 4B.

## Git-scoped self-scan determinism correction (2026-07-11)

Task 4B's first checkpoint attempt proved the open staged-score defect with an
exact causal sequence. The same two path hashes scored 13.061 at 21:45:14 and
17.773 at 21:45:27. The first run appended itself to project history, crossed
the three-consecutive-scan flywheel threshold, and promoted
`ai/compression-profile`; the commit hook then loaded that new state. Both runs
used SlopBrick 0.44.0, so binary selection did not cause this incident.

Commit `efb069b90` (`fix(slopbrick): make staged scans deterministic`) now:

- makes non-empty `--staged` and `--changed` scans independent of local
  flywheel tuning and read-only for project memory, telemetry, baseline,
  incremental/AST cache, and managed-snippet state;
- restores parser-cache environment state after every invocation;
- resets sequential cross-file duplicate state and serializes concurrent
  `runScan()` calls while those rules remain process-scoped; and
- defensively clones mutable rule configuration before ordinary flywheel
  overrides are applied.

Verification was mutation-driven: the initial non-empty regression failed on
score drift and `.slopbrick` writes; the first correction exposed a second
in-process `dup/near-duplicate` leak. The final fixture is larger than 2 KiB,
reliably fires `ai/compression-profile`, proves seeded `high` flywheel state
does not change its `low` staged severity, checks every implicit persistence
flag in the built CLI, and covers concurrent cross-project calls. Six focused
suites pass 240/240, package typecheck/build pass, and independent rereview is
APPROVE. Two separate packaged runs analyzed 7/7 and emitted identical score
and finding projections at 9.250854; the aggregate `.slopbrick` hash remained
`4b52530a94352cfccb233ab5f1808b3042fac49305418b4cf40f93866f0bb222`.

This correction closes staged score drift and implicit state writes only. The
hook is still installed as unpinned `npx slopbrick --staged`; repeated
baseline-migration warnings, type-only-use false positives, irrelevant visual
axes, and active/suppressed presentation remain open Gate 2 requirements.

## v10.3 immutable release materialization — Task 4B implementation (2026-07-11)

Task 4B is implemented at commit `6ce6259da` against frozen release-plan SHA
`08d30d9dca5b177764c5c631d98d570d75b2702a556438eaf8c10dadf292c26f`.
The exact bounded review is
`.superpowers/sdd/v103-release-task4b-review.md`.

The slice pins the approved yauzl/yazl dependency graph and adds the raw
`safe-zip-v1` authority, bounded owned stored/deflate streams, CRC32/SHA-256
verification, and canonical materialization inventory/receipt/reference
codecs. The raw parser and yauzl metadata must agree by ordinal; ASCII path,
host/type/flag/method/extra, range, descriptor, ratio, and aggregate budgets
fail closed. Receipt inputs are snapshotted before retention so hostile getters,
proxies, sparse collections, and revoked typed arrays cannot escape the stable
error domain or mutate canonical output.

Verification and review evidence:

- RED covered missing modules, then adversarial review added reserved
  descendants, sparse inputs, pre-retention budgets, exhaustive raw/yauzl
  mutations, exact low-memory inventory ceilings, short-output deflate,
  initialization-ref cleanup, and hostile accessor/proxy cases;
- safe ZIP 30/30, receipt/reference 44/44, trusted POSIX cache 43/43, and
  acquisition 184/184: **301/301 pass**;
- direct strict compilation of the three new test/helper files, package
  typecheck, frozen install, build, and diff checks pass;
- independent receipt, full-specification, and archive/filesystem-security
  reviews all conclude APPROVE with no remaining finding; and
- exact source/test/dependency hashes are frozen in the Task 4B review.

The final staged scan analyzed 2/2 with no parse failures and passed at AI Slop
13.061, engineering 100, security 100, and repository health 94.8. Its repeat
and the commit hook produced the same score, and every existing `.slopbrick`
byte remained unchanged. The warning/axis UX defects above remain open and
this scoped result is not whole-project release evidence.

The exact ZIP dependency delta adds no known advisory. The production audit
still has 17 pre-existing findings and the complete graph 20; independent
remediation remains mandatory before packed Task 6 evidence.

No external corpus byte, source register, label, admission decision,
selection, run, signal/verdict, remote, tag, publication, or deployment
changed. At that checkpoint, Task 4C was next; its completion is recorded below.

## v10.3 immutable release materialization — Task 4C implementation (2026-07-12)

Task 4C is implemented at commit `04697806e`
(`feat(slopbrick): publish verified calibration archive trees`) against the
frozen Task 4 plan.

The slice adds sequential bounded extraction, private verified trees, durable
no-replace hard-link publication, complete reuse verification, and identity-safe
postorder cleanup.

Independent specification, code-quality, and archive-security rereviews all
returned APPROVE. Their final passes followed fixes for recycled-FD close retry,
ambiguous hard-link cleanup, and `fs.Dir` lifecycle recovery.

Verification evidence:

- Node 20.19.6: 425/425 focused Task 4 tests passed;
- Node 22.22.3: 425/425 focused Task 4 tests passed;
- Node 24.15.0: 425/425 focused Task 4 tests passed;
- SlopBrick: 2,956 passed and 9 skipped;
- repository suites: core 116, website 37, engine 59, and SlopBrick 2,956 all
  passed; and
- typecheck, build, and frozen install completed successfully.

The full-suite test repairs are commit `93cbc36eb`
(`test(slopbrick): stabilize time and staged-watch contracts`). Generated
website package metadata was refreshed at `81e79b9ec`.

Audits remain intentionally red: production reports 17 advisories and the full
graph reports 20. No advisory path includes `yauzl`, `yazl`, `pend`, or
`buffer-crc32`.

The partial staged precommit self-scan scored 7.2/100 and reported one low
compression-profile finding. At this Task 4C checkpoint it was scoped
diagnostic evidence; the current package/root self-scan results are recorded
in the Gate 2 checkpoint below.

No push, tag, GitHub release, package publication, or deployment occurred. The
corpus remains provenance-gated and cannot support admission or calibration
claims. Release-materialization Task 6 is now bounded-contract approved; its
clean commit-bound prerequisite remains open.

## Historical Gate 2 verification checkpoint — 2026-07-12 (superseded)

The recovery branch now includes focused correctness slices for parser-backed
comment-line metrics, AST-proven wildcard CORS facts, project-level
identical-block coordination with incremental parity, effective issue-set
diagnostics, Unicode-safe source positions, lexical security boundaries, and
nonblocking large JSON/beacon completion. The focused integrated slice passed
114/114 selected tests; the
post-type narrowing CORS/clone/default-off slice passed 34/34. SlopBrick and
website typechecks passed, and the website prebuild now fails closed when the
executable registry, generated rule catalog, and signal table drift.

Final bounded package evidence after the latest fixes:

- `@usebrick/core`: 116/116 tests;
- `@usebrick/engine`: 59/59 tests;
- `@usebrick/website`: 37/37 unit tests and Astro typecheck with three
  pre-existing hints;
- `slopbrick`: 263 test files passed, 5 skipped; 3,145 tests passed, 9
  skipped; and
- workspace build: core codegen/build, website Astro build, engine build, and
  SlopBrick bundle all completed. The bundle retains non-fatal Zod/Rollup
  declaration warnings; it is not warning-clean.

The corrected package-relative v0.44.0 self-scan exited 0 and produced a
443,030-byte JSON report: 204/204 selected/analyzed, 0 failed/skipped, complete
and valid score status, AI Slop 4.5955, Engineering Hygiene 100, Security 100,
Repository Health 98.1468, 114 active findings (113 low compression-profile
and one isolated heaps-deviation), and 266 suppressed
findings from 36 default-off rules. The broader platform-root scan likewise
completed 435/435 with no failures/skips after the security lexical-boundary
fix (AI Slop 4.2833, Hygiene 99.8, Security 83.3333, Health 94.8930, 208
active, 545 suppressed). The pre-fix root snapshot was 434/434 with Security
18.5185 because rule comments and advice strings triggered
`security/sql-construction` and `security/fail-open-auth`; the focused fix
removed those findings. These are diagnostic snapshots, not release evidence:
the package/root results remain dominated by one low compression heuristic and
mixed self-hosting fixtures.
The detailed correctness/usefulness/UX review is
`.superpowers/sdd/self-scan-review-2026-07-12.md`.

## Immutable release-asset Task 6 — resolver, run binding, and packed consumer — 2026-07-12

Task 6 is bounded-contract approved after an independent implementation and
fix-wave review. The resolver now matches `(repositoryId, commitSha,
materialization binding)`, rejects duplicate/malformed candidates, realpaths
and contains roots/files, rejects symlinks and non-regular files, and hashes
the bytes before scanning. The complete checkout-map hash continues to bind
release materialization into the frozen run without adding a second run field.

The calibration CLI hashes legacy manifest bytes before Core validation or
output mutation, accepts only flat methods `v10.3.0`/`v10.3.1`, and routes a
complete admission-shaped source to the explicit Task 9B reservation. Packed
consumer tests install the actual tarball into an empty project with an
isolated hoisted pnpm store, no workspace links or `NODE_PATH`, positive
generated-Core declaration assertions, valid/invalid packed manifest checks,
and offline materialize/select/verify.

Evidence:

- focused resolver/CLI/run matrix: 28 tests passed;
- packed consumer: 4 tests passed under Node 24.15.0 and 4 under Node
  22.22.3;
- historical durable receipts (superseded by the corrected same-tarball pair
  recorded below):
  `.superpowers/sdd/task-6-receipts/task-6-packed-consumer-node-v22-22-3.json`
  and `task-6-packed-consumer-node-v24-15-0.json`; the current dirty files
  are not mutually tarball-identical (Node 22
  `9b3bce9d721d5c7a14a52dee64cc4ec507718619cdeeb6a73905c78887764070`, Node
  24 `dfc6c36211d8cd94d55641163de3ccd891dd33820d84348c7319bfe5422ac053`),
  so the earlier shared-hash statement is stale and both receipts require a
  same-tarball clean-boundary rerun;
- recursive serial gates: Core 116/116, website 37/37, engine 59/59, and
  SlopBrick 3,145 passed/9 skipped across 263 passed/5 skipped files;
- recursive lint, typecheck, test, build, and `git diff --check` exited 0.

The receipts identify builder commit `81e79b9ec47f46f70575ae650beccd3a278b60e5`
with `dirty: true`. No commit was created, so the clean builder-commit and
commit-bound receipt rerun required to close the release prerequisite remains
open. No corpus admission, tag, push, publication, deployment, or remote
state changed.

The current workspace registry is 119 rules/27 categories in an unreleased
candidate; npm v0.43.0 remains the latest verified public release at 103/22.
Historical v10.1 metadata covers 103 measured rules and keeps 576,750 analyzed
files distinct from 581,550 sampled paths. No signal-strength promotion,
corpus admission, tag, push, publication, deployment, or Task 5 calibration
run has occurred.

## Runtime/release-workflow Task 5 readiness — 2026-07-12

The separate runtime/release-truth slice is implemented without changing
remote, tag, registry, or environment state:

- the root and all four workspace packages declare Node.js `^22.0.0 || ^24.0.0`;
  `slopbrick doctor`, runtime-policy tests, README/package docs, and the AUR
  recipe use the same policy;
- CI exercises Node 22 and 24 plus a packed-consumer matrix on both lines;
- `slopbrick-review.yml` accepts explicit manual-dispatch inputs, captures
  exit 1 (threshold) separately from exit 2+ (failure), writes bounded
  multiline output, and does not rely on PR-only context; and
- `publish.yml` resolves an exact tag/version, runs quality gates, packs one
  tarball, records its SHA-256/byte receipt, uploads it before the `publish`
  environment gate, re-verifies the downloaded artifact, and publishes only
  that tarball. Registry errors fail closed and already-published versions are
  skipped idempotently.

Focused runtime and packed-consumer tests pass 7/7; SlopBrick typecheck passes;
`git diff --check` is clean; and Ruby YAML parsing passes for all three
workflows. This is workflow readiness evidence, not authorization to publish.

## Immutable release-asset Task 5 — 2026-07-12

The v10.3 `cal:materialize` slice is complete and independently approved. It
validates the Core corpus manifest before mutation, acquires/extracts each
distinct release archive once, verifies `rootPrefix` containment and every
declared file hash, merges only validated Git base-map entries, and writes a
local-only checkout map with exclusive atomic publication. Errors are stable
and path-free, including the final verification race boundary.

Final focused evidence is 16/16 tests, package typecheck, and package build
green. The build retains the known non-fatal Zod declaration warnings. The
implementation report and independent review are
`.superpowers/sdd/task-5-report.md` and `.superpowers/sdd/task-5-review.md`.
No corpus file was admitted, and no network, tag, registry, publication, or
deployment state changed. The remaining release-materialization gate is the
clean builder commit and commit-bound receipt rerun for Task 6.

## Historical dependency and corpus audit — 2026-07-12 (superseded)

The bounded read-only audit confirms that the release lane remains open for
two independent reasons. `corepack pnpm audit --prod --audit-level moderate
--json` reports 17 unique advisories (3 low, 10 moderate, 4 high, 0 critical);
the full graph reports 20 (4 low, 11 moderate, 4 high, 1 critical). All
production paths root in the website Astro/Vite/esbuild graph; no advisory path
traverses SlopBrick's `yauzl`, `yazl`, `pend`, or `buffer-crc32` ZIP edges.
Remediation is a separate dependency/lockfile scope and has not been applied.
Evidence: `.superpowers/sdd/dependency-gate-audit.md`.

The centralized corpus review validator passes:
`node /Users/cheng/corpus-expansion/v10.3/review/validate-review-artifacts.mjs`
returns `v10.3 corpus review artifacts: valid`. This validates review artifacts,
not eligibility. The register is still `v10.3-draft` with 12 sources and 317
pinned legacy repositories; the 452,382 inventory rows remain declared intake
records, not eligible files. HumanEval contributes zero eligible pairs and
EvalPlus contributes zero eligible pairs; no `manifests/`, `selection/`, or
`runs/` artifact exists. The corpus audit therefore keeps AI-positive and
human-negative promotion fail-closed until admission Tasks 0-8 are completed.

The manual self-scan UX review is recorded in
`.superpowers/sdd/self-scan-ux-audit.md`. Both final JSON artifacts are
complete and internally accounted (204/204 and 435/435; no failures, timeouts,
parse errors, or failed thresholds), but `ai/compression-profile` supplies
113/114 and 186/208 effective findings respectively. The review also found
self-referential SQL examples affecting the platform security view, a legacy
`totalScore: 0` field beside populated score fields, and unclear default-off
versus suppressed-count wording. These remain manual UX/context gates, not
evidence to promote a signal or release.

The portability audit is recorded in `.superpowers/sdd/path-portability-audit.md`.
The active v10.3 control-plane entrypoint has no machine-specific corpus
default, but historical executable calibration scripts still contain local
clone/binary fallbacks. Those scripts remain non-portable research tooling and
must not be used as Gate 3 evidence until isolated argument/environment fixes
and tests land; provenance metadata and historical documentation are retained
as labeled local evidence rather than rewritten.

## Self-scan renderer context fix — 2026-07-12

The manual UX review exposed a real report defect: Markdown grouped every
`USEFUL`/`OK` verdict under `AI Findings`, even when the issue itself declared
`aiSpecific: false` (for example `perf/css-bloat` and security findings). The
renderer now keeps `NOISY`/`DORMANT` verdicts suppressed and uses the issue's
explicit polarity to place non-AI findings in `Engineering Hygiene`; unknown
polarity retains the signal metadata fallback. The focused Markdown/HTML/
bucket renderer suites pass 31/31 tests and SlopBrick typecheck plus build
pass. This changes presentation only; it
does not alter scoring, calibration, or signal strength.

## Dependency-remediation verification — 2026-07-12

The pre-remediation advisory counts recorded above are historical. The
bounded, intentionally uncommitted dependency tranche now pins workspace
Vitest/coverage to `3.2.7`, the Vitest test graph to Vite `6.4.3`, YAML to
`2.8.3`, the tsup edge to esbuild `0.28.1`, and the private website to Astro
`7.0.7`. The root/core/engine/SlopBrick engine contract remains exactly
`^22.0.0 || ^24.0.0`; the private website advertises `^22.12.0 || ^24.0.0`
because Astro/Vite require Node 22.12.
Astro 7's rejection of literal braces in the StructureDemo code sample was
fixed with HTML entities; its static build now passes.

Fresh audit commands both return zero advisories:

```text
corepack pnpm audit --prod --audit-level moderate --json  -> exit 0, 0 advisories
corepack pnpm audit --audit-level moderate --json         -> exit 0, 0 advisories
```

Post-migration verification is green: website typecheck (0 errors, 3 hints),
website unit tests (6 files/37 tests), website build, recursive typecheck,
recursive tests (Core 116, website 37, engine 59, SlopBrick 3,151 with 9
skipped), and recursive build. The build retains known non-fatal Zod DTS
warnings. Vitest 3/Vite 6's temp-file SSR boundary is handled in the engine
and SlopBrick test configs by allowing only the real OS temp directory used by
contract fixtures; the affected focused suites pass 115/115.

This clears dependency and recursive verification on the current dirty bytes.
The manifests/lockfile and test-harness config still require independent
review, a clean commit, and commit-bound Task 6 receipt reruns. No corpus
admission, signal promotion, tag, push, publication, or deployment occurred.

## Post-UX and resource-budget verification — 2026-07-12

The bounded serial SlopBrick rerun after the default-off renderer fix passed:
265 test files, 5 skipped; 3,157 tests, 9 skipped. SlopBrick typecheck, build,
the focused packed-consumer suite (5/5), focused report/UX suites (146/146),
and `git diff --check` also pass. The earlier default-parallel run is retained
as a failure record: `tests/cli/large-json-output.test.ts` exceeded its 30s
test timeout under four concurrent Vitest workers, while the same test passes
alone in 16.5s and in the serial full run in 8.5s. This is resource contention,
not a JSON truncation defect.

The test configuration now accepts `SLOPBRICK_VITEST_WORKERS` with a bounded
fallback, and CI/publish/pre-push workspace gates serialize package execution;
the configuration contract tests pass. This keeps subprocess-heavy scans
within the RAM budget without disabling parallel subagents.

The renderer correction is now explicit: Markdown/HTML remove `severity: off`
instances from actionable AI/Hygiene buckets and report them as audit-only
(`271` instances across `7` rules in the current package artifact). The pretty
CLI detail feed is capped per repeated rule by default and `--full` restores
the complete instance feed. The root self-scan and manual stratified review
remain open.

## Historical pre-commit hook offline-boundary verification — 2026-07-12 (superseded)

The installed hook sentinel now calls the project-local
`./node_modules/.bin/slopbrick --staged` directly. This prevents a global npm
installation from silently substituting another version and replaces the
legacy network-permitting `npx slopbrick --staged` sentinel while preserving
surrounding hook content. The focused installer suite passes 7/7 with one
Vitest worker. Its contract tests cover fresh installation, idempotence,
legacy upgrade, uninstall, empty repositories, malformed sentinels, and
execution through a stub local binary that records the exact argument and
returns a non-zero status; the observed invocation is:

```text
--staged
```

This is only a prerequisite for deterministic hook behavior. A real installed
packed-binary commit on Node 22 and Node 24, missing-binary behavior, and
package-manager/shell-quoting cases remain open before the Gate 2 prevention
loop can be marked complete.

## Score denominator and failure-outcome verification — 2026-07-12

The score denominator now has one explicit predicate: a file contributes only
when it has neither a parse error nor any `failureKind` (`parse`, `timeout`,
`crash`, or `internal`). The same predicate drives scorable results, duplicate
source eligibility, verbose/accounting analyzed and zero-finding counts,
`scoreBasis`, enrichment denominators, and test-quality/maintenance/business-
logic aggregates. This prevents a timeout/crash/internal failure from being
counted as successfully analyzed merely because it lacks a legacy parse-error
string.

The implementation is `src/cli/scan-accounting.ts`, with the regression
contract in `tests/cli/scan-accounting.test.ts`. The red import-first test was
followed by green focused evidence: `scan-accounting`, `metrics`, and
`scan-completion` passed 148/148 under one worker; SlopBrick typecheck passed.
The complete recursive gate still must be rerun after this slice.

## Pre-SCORE-02 stable same-tarball Task 6 paired receipts — 2026-07-12

After the pure-engine boundary correction and latest build, the packed
consumer used one tarball:
`/tmp/slopbrick-task6-pair-corrected/slopbrick-0.44.0.tgz`, SHA-256
`6cf460a894d2d111529e5ffe0b0b983a56002baac3eb9eb90c4a19425e8701e8`.
The external builder was commit
`81e79b9ec47f46f70575ae650beccd3a278b60e5`, intentionally dirty, with status
SHA `64b186f0901e83095d3458fade6b4c049667304c8f484235f111cd8a4a7a9b95`.

- Node 22.22.3: 9/9 passed in 10.42 seconds;
- Node 24.15.0: 9/9 passed in 11.90 seconds, including the pair assertion;
- strict offline installation succeeded on both runtimes;
- ESM/CJS imports, materialize/select/verify, valid/invalid corpus behavior,
  builder identity, and post-pack identity checks all passed; and
- both receipts agree on tarball, commit, dirty state, and status hash.

Receipt files are
`/tmp/slopbrick-task6-pair-corrected/receipts/task-6-packed-consumer-node-v22-22-3.json`
and
`/tmp/slopbrick-task6-pair-corrected/receipts/task-6-packed-consumer-node-v24-15-0.json`.
This closes the stable dirty same-tarball contract for that snapshot only.
SCORE-02 and later Task 0/renderer edits changed the worktree status hash, so
the pair must be rerun against the final current build. A user-authorized
clean builder commit and commit-bound rerun remain required for release.

## Historical Task 6 paired receipts — 2026-07-12 (superseded)

After SCORE-02, the project-context renderer fix, Task 0 portability patch,
and the final SlopBrick build, the current-source packed consumer used:
`/tmp/slopbrick-task6-pair-task0-portability/slopbrick-0.44.0.tgz`, SHA-256
`7106c508ba118c6202beb9c8fd69f31361e771966963b835a4a13fff2a9a2c4d`.
The builder is commit
`81e79b9ec47f46f70575ae650beccd3a278b60e5`, intentionally dirty. The exact
status capture uses `git status --porcelain=v1 --untracked-files=all` and has
SHA `4858dfa27ba0045af019b6d48c83eb24d56c96555443b45bd8f4ce68c8fdc54b` with
173 entries.

- Node 22.22.3: 9/9 passed in 6.35 seconds;
- Node 24.15.0: 9/9 passed in 4.96 seconds, including pair assertion;
- strict offline install succeeded on both runtimes;
- ESM/CJS exports, valid/invalid corpus validation, materialize/select/verify,
  builder identity, and post-pack identity all passed; and
- both receipts agree on tarball, commit, dirty state, status hash, and
  unchanged builder-after-pack identity.

Receipt files:
`/tmp/slopbrick-task6-pair-task0-portability/receipts/task-6-packed-consumer-node-v22-22-3.json`
and
`/tmp/slopbrick-task6-pair-task0-portability/receipts/task-6-packed-consumer-node-v24-15-0.json`.
The clean commit-bound release prerequisite remains open.

## Historical bounded verification and self-scan refresh — 2026-07-12 (superseded)

After the pure-engine boundary correction, test-budget contract fix, explicit
large-JSON timeout budget, SCORE-02 project-finding slice, Task 0 bootstrap,
and project-context renderer slice, the final serial SlopBrick suite passed:
268 files passed, 5 skipped; 3,182 tests passed, 9 skipped. The command used
one Vitest worker and `NODE_OPTIONS=--max-old-space-size=2048`. The two
previous failures are now covered: the worker-budget test honors the explicit
`SLOPBRICK_VITEST_WORKERS=1` override, and the 900-file large JSON contract has
a 60-second test budget because the full subprocess suite takes about 35–43
seconds on this host. SlopBrick typecheck and `git diff --check` pass.

Fresh built-CLI self-scans were captured at:

- `/tmp/slopbrick-self-scan-final2.json`: 208/208 complete and valid, 117
  active, 272 suppressed, AI Slop 4.6208, hygiene 99.9501, security 100,
  Repository Health 98.1367, with a 208-file effective score basis;
- `/tmp/platform-root-self-scan-final2.json`: 439/439 complete and valid,
  211 active, 550 suppressed, AI Slop 4.2988, hygiene 99.7989, security
  83.3333, Repository Health 94.8868, with a 439-file effective score basis.

Both reports have zero failures/skips/parse errors and no failed thresholds.
Markdown re-rendering places statistical compression findings in the AI lane,
engineering findings in the hygiene lane, and default-off instances in an
explicit audit-only summary. The stratified usefulness review remains open
because compression-profile dominates both active feeds and rule/example code
affects the root security view.

## v10.3 corpus admission readiness audit — 2026-07-12

The read-only corpus audit confirms that the current blocker is admission
implementation and provenance, not raw volume. The review validator returns
`v10.3 corpus review artifacts: valid`; EvalPlus register consistency passes
4/4. The source register is `v10.3-draft` with 12 sources, and the repository
inventory has 317 pinned local repositories (225 declared AI and 92 declared
human), all with matching recorded HEADs; 17 lack a root license and remain
ineligible. The initial composed target is 329 entries (12 register sources
plus 317 repository leaves).

Admission has not started: `review/admission/` is absent and `manifests/`,
`selection/`, and `runs/` are empty. Legacy inventories contain 224,903 AI and
227,479 human rows; post-exclusion declared pools are 196,733 AI and 167,028
human. Exact cross-polarity overlap is 3,522 hash clusters / 14,804 records,
with two origin-family overlaps. Controlled HumanEval has 100 AI + 100 human
paired records but 8 unsafe pairs and zero eligible under the immutable bundle
contract. EvalPlus has 164 + 164 paired records, 163/164 StarCoder files
parseable, 11 unsafe pairs, and zero eligible.

No additional public repositories should be pulled yet. The admission plan
requires a deterministic census and independent review before acquisition;
pulling more sources now would bypass the provenance gate.

## Workspace gate note — 2026-07-12

Workspace-wide typecheck passes for all four packages (website reports three
non-error hints). The serialized recursive build reaches Core codegen, then
stops at Core's intentional freshness guard because the generated
`packages/core/src/generated/health.ts` reflects the uncommitted health-schema
description update. Package/engine/website build artifacts were independently
built successfully, with the existing non-fatal Zod/Rollup declaration
warnings. The Core freshness failure is a clean-commit prerequisite, not a
source compilation failure; no commit was created.

## SCORE-02 project-finding effective-set slice — 2026-07-12

The scan pipeline now computes `runProjectRules()` before headline aggregation
and appends its active, effective findings to the same issue groups consumed by
`aggregateReport()`. Enrichment likewise adds active non-file findings to its
secondary effective inputs using object identity to avoid duplicating the
per-file findings already present in `allIssues`. Default-off and explicit
`severity: off` findings continue through `effectiveIssuesForScore()` and stay
audit-only.

The TDD regression fixture uses three clean TSX files sharing `gap-4`: the
project-level `layout/gap-monopoly` finding remains visible and now produces a
non-zero AI Slop Score. A second contract injects an active project-level test
finding and verifies `testQuality` decreases. Bounded evidence:

```text
scan-completion headline contract: 1/1 passed (1 worker)
scan-completion secondary-input contract: 1/1 passed (1 worker)
scan-completion effective-set companion slice: 6/6 passed, 100 skipped
rules/project.test.ts: 19/19 passed
slopbrick typecheck: passed
slopbrick build: passed (existing non-fatal Zod DTS warnings)
```

This closes the project-rule omission in SCORE-02. The deterministic
whole-project renderer fixture is recorded below; an explicit policy/test for
whether findings attached to incremental-cached files may contribute to
incomplete numeric diagnostics remains open. No release or calibration
admission follows from this slice alone.

## Whole-project CLI/MCP golden parity — 2026-07-12

`tests/cli/whole-project-golden.test.ts` now covers a deterministic four-file
workspace through the source scanner, the built CLI worker, and the persisted
MCP health path. The focused one-worker run passed 1/1. It reconstructs the AI
Slop and Repository Health scores from the project/file findings, checks all
four score fields, score basis, completion/validity, and active-versus-audit
issue views, and confirms that the built scan—not a source-seeded snapshot—
writes the persisted MCP health contract. The fixture also asserts the current
bundle contains the local hook sentinel as a cheap build-identity witness.
It does not replace the explicit fresh-build gate recorded in the verification
refresh below. Incremental cache semantics are covered by the separate contract
below; hash-only cached files are intentionally not hydrated into project rules.

## Incremental-cache evidence policy — 2026-07-12 (superseded count)

`tests/cli/incremental-cache-contract.test.ts` passed 5/5 under one worker at
this checkpoint. The final rebuilt-package gate below supersedes it with 9/9.
It
seeds a hash-only cache with an active finding, changes the other file, and
verifies that the second run is explicitly `partial`/`incomplete`, uses only
successfully analysed files for its diagnostic score-basis coverage count, does not hydrate
ordinary cached findings into that incomplete score, and does not overwrite
health/cache evidence through `persistRun`. It also proves workspace-rooted
relative cache paths, fail-open malformed caches, cache-write failure
isolation, and truthful all-cache-hit messaging. Project-level rules over
cached files are intentionally not hydrated; the fail-closed policy is recorded
in the current verification refresh.

## Empty-scan renderer boundary — 2026-07-12

The direct human-renderer boundary is now score-free for not-applicable and
incomplete reports: `formatPretty`, brief/why-failing variants, and Markdown
return the explicit validity notice (Markdown additionally includes accounting
metadata). The regression
matrix is green: renderer/UX/heatmap/Markdown 78/78, renderer-contract 7/7,
and the ordinary plus staged/changed CLI matrix 30/30. Package typecheck/lint
and `git diff --check` also pass. The machine-format compatibility decision
for incomplete numeric diagnostics remains separate; this closes the direct
human-renderer leakage found in the manual review.

The combined current Gate 1/empty-scope focused rerun also passed 7 test files
and 132 tests under one worker (`scan-completion`, CI, Git-subset read-only,
renderer, whole-project parity, CLI/MCP golden, and incremental-cache policy).

## Whole-project renderer golden fixture — 2026-07-12

`tests/report/whole-project-parity.test.ts` adds a deterministic cross-surface
fixture with one active project-level finding, one active file-level finding,
and two default-off findings (one file-level and one project-level). The
contract is intentionally split: JSON and SARIF retain all four findings for
machine audit, while Markdown, HTML, and pretty retain only the two actionable
findings and report the two suppressed instances. All machine and human
surfaces preserve the canonical score values and score-basis denominator; the
MCP branch reads the same persisted health snapshot and verifies scores,
completion, validity, and basis. Project-level human findings now say
`project-wide` rather than `unknown context`.

Bounded evidence:

```text
tests/report/whole-project-parity.test.ts: 2/2 passed (one worker)
tests/report/finding-context.test.ts + tests/report/renderer-contract.test.ts: 11/11 passed
slopbrick typecheck: passed
```

This closes the deterministic renderer fixture and a project-level context UX
gap. The separate whole-project CLI/MCP golden test below now supplies the
end-to-end source-versus-built scan and persisted-health parity evidence; the
incremental-cache policy is fail-closed, with full hydration reserved for a
future versioned cache schema.

## v10.3 admission Task 0 bootstrap — 2026-07-12

The admission bootstrap probe is now implemented as the dependency-free
`scripts/cal/check-v103-admission-tools.mjs`, with seven focused behavioral
tests. The probe checks Node/API behavior, Corepack/pnpm, read-only Git, the
configured Python/pyarrow profile, and the tsup Node 18 build target. `du`,
`jq`, and `shasum` are explicitly diagnostic-only and cannot contribute to an
authority hash; the script has no network client surface and rejects
candidate-byte paths before access.

Evidence:

- focused admission-tools test: 7/7 passed under one worker;
- `node --check` and SlopBrick typecheck/lint passed;
- the real diagnostic exits 0 with `ready: true` and
  `authorityEligible: false`; and
- the candidate-byte probe exits 2 with zero stdout and the expected refusal.

The portability follow-up removes the machine-specific default corpus root;
candidate-byte paths now fail closed unless a caller explicitly supplies a
reviewed root. The focused test remains 7/7, `node --check`, SlopBrick
typecheck, and the package build pass, and the Task 6 packed pair was rerun
against the resulting source snapshot. This closes Task 0 Steps 1–2 only. Its
full affected-package/recursive gate and independent review remain open; no
corpus files were pulled or admitted.

### Task 0 Step 3 gate probe rerun — 2026-07-13

The non-mutating rerun passed SlopBrick `lint` (`tsc --noEmit`) and the focused
admission probe file, 1 file with 7/7 tests, under one worker. A fresh
`corepack pnpm -r build` then stopped at the expected Core codegen-freshness
guard: codegen reproduced the current `packages/core/src/generated/health.ts`
change and refused to continue until that generated peer is committed. The
command also completed the website prebuild/build before the Core stop; no
files were staged, committed, or reverted. Therefore Task 0 Step 3 remains
open pending the owner-authorized clean boundary, recursive gates, independent
review, and bootstrap commit.

## Historical bounded verification refresh — 2026-07-13 (superseded)

The final current-source Task 6 pair was rerun after the cache-contract,
renderer, fail-closed hook, report-format, golden-fixture, persistence, and
report-UX changes. It used one tarball
`/tmp/slopbrick-task6-current-final-20260713/slopbrick-0.44.0.tgz` with
SHA-256
`61d3b9e9c3a8d8d01b19425431f3dca56d395e30b719461f1c0e34ad9c398846`.
Node 22.22.3 and Node 24.15.0 each passed 9/9 under strict offline
installation; both receipts agree on builder commit
`81e79b9ec47f46f70575ae650beccd3a278b60e5`, dirty status, 179 status entries,
and status SHA-256
`0142d40f2cca9afa8f713d7e5bf4ed9fc1f9e5e6895f73771fa2764e2574fe6f`.
Receipts:

```text
/tmp/slopbrick-task6-current-final-20260713/receipts/task-6-packed-consumer-node-v22-22-3.json
/tmp/slopbrick-task6-current-final-20260713/receipts/task-6-packed-consumer-node-v24-15-0.json
```

The definitive bounded full SlopBrick suite passed 276 files (5 skipped) and
3,208 tests (9 skipped) with one Vitest worker and a 2 GiB heap cap. Recursive
typechecks passed; Core, website, and engine tests passed 116/116, 37/37, and
59/59; website build and SlopBrick build passed. The recursive build is
intentionally not reported green: Core's codegen-freshness guard stops on the
existing uncommitted `packages/core/src/generated/health.ts` change. No
generated bytes were reverted or staged. The current focused rebuilt-package
gate is 19/19: incremental cache 9/9, whole-project CLI/MCP golden 1/1,
renderer contract 7/7, and cross-surface parity 2/2. The rebuilt hook and
packed-consumer checks add 16/16 focused tests, including the real-commit
missing-binary failure case on both supported Node lines. The report command now
rejects unsupported output formats with exit 2 rather than silently falling
back to pretty output; machine-readable incremental JSON remains parseable.

The current built-CLI self-scans are complete/valid and parse-error free:

| artifact | bytes | analyzed | active | suppressed | AI slop | hygiene | security | repository health |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `/tmp/slopbrick-self-scan-current-final.json` | 456,234 | 208/208 | 119 | 273 | 4.6849 | 99.9501 | 100.0000 | 98.1111 |
| `/tmp/platform-root-self-scan-current-final.json` | 893,208 | 439/439 | 213 | 551 | 4.3466 | 99.7989 | 83.3333 | 94.8677 |

Both scans had zero failures/skips/parse errors and no failed thresholds;
their effective score denominators are 208 and 439. The rebuilt artifacts remain
complete and parse-error free. This is accounting/renderer evidence, not
calibration or release approval. The manual review is closed for diagnostic
triage; compression-profile dominance and self-referential rule/example
findings remain visible, contextualized, and explicitly non-release evidence.

## Corpus centralization refresh — 2026-07-13

The legacy corpus is now physically rooted under `/Users/cheng/corpus-expansion`.
The former external baseline symlinks were materialized into
`positive/baseline` and `negative/baseline`; the copied content-tree digests
match the source-at-copy digests for 5,833 positive files and 52,280 negative
files; the maintained selected lists contain 5,809 and 52,280, while generated
`.slop-audit` artifacts are excluded and quarantined. Maintained filelists were
rewritten to canonical paths and retain
224,903 positive and 227,479 negative unique paths with zero exact overlap.
The v10.3 JSONL inventories were rebuilt from those lists with 224,903 and
227,479 indexed records and zero missing files. The review validator passes.
The durable receipt is
`/Users/cheng/corpus-expansion/v10.3/review/corpus-centralization-receipt-2026-07-13.json`.
This remains quarantine inventory: no label was promoted and no admission
manifest, selection, run, or new-source acquisition was created.

## Pretty renderer lane clarity — 2026-07-13

The human-readable pretty report now separates active findings into explicit
`AI-specific signals` and `Engineering findings` lanes. `severity: off`
instances are filtered before lane assignment, and an empty lane is explained
without claiming that the whole report is clean. The Markdown renderer's
existing `Issue.aiSpecific`-based AI Findings/Engineering Hygiene split remains
unchanged in this slice. The new pretty test was intentionally red (3/3
failures) before implementation, then passed 3/3; the full report suite passed
160/160 under one Vitest worker, package typecheck passed, and an independent
review approved the change with no P0-P2 findings. Scores, JSON/SARIF, CLI
exit behavior, and calibration inputs are unchanged. Together with the
stratified review and score-message correction, this closes the diagnostic
presentation/usefulness decision; release-signal use remains gated by clean
boundary and provenance evidence.

The first direct full-Vitest run after the source edit failed one golden parity
assertion because `dist/index.js` still contained the prior `Issues (5)`
renderer. A SlopBrick rebuild fixed the artifact boundary; the failed test then
passed 1/1 and the full suite passed 273 files (5 skipped), 3,201 tests (9
skipped). The gate now records build-before-golden as an explicit ordering
requirement.

## Active-signal score-message consistency — 2026-07-13

Manual rendering of the current package self-scan exposed a contradiction:
117 active AI-specific findings were shown alongside `AI Slop Score: 4.7
[NO SLOP]` and `Repo has no detectable AI slop`. A TDD regression was red
before the fix (one active-AI case failed, one clean case passed), then green
2/2. The shared report-aware band/message helper now keeps pretty, composite,
verdict, and brief output qualified as `LOW` / `low amount of AI slop` when an
active AI-specific finding exists below 10, while clean/no-AI reports retain
the no-slop wording. The full report suite passed 162/162; the rebuilt
source/built golden test passed 1/1; the full SlopBrick suite passed 273 files
(5 skipped), 3,201 tests (9 skipped); typecheck, build, diff check, and
independent review passed. Scores, JSON/SARIF, CLI exit behavior, and
calibration inputs are unchanged. The diagnostic usefulness decision is closed;
compression-cohort and self-referential root-context findings remain
non-release context pending provenance and clean-boundary gates.

## Rebuilt source-bound self-scan refresh — 2026-07-13

After the Heaps wording change and rebuilt CLI, package-relative one-thread
self-scans were rerun and the canonical JSON artifacts were refreshed in place:

| artifact | bytes | analyzed | active | suppressed | AI slop | hygiene | security | repository health |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `/tmp/slopbrick-self-scan-current-final.json` | 456,234 | 208/208 | 119 | 273 | 4.6849 | 99.9501 | 100.0000 | 98.1111 |
| `/tmp/platform-root-self-scan-current-final.json` | 893,208 | 439/439 | 213 | 551 | 4.3466 | 99.7989 | 83.3333 | 94.8677 |

SHA-256: package `887a483d8e13063fe21709ad263cd08cb930ed2e2a1139ec006be97cb1f88ff7`;
root `e29d60b22141a0ee9d1b24b5e0d9bda690df0b5062bed07b859469df525d7bce`.
Both are complete/valid with zero failed, skipped, and parse-error counts;
the latest runtime wording changes alter message bytes and small diagnostic
counts, while the same compression/self-reference caveats remain.

## Live clean-boundary audit — 2026-07-13

The current checkout was recomputed after the final renderer/rule/explain/language/audit work:
branch `codex/v0.45-recovery`, HEAD
`81e79b9ec47f46f70575ae650beccd3a278b60e5`, 249 status entries, status SHA
`279d914a57f088c89a7f82a43c6961ba196f61387ff69d579611ec37ab3467ea`, and
`git diff --check` clean. Fresh in-memory Core codegen matches the current
generated health peer byte-for-byte (5,241 bytes; SHA
`e814e3b380c54cdd20e280c24a91b35957cbe36d9300d2f975d0181ce9d13cde`), but
the generated file is uncommitted, so the freshness guard correctly fails.

The coordinated clean-builder concern is larger than that two-file drift. It
includes the root, Core, Engine, SlopBrick, and Website package manifests plus
`pnpm-lock.yaml`, the Core README, and the health schema/generated pair, along
with the intended behavior/docs/spec changes in this dirty worktree. No safe
commit subset can be inferred from the 188 mixed entries. Owner authorization
is required before staging/committing the reviewed tranche; after it, rerun
Core contract and recursive gates plus clean Node 22/24 Task 6 receipts bound
to the new commit. No commit, push, tag, publication, deployment, or remote
state changed in this audit.

The index already contains an independent five-path SlopBrick beacon/CLI/
calibration boundary (`src/beacon/endpoint.ts`, `src/cli/program.ts`,
`tests/beacon/cli-nonblocking.test.ts`,
`tests/cli/large-json-output.test.ts`, and
`tests/integration/calibration.test.ts`); two of those also have unstaged
follow-up edits. They must be explicitly retained or split during the owner-
authorized commit review and must not be accidentally bundled with the Core or
dependency migration.

## Hint-policy and explain-surface slices — 2026-07-13

The RULE_HINTS policy slice rewrote statistical/design guidance so it gives
engineering context instead of asking an agent to vary code to look human or
to infer authorship. The coverage, non-empty/length, duplicate-key, and
detector-evasion policy tests pass 4/4; SlopBrick typecheck passes, and the
independent wording review found no remaining changed hint that teaches score
shaping. Runtime rule messages remain a separate wording surface.

The rule-level `explain` formatter now labels its built-in static policy,
evidence category, historical calibration estimates with percent/lift units,
confidence-limit unavailability, and the absence of a location-specific fact
or snippet. Its focused contract suite passes 9/9; the rebuilt bundled command
was inspected and the snapshot suite passes 3/3. This is an honest partial
UX improvement, not exact matched-fact or v10.3 cohort evidence.

## Advertised-language routing smoke — 2026-07-13

The rebuilt CLI language smoke creates a fixture for every advertised
extension in the 12 `LANGUAGE_SUPPORT` rows (26 files), passes an explicit
`--include '**/*'`, and runs one worker with telemetry/color disabled. The
direct source `runScan` path and rebuilt CLI both pass 1/1 with complete/valid
accounting, requested = analyzed, zero failure/skip/parse/timeout/crash/internal
counts, exact expected per-file paths, and a non-empty `facts.v2._source` for
each direct result. The common placeholder rule fires exactly once for every
extension, exact issue paths are asserted, Rust/TSX parser witnesses are
checked, and the tsup source-map freshness guard passes before the packaged
run. This closes the four-layer language matrix contract; clean-builder and
release gates remain separate.

## Historical bounded full-suite rerun — 2026-07-13 (superseded)

After the explain snapshot update, the serial one-worker suite passed **276
files** (5 skipped), **3,208 tests** (9 skipped) under the 2 GiB heap cap.
The previous failure was only the expected snapshot mismatch from the new
explain output; the corrected snapshot now passes. `git diff --check` remains
clean. These are dirty-worktree gates and do not create a clean release receipt.

## Runtime wording, explain provenance, and deterministic smoke — 2026-07-13

The runtime Issue wording audit now AST-scans actual `message` and `advice`
property initializers (excluding comments and descriptions). Its red pass
found 70 unsupported AI/LLM, human-vs-AI, training-data, or authorship claims;
the green pass neutralizes those strings without changing rule IDs, thresholds,
severities, polarity, or detection logic. The policy test is 1/1 and the
targeted rule suite is 175/175.

The rule-level explain surface now prints the schema-validated historical
calibration date and explicitly says that v10.3 source/cohort provenance is
unavailable. It does not invent exact finding spans or snippets. The explain
contract suite is 10/10 and the rebuilt snapshot/bundled CLI checks pass.

`tests/calibration/v103-smoke.test.ts` adds a deterministic 100-verified-AI +
100-verified-human synthetic run. It accounts for all 200 selected records,
including two timeout and two scanner-failure terminal cases, and repeats the
run byte-equivalently. Its follow-on unequal-arm case covers 3/7 polarity
sizes, zero-fire observations, and serial/three-worker equivalence; the focused
smoke suite is now 3/3. This proves mechanics, not corpus eligibility or
calibration quality, and does not make the unavailable metric producer
available.

## Historical source-bound self-scan refresh — 2026-07-13 (superseded)

After the runtime wording slice, language smoke correction, and rebuilt CLI,
the package-relative and repository-root scans were rerun serially with one
worker, `--no-telemetry`, and color disabled. Both JSON artifacts are complete
and valid, with requested = analyzed and no failed, skipped, parse, timeout,
crash, or internal-failure outcomes:

| artifact | bytes | analyzed | active | suppressed | AI slop | hygiene | security | repository health | SHA-256 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `/tmp/slopbrick-self-scan-current-final.json` | 456,234 | 208/208 | 119 | 273 | 4.6849 | 99.9501 | 100.0000 | 98.1111 | `887a483d8e13063fe21709ad263cd08cb930ed2e2a1139ec006be97cb1f88ff7` |
| `/tmp/platform-root-self-scan-current-final.json` | 893,208 | 439/439 | 213 | 551 | 4.3466 | 99.7989 | 83.3333 | 94.8677 | `e29d60b22141a0ee9d1b24b5e0d9bda690df0b5062bed07b859469df525d7bce` |

The score changes are diagnostic-only consequences of current dirty bytes;
they do not close the compression/self-reference review, corpus provenance,
or clean-builder receipt gates.

## Finding evidence, rebuilt gates, and historical source refresh — 2026-07-13 (superseded)

The finding-level evidence slice is independently approved. `Issue.evidence` is
an optional typed union with exact bounded spans or explicit producer omission;
`typo/placeholder-text` emits the exact matched attribute span and typed
matched facts for normal values. Report JSON preserves the evidence, while MCP
projects it under `whyItFired.evidence` with deterministic bounds, path/secret/
source-like redaction, recursive details-drop metadata, and an honest
`status: "omitted"` when the exposed evidence is not exact. MCP message,
advice, and summary fields are bounded and sanitized at the boundary. No Core
health/structure schema or v10.3 provenance claim was added.

Independent review returned Spec Compliance **approved** and Task quality
**Approved** with only conservative-heuristic maintenance notes. The focused
slice now passes **95 tests** across the rule, MCP, docs, and language-contract
files; SlopBrick typecheck, MCP documentation generation, and `git diff --check`
also pass.

The rebuilt SlopBrick package build passes with the existing non-fatal Zod DTS
warnings. The bounded serial package suite then passed **276 files** (5
skipped) and **3,248 tests** (9 skipped) under one Vitest worker and
`--max-old-space-size=2048`. Recursive typecheck passes for Core, Engine,
SlopBrick, and Website (Website reports three existing hints and zero errors).

Fresh built-CLI self-scans were rerun serially with one worker, telemetry and
color disabled, using the package workspace and repository-root workspace:

| artifact | bytes | analyzed | active | suppressed | AI slop | hygiene | security | repository health | SHA-256 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `/tmp/slopbrick-self-scan-current-final.json` | 452,624 | 208/208 | 119 | 274 | 4.6849 | 99.9501 | 100.0000 | 98.1111 | `8d97799a84678213b2a229df104af5090bb901d94939ba422f48f4868e4fc8e8` |
| `/tmp/platform-root-self-scan-current-final.json` | 878,369 | 439/439 | 212 | 550 | 4.3307 | 99.7989 | 83.3333 | 94.8741 | `c3311001d00cfc844d2c46632a56cca23fd9acc3df004c777994f189a90caa62` |

Both reports have `completionStatus=complete`, `scoreValidity=valid`,
requested = analyzed, zero failed/skipped/parse/timeout/crash/internal-failure
counts, and no failed thresholds. These are diagnostic self-scan artifacts;
compression/self-reference findings, corpus provenance, and clean-builder
receipt requirements remain open.

The v10.3 admission-tool bootstrap was rechecked with the explicit reviewed
Python environment at `/Users/cheng/corpus-expansion/v10.3/review/.venv/bin/python`:
all authoritative prerequisites report `pass`, `ready: true`, and
`candidateBytes.accessed: false`, but the diagnostic correctly remains
`authorityEligible: false` because no approved commit-bound admission context
exists. `du`, `jq`, and `shasum` remain diagnostic-only; no candidate bytes or
network client were used. This does not advance Task 0 Step 3 or create a
manifest.

The live worktree now contains **256 status entries** with status-list SHA-256
`7c822178635e83703415d566cdd4e2aabbb4bc21bd8d98e34ef6534a255cf068`. This
supersedes the earlier 249-entry documentation snapshot; the old Task 6
receipts remain bound to their original 179-entry dirty snapshot and were not
reused as release evidence.

## Task 2A envelope-relation verifier — 2026-07-14

`packages/slopbrick/src/calibration/v103/admission-overlap-publication.ts`
now exposes `verifyOverlapArtifactRelations`. It validates each Core envelope
and then joins the generation, index, resource receipt, and ledger through
authority hashes, the ledger's index receipt hash, coverage/count/completion
flags, and exact generation-artifact byte/hash/kind bindings. Publication,
recovery, and read-only current verification all pass through the immutable
tree verifier that invokes this relation check.

Evidence: publication **24/24**, CLI **5/5**, combined publication/CLI
**29/29**, and the seven-file overlap focus **55/55** with
`NODE_OPTIONS=--max-old-space-size=2048` and one Vitest worker. The full
SlopBrick package gate passes **301 files / 3,466 tests** (5 skipped files / 9
skipped tests), Core passes **22 files / 183 tests**, schema validation passes,
recursive typecheck passes with zero errors, and `git diff --check` passes.
The package build succeeds with the existing non-fatal Zod DTS warnings.

This remains fixture/local authority evidence only. Recursive build is still
blocked by the deliberate uncommitted Core observation/health generated-peer
freshness guard; no real corpus bytes were read or admitted, and external
tool authority, checkpoint/resume, clean commit-bound receipts, release, and
deployment remain open.

## Scope and Task 6 clean-boundary audit — 2026-07-14

The recovery diff is predominantly tests, fixtures, documentation, and
contract/recovery machinery rather than default-on scanner behavior. Against
`main` it is 292 files and +43,812/-1,900 lines, classified approximately as
runtime +10,124, tests/fixtures +16,120, docs/plans +15,822, and
schemas/generated +320. This explains the large line count without implying
that the scanner was rewritten or that every release gate is closed.

Read-only dependency review: current production and full-graph moderate-level
audits report zero advisories. The migration is intentionally uncommitted and
still needs independent review plus a clean recursive boundary. The local
ignored Node 22/24 receipt files are not release evidence: Node 22 is an older
receipt shape/tarball and Node 24 is a newer dirty snapshot; they do not share
one exact tarball and builder identity. The live worktree is 716 status
entries (5 staged, 445 untracked). Regenerate both receipts only after an
explicitly authorized scoped clean Task 5+6/dependency boundary. No remote,
corpus, or publication state changed.

## Gate 1 score/property and valid-report accounting extension — 2026-07-14

The bounded score matrix now has an assembly-level end-to-end slice:
`tests/cli/score-contract-matrix.e2e.test.ts` passes **6/6** through the
effective-issue projection, score assembly, and JSON/SARIF/pretty/Markdown/
HTML renderers. It covers AI-only, hygiene-only, backend-only, mixed, and
audit-only findings, bounds, score-basis conservation, health monotonicity,
and input-order invariance. `tests/engine/score-contract-properties.test.ts`
passes **3/3** with 150 randomized cases for bounded scores, harmful-evidence
monotonicity, and non-AI security separation. Valid Markdown and HTML now
render detailed scan accounting when it is available; selection-only reports
render unknown zero-finding/failure buckets as `n/a`; renderer-contract
coverage is **10/10**. This is still synthetic/local evidence and does not
close the full runScan/subcommand, health-wire, provenance, corpus, or release
gates.

## Historical package gate refresh — 2026-07-14 (superseded)

After the accounting fix, the rebuilt SlopBrick package gate passes **303 test
files / 3,477 tests** (5 skipped files / 9 skipped tests) with one Vitest
worker and `NODE_OPTIONS=--max-old-space-size=2048`. The affected renderer
contract is green at 10/10, including the regression that keeps unknown
selection-only zero-finding and failure buckets as `n/a`. Workspace
typechecking is also green for Core, Engine, SlopBrick, and Website (zero
errors; Website retains three non-blocking hints). `git diff --check` passes.

At this earlier package-gate checkpoint the dirty snapshot was **729 status
entries** (448 untracked); the later bounded validity/toolchain follow-up
supersedes that count. These results are not a clean-commit or release
receipt.

This is a dirty-worktree verification and supersedes neither the clean-builder
receipt requirement nor the recursive build boundary: Core's codegen freshness
guard still detects the intentionally uncommitted generated observation/health
peers. No corpus bytes, commit, remote, release, or deployment state changed.

## Historical workspace-binary self-scan correction — 2026-07-14 (superseded)

The release self-scan was rerun through the explicit workspace binary
(`corepack pnpm --filter slopbrick exec -- node ./bin/slopbrick.js ...`) after
the bare `slopbrick` command was found to resolve the stale packed
`node_modules/slopbrick` left by the Task 6 consumer test. The workspace-bound
run is the only current self-scan evidence:

| selected/analyzed | observed/excluded | active/total issues | AI slop | hygiene | security | health | exit |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 227/227 | 765/538 | 157/532 | 17.1909 | 99.4702 | 100.0000 | 92.9647 | 1 (findings) |

The JSON is `completionStatus=complete`, `scoreValidity=valid`, and has zero
parse, timeout, crash, internal, or skipped outcomes. The targeted Zipf output
uses the neutral non-authorship wording from the current source. The stale
packed-binary result is explicitly discarded and must not be used for release
scores. The release checklist and canonical plan now require the explicit
`node ./bin/slopbrick.js` path. This remains diagnostic dirty-worktree
evidence; no source, corpus, or remote state changed.

## Human-report evidence parity — 2026-07-14

The final bounded renderer gap from the finding-evidence review is now closed:
HTML report issue rows render an exact bounded snippet and source span when
`Issue.evidence.exact` is present, and render an explicit omission reason and
span when the producer cannot expose exact evidence. The HTML output is escaped
and bounded by the same producer contract as JSON, Markdown, pretty terminal,
and SARIF projections; this does not imply that every rule currently produces
an exact span. The focused HTML suite passes **15/15**, and the combined
CLI/MCP/report parity slice passes **268 tests**. SlopBrick typecheck, build,
and `git diff --check` remain green (with the existing non-fatal Zod DTS build
warnings). This is a local renderer-contract improvement, not v10.3 cohort
evidence or release approval.

## Bounded validity, threshold, and toolchain follow-up — 2026-07-14

The next bounded review closed four local contract gaps:

- DB/docs subcommands now fail closed for ordinary empty or partial canonical
  scans, omit domain scores from partial machine envelopes, and emit explicit
  score-free `not-applicable` metadata when their own domain denominator is
  zero. Docs-only workspaces retain a measured docs score, and Git-scoped
  empty scans remain intentional no-ops. The focused validity matrix is
  **26 tests**; the combined validity/score/workflow focus is **95 tests**.
- Complete scan JSON now exposes the same `failedThresholds` value used by the
  CLI exit gate. The regression catches the previous self-scan mismatch where
  stderr reported `meanSlop` failure while JSON serialized an empty list.
- Core codegen freshness now considers unstaged, staged, and untracked schema
  and generated files, and checks schema/generated-peer parity including
  orphan peers. The pure helper contract is **2/2**. The current contract gate
  therefore fails honestly on the intentionally dirty 71-schema/71-generated
  admission tranche plus the tracked observation/health changes; no bytes were
  discarded or hidden.
- Website `dev` regenerates `product-facts.json` before Astro starts, and
  workflow jobs use the repository's exact `packageManager` instead of a
  floating pnpm major. The website unit suite is **38/38**, built output has
  four pages, and Playwright/axe is **16/16**.

The refreshed RAM-bounded SlopBrick package gate is **307 test files / 3,540
tests** (5 skipped files / 9 skipped tests). Core is **23 files / 185 tests**;
Engine is **5 files / 59 tests**. Recursive typechecking is green with zero
errors; Website retains three non-blocking hints. Production and full-graph
moderate dependency audits report no known vulnerabilities. The current
workspace self-scan is complete/valid at **227/227**, with **157 active / 531
total** findings (**374 default-off suppressed**), AI Slop **17.1909**, hygiene **99.4702**, security **100.0**,
health **92.9647**, and `failedThresholds=["meanSlop"]`; exit 1 is the expected
policy result, not a scan crash. The current worktree is **757 status entries**
(**456 untracked**). These remain dirty-worktree diagnostics, not corpus
admission or release receipts.

## MCP stdio and corpus authority follow-up — 2026-07-14

The MCP newline-delimited transport now flushes a final valid JSON-RPC request
when the input stream closes without a trailing newline. The regression was
red before the change and green afterward; the six MCP files pass **110/110**,
SlopBrick typecheck passes, and `git diff --check` is clean. This closes one
transport edge case only.

The read-only corpus audit confirms **317/317** pinned checkouts resolve under
`/Users/cheng/corpus-expansion/` with matching recorded HEADs. The declared
inventories remain **224,903 AI rows** (SHA-256
`4511193d10dc39ac7589047e945c531d20752b3344b1d22e96a9d348bc7d34f0`) and
**227,479 human rows** (SHA-256
`85ef9452e96977ec21578a8f87026d374ac510daad20fd34af8d6d363b38c215`). No
additional repository pull is justified. Authority remains
`verified_ai=0`/`verified_human=0`: the 329-entry generation-0 admission
register, source reviews, evidence/tool/acquisition receipts, complete
ledgers, and witness are absent. AI-positive/human-negative remains declared
polarity, not admitted truth; no flat manifest or receipt was generated and no
corpus bytes were changed.

The current tracked diff census is **460 files, +62,175/-6,139 lines** against
`main`; by path bucket this is approximately **+17,736 SlopBrick docs**, **+1,684
root docs/specs**, **+20,904 tests/fixtures**, **+12,375 SlopBrick runtime**,
**+1,896 Core**, **+429 Engine**, **+380 Website**, **+3,730 tooling/CI/release**,
and **+3,041 other package** lines. The worktree is **757 status entries** with
**456 untracked files** and
approximately **48,784 untracked lines**. Its reproducibility hash is status
SHA-256
`7e1175279aa03f5dec68d66f6fed278bf2bbe40587326e76d998f60a7f338e50`.
The larger figures supersede earlier dirty checkpoints and reinforce the
implementation freeze.

## Read-only genesis composition diagnostic — 2026-07-14

The new `cal:admission:genesis` command was run against the frozen v10.3
inventories. It produced 329 register entries and 329 source-quarantine
reviews, conserved 452,382 selected units, preserved the exact AI/human file
hashes, and emitted zero candidate/eligible units. It is deliberately
non-persistent (`authorityEligible=false`, `persisted=false`); no corpus or
external admission files changed. The focused test is 4/4 and package
typecheck is green. Use `pnpm --silent` when consuming its `--json` stdout so
the package-manager banner does not precede the JSON document.

The final RAM-bounded SlopBrick package rerun passes **306 test files / 3,519
tests** (5 skipped files / 9 skipped tests) at one worker and exits 0. It
includes the genesis composition contract and the MCP no-trailing-newline
regression; the 119-rule registry/catalog regeneration is also green. The
genesis contract includes polarity binding and normalized-summary conservation
regressions: a repository declared human cannot contribute to the AI arm (or
vice versa), and inconsistent repository byte totals are rejected. The focused
genesis suite is **4/4**.

## Synthetic clean commit-boundary consumer diagnostic — 2026-07-14

A disposable copy of the current platform bytes was committed only under
`/tmp/platform-clean-cVXytA` at synthetic commit
`f47afa0847ad0c8a3b0c8a507f3ee32e8a691946`, excluding the unrelated root
artifacts `TODO.md`, `.astro/types.d.ts`, and `src/env.d.ts`. It had zero
status entries and status SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

Recursive typechecks passed with zero errors (Website retained three
non-blocking hints). The recursive build passed for Core, Engine, SlopBrick, and Website; Website
emitted four static pages and only the known non-fatal Zod declaration warning
remained. One offline `slopbrick@0.44.0` tarball passed the packed-consumer
suite **9/9** on Node 22.22.3 and Node 24.15.0. The paired-receipt assertion
also passed with tarball SHA-256
`84a5c5115b59b1566611fcdb4c41891eb630a8d25a7259f8af92e866717bf0fc`, the
synthetic commit, clean status, and status hash.

This is a diagnostic clean-snapshot result only. The synthetic commit is not
the real branch commit and cannot substitute for owner-authorized commit-bound
release evidence.

The complete JSON genesis diagnostic was captured outside the repository at
`/tmp/v103-genesis-diagnostic-20260714.json` (530,426 bytes,
SHA-256 `aec85e3901069d6dc1aba23b782b387e4ff0c4965160455e82c734bccd48be87`).
Independent validation returned `ok=true`, 329 registered/reviewed sources,
and 452,382 quarantined units; no external admission artifacts were created.

## Task 2A terminal checkpoint/resume and sidecar-integrity follow-up — 2026-07-14

The overlap builder now writes four canonical, no-clobber, fsynced local
checkpoints when a task-scoped invocation intent is supplied. The phases are
`postings`, `candidate_pairs`, `exact_edges`, and `clusters`; each checkpoint
binds the universe, normalizer registry, policy, input/output shard hashes,
and continuation cursor. A terminal `clusters` checkpoint resumes without
reading the universe stream or source bytes. Intent mismatches, altered
checkpoint bytes, substituted shards, and non-terminal resume requests fail
closed. Terminal recovery now validates Core index, ledger, and resource
receipt shapes/self-hashes and their authority/completion joins before it
returns a result; this closes a real local-sidecar tamper gap.

The checkpoint/resume suite is **4/4** green, the overlap/builder/scale/
publication/CLI/resume and health-structure focus is **90/90**, SlopBrick
typecheck passes, and `git diff --check` is clean. Only terminal resume is
implemented; phase continuation from an earlier checkpoint remains open. The
full RAM-bounded package gate is now **306 files / 3,519 tests** (5 skipped
files / 9 skipped tests), with no corpus, commit, push, release, publish, or
deployment mutation.

## Disposable source-bound self-scan refresh — 2026-07-14

The rebuilt workspace binary was rerun in a disposable copy with project
memory and telemetry disabled, one worker, and no repository mutation. The
JSON artifact is `/tmp/slopbrick-self-scan-current-20260714-final.json`
(585,972 bytes, SHA-256
`20cb0bcad4b02925e974c7f61585ef683e0b798b5e62be680e51099203f8f623`). It
analyzed **227/227** files with zero failure/skip/parse/timeout/crash/internal
outcomes, **157 active / 531 total** findings, **374 default-off suppressed**,
AI Slop **17.1909**, hygiene **99.4702**, security **100.0000**, health
**92.9647**, and `failedThresholds=["meanSlop"]`. Exit 1 is the expected
policy result, not a crash; this remains diagnostic rather than release
evidence. The brief terminal output is useful and internally coherent. The
headline now names the metric (`Repo's AI-slop score is low`) so the
lower-is-cleaner direction is not ambiguous; the focused UX regression passes.

The follow-up scan after that correction retains the same valid 227/227
coverage, 157 active / 531 total findings, and expected meanSlop threshold
failure. The new disposable JSON receipt is
`/tmp/slopbrick-self-scan-current-20260714-ux.json` with SHA-256
`4e52627940f6d4d5a6419387ed42a26fa866f15895804723cee56fdecaf30593`.

## Genesis blocker-count diagnostic refresh — 2026-07-14

The read-only genesis command now reports deterministic, overlapping blocker
counts alongside its zero-authority result. Receipt:
`/tmp/v103-genesis-diagnostic-20260714-blockers.json` (531,205 bytes,
SHA-256
`727263f7806c8039ec2afce4079d48778d8cb5965a6b7eef81be69eaa7d683a3`).
The real frozen inputs produce 329 reviewed source entries and 452,382
quarantined additive units, with 0 candidate sources, 0 candidate units, and
0 eligible units; `authorityEligible=false` and `persisted=false`. Counts
(source reviews / additive units) are: authorship_unproven 323 / 452,382;
evidence_unresolved 327 / 394,293; family_unknown 318 / 394,293;
license_scope_ambiguous 10 / 58,089; materialization_unverified 3 / 0;
review_incomplete 329 / 452,382; source_bytes_unbound 1 / 58,089;
source_inventory_open 1 / 0; source_wide_quarantine 329 / 452,382. Counts
overlap by design and omit the aggregate inventory view from additive totals.
The focused genesis suite is 5/5 and typecheck is green. A second read-only
run with the same frozen inputs and timestamp produced the identical diagnostic
SHA-256, confirming byte-level reproducibility. No corpus bytes,
manifest/selection/run artifacts, or remote state changed, and no admission
promotion is justified by this diagnostic.

## Final full package and health-wire matrix gate — 2026-07-14

The latest RAM-capped serial SlopBrick package gate passes **307 test files /
3,540 tests** (5 skipped files / 9 skipped tests), including the genesis,
persisted-health, heatmap, and doctor validity coverage. Core remains **23/185**
and Engine **5/59**; recursive typecheck remains green. The focused validity and
renderer matrix is **121/121**. The package build has only the known non-fatal
Zod declaration warnings. The clean codegen boundary, real 452,382-unit
admission/calibration, and release gates remain open; no corpus or remote state
changed.

The post-gate worktree census is **757 status entries / 456 untracked files**
with approximately **48,784 untracked lines**; the tracked diff against `main`
is **460 files, +62,175/-6,139 lines**. Current status-list SHA-256:
`7e1175279aa03f5dec68d66f6fed278bf2bbe40587326e76d998f60a7f338e50`.

`HEAD` is `81e79b9ec`, 260 commits ahead of `main`; the current uncommitted
tracked delta against `HEAD` is **301 files, +18,859/-4,735 lines**. The
460-file figure is cumulative branch-vs-`main` history, so the clean-boundary
review concerns the dirty delta plus untracked files, not a rewrite of all
existing branch commits.

## Historical synthetic clean-boundary consumer rerun — 2026-07-14 (superseded by corrected v3 below)

To test the implementation at a real clean commit boundary without changing
the owner-controlled worktree, the current bytes were copied to
`/tmp/platform-clean-current-AAX30H`, excluding only `TODO.md`,
`.astro/types.d.ts`, and `src/env.d.ts`. The disposable snapshot was committed
at synthetic commit `0118fdeb02fe712b482729f1ea612aa8496989a0`; its status is
empty and its empty-status SHA-256 is
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
The snapshot predates later documentation-only plan/evidence edits, so its
implementation commit is not byte-identical to today's full documentation.
The dedicated recursive typecheck log is
`/tmp/platform-clean-current-AAX30H-typecheck.log` (SHA-256
`77a8ba5d727e74962cdb7fb9f4dd04f7db7e699e54f3e03eb09e82b86e17b93f`).

The snapshot passes recursive serial typecheck (0 errors; the same three
non-blocking Website hints), recursive build, and recursive serial tests:
**307 SlopBrick test files / 3,540 tests passed**, with 5 intentionally skipped
files / 9 skipped tests. The build output contains only the known non-fatal Zod
declaration warnings.

Packed artifact:

- path: `/tmp/task6-clean-current-AAX30H/slopbrick-0.44.0.tgz`
- SHA-256: `c048fe0ac0e4d7fb1a988e7899c3a6765b87f8cc6e09c13d63521ce548beb826`

The packed consumer passes **9/9** on Node **22.22.3** and **9/9** on Node
**24.15.0**. Both runs used strict offline dependency installation and
local-process-only CLI execution. The current receipt directory is
`/tmp/task6-clean-current-AAX30H/receipts-v2/`; the external packer is recorded
as Node 24.15.0, npm 11.12.1, and pnpm 10.34.5. The paired receipts bind the
same artifact, synthetic commit, and clean status:

- Node 22 receipt SHA-256:
  `0858f8f9125fc3303071ffcd9e10dac83900004174f964c673e1bbbacef12919`
- Node 24 receipt SHA-256:
  `f393f8dc7ca5fee719fd2561c9b48ff249c3ae837529b03ced355936675dacf5`

Each receipt reports successful install, ESM import, CJS require, materialize,
select, and verify stages. This is diagnostic evidence that the current bytes
survive a clean build and packed-consumer boundary; it does not replace the
owner-authorized branch commit, release receipt, npm publication, deployment,
or the still-zero-authority real corpus admission/calibration gates.

## Partial-watch heatmap validity matrix — 2026-07-14

`tests/integration/watch-mode.test.ts` passes **45/45** with one worker and the
built SlopBrick CLI present; without that binary the suite is explicitly
skipped rather than reporting vacuous passes. The
14 added cases cover partial scans (one successful file and one parse failure)
through both watch invocation forms and human, JSON, SARIF, HTML, JSON-file,
HTML-file, and quiet heatmap output. Each path retains partial validity and
requested/analyzed/failed accounting, omits all five canonical numeric
headline fields from streamed and file-output streams, and stays stable after
debounce. This is automated evidence for the partial-watch slice;
manual keyboard-copy and terminal-width UX remain open.

## Corrected packed-consumer provenance rerun — 2026-07-14

The packed-consumer harness was corrected so an externally supplied tarball
preserves independently supplied packer metadata; the consumer install runtime
is recorded separately. The disposable clean snapshot is synthetic commit
`79d44f58d8e3da7c87588cf9d7d78e42ce021642`, with zero status entries and empty
status SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

The v3 tarball is
`/tmp/task6-clean-current-AAX30H-v3/slopbrick-0.44.0.tgz`, SHA-256
`c048fe0ac0e4d7fb1a988e7899c3a6765b87f8cc6e09c13d63521ce548beb826`.
Node 22.22.3 and Node 24.15.0 each pass **9/9** with strict offline install
and local-process-only CLI execution. The corrected receipts bind packer Node
24.15.0, npm 11.12.1, pnpm 9.15.0; `stages.install.pnpmVersion=10.34.5` is
the separate consumer installer value.

- Node 22 receipt SHA-256:
  `7197b4860be4f1cd8457ecea274186a5cf7b6be68cc16287b838509f3feb5541`
- Node 24 receipt SHA-256:
  `ebb31a46401f6f55f9f17bc4918fe93700a32493701990c2625c46cbb45ca8b2`

This is diagnostic evidence only and does not close owner authorization,
corpus provenance/admission, calibration, release, publish, or deployment.

## Live package-local self-scan cleanup — 2026-07-14

The source-bound command was run through the explicit package-local binary:
`corepack pnpm --filter slopbrick exec -- node ./bin/slopbrick.js`. The bare
workspace `slopbrick` lookup is not authoritative because it can select the
stale packed consumer copy under root `node_modules`. The local binary covered
227/227 files with no failed scan outcomes. Eight concrete dead-code findings
were removed and the rebuilt result is **147 active / 519 total**, with **372**
default-off suppressed findings, AI Slop **16.877047461130914**, Engineering
Hygiene **99.7**, Security **100**, and Repository Health **93.1**. Exit 1 is
the expected `meanSlop` threshold failure (`16.877047461130914 > 15`), not a
runtime failure; the 140 remaining compression-profile findings are retained
as calibration evidence.

The machine receipt is `/tmp/slopbrick-selfscan-clean-20260714/report.json`,
SHA-256
`720d103e6acd40a64da5d5844ecf7c5b1d3b20fed74f185d785ba99a893302bb`.

The focused cleanup tests pass 81/81, the full RAM-bounded SlopBrick gate passes
307 files / 3,540 tests (5 files / 9 tests skipped), and package build,
recursive typecheck, and diff checks pass. This remains diagnostic evidence;
it does not authorize corpus promotion or any remote/release mutation.

## Corpus row-versus-content overlap audit — 2026-07-14

The positive/AI and negative/human JSONL totals are unique `sourceId` rows
(224,903 and 227,479), not deduplicated content files. Source IDs have zero
cross-polarity overlap, but `normalizedPath` values overlap across polarities
4,405 times and `contentSha256` values overlap **3,522** times; unique hash
counts are 190,310 AI and 217,837 human, with 18,691 and 3,135 duplicate hash
keys within the respective pools. This is quarantine-only diagnostic evidence.
It clarifies that zero exact source-keyed path overlap is not zero content
leakage. Overlap/family/rights/label review remains required before admission;
no corpus bytes or manifest state changed.
The recursive-canonical diagnostic receipt is
`/tmp/v103-corpus-overlap-diagnostic-20260714.json`, SHA-256
`afb1550087ff5a2644da4f25d84e082c4666307c1fef6ef5f637527748ac305b`;
`authorityEligible=false` and `persisted=false`.

## Staged beacon and large-output boundary review — 2026-07-14

The shared index already contains the five reviewed beacon/CLI/calibration
boundary files. `git diff --cached --check` passes. The focused current run
passes **2 files / 2 tests**, while `tests/integration/calibration.test.ts` is
intentionally skipped because the real corpus has no admitted records. The
subprocess beacon test proves a hanging opted-in endpoint cannot keep a
completed JSON scan alive; the 900-file test proves JSON output larger than
128 KiB is fully flushed before a policy-failure exit. These bytes remain
staged-but-uncommitted and require explicit retention/splitting during the
owner-authorized clean-boundary review.

## Corpus deficit and acquisition decision audit — 2026-07-14

The independent read-only corpus audit finds no eligible strict 100-AI/100-human
witness in the current sources, and no justification for another public-repo
pull yet. The 12-entry draft source register is entirely pending/quarantine
(SHA-256 `1bed3c36f105413411b52a62b8a8543115da5c11c3c4c25ecc8025dfb0ba5deb`);
317 checkout reviews and 452,382 selected rows remain discovery-only, with
`verified_ai=0` and `verified_human=0`.

HumanEval is integrity-valid at 100/100 pairs but has eight unsafe
cross-polarity groups, at most 92/92 content-distinct pairs, and
`eligiblePairsAfterSourceContract=0` (audit SHA-256
`cdf8be81fcd243cc04259b68e8257ae29aa85c846b46169d0c752d4573320e3a`). EvalPlus
has 164/164 files but zero eligible/promoted files; its generator metadata,
asset rights, scaffold/label, and `.pyc` disclosure issues remain open. The
two strong-authorship discovery records have no per-file evidence or rights
chain. The correct next order is immutable evidence publication, source review,
and deterministic census; only a measured deficit then authorizes the planned
bounded acquisition round. No corpus bytes changed.

## Owner-authorized commit and bounded corpus resolution — 2026-07-14

The owner authorized the local commit and corpus work only; no remote action
was authorized. Commit `8de6cfb712620e5fd554f73e677ac1a9d6cb7947` contains the
754 intended paths (`+67,843/-4,736`), while unrelated untracked `TODO.md`,
`.astro/`, and `src/` remain outside the commit. Post-commit recursive
typecheck/build and Core contract/codegen freshness pass; Core is 23 files / 185
tests and the SlopBrick package build passes with only known non-fatal Zod
declaration warnings. No push, tag, release, npm publication, deployment, or
remote mutation occurred.

The bounded Mendeley Data acquisition is pinned to DOI
`10.17632/kjh95n54f8.1`, archive size 2,251,807 bytes, SHA-256
`c6cb156a8fa627c9228b7798ea7d25be9327a4d1f72f40b16ddae3e6d807e0c4`. Static
inspection found 10,000 rows (5,000 AI / 5,000 Human), four languages, 1,780
prompt-summary rows, three shared problem IDs, and zero exact cross-polarity
code hashes. Candidate code was not executed. Audit and proposal receipts are
persisted under `/Users/cheng/corpus-expansion/v10.3/review/` with hashes
`dee8a555f886be193718ac77333d8ef3fd0ad853e5e26b80bf6fc19cad68c655` and
`7cf431b353569cef62d578fab4fce5c7c94ecffa6c0e055e239a3b51cbf85414`.

This acquisition remains quarantine-only: it lacks row-level generator and
rights evidence, immutable projection, lineage/overlap/privacy ledgers, and
independent review. The source register is unchanged; authoritative counts are
still `verified_ai=0`, `verified_human=0`, and `eligible_gold=0`, with no
manifest/selection/run/calibration output. The copied genesis and overlap
diagnostics remain non-authoritative. The Mendeley projection is now persisted;
next is generator/rights/lineage evidence recovery, then an acquisition-bound
register delta and deterministic census. No more repository pulls remain in
this bounded round.

## Quarantine projection materialization — 2026-07-14

The Mendeley CSV is materialized as a static quarantine projection at
`/Users/cheng/corpus-expansion/v10.3/sources/benchmarks/humanvsai-code-dataset/projection-v1/`.
It contains 10,000 regular files and a canonical JSONL manifest with exact
UTF-8 `Sample_Code` field bytes and no added newline. Code bytes total
6,195,562; the manifest SHA-256 is
`588afb3fe94fdde5958ee4aeac9a5ce3b0680cff61d329ec91998819206c6eab` and the
path/hash/size tree SHA-256 is
`261dc463129e50be4ed7e573b83d67b168828afc03ad4c37a7b35f9a0ace6452`.
The projection receipt self-hash is
`80d9ad37ee5c89cba806482280646c6332087b0b2e82150a1af2e59924e64abf`.

A streaming verification reread every manifest row and projected file,
matching all hashes and byte counts with no orphan or duplicate path. The
projection preserves the AI-positive/human-negative mapping and stable source
row IDs but remains quarantine-only: generator/rights/lineage/quality/privacy
evidence and independent review are still missing, so authoritative counts stay
`verified_ai=0`, `verified_human=0`, `eligible_gold=0` and no manifest,
selection, run, or calibration output exists.

## Second bounded source: OSS-forge HumanVsAICode — 2026-07-14

The final second source in the bounded round is Hugging Face
`OSS-forge/HumanVsAICode`, pinned to revision
`03cac5dd2096c8901cb7a74e2ecbd26624ada497` and declared `cc-by-sa-4.0`. Its
Python and Java payloads total 1,156,626,118 bytes and match LFS SHA-256
`f1347d75e05bdd4ddf4a7538ded47c3499027b014c2cd27898c0ef4735a5308d` and
`fe845d53204f4a8fe1e04d7a826163a253e2404f80171845c482c6005e384c20`.
With Mendeley, the round totals 1,158,877,925 bytes; no third source is being
pulled.

The static audit
`/Users/cheng/corpus-expansion/v10.3/review/ossforge-humanvsaicode-audit-2026-07-14.json`
(file SHA-256
`21848613f87f5d4b1b596dc3bb61a868bf5abdc44c64793a16be0f516a5caa0a`, embedded
self-hash `82241981700f836c2cf10921e1630a954c3e333bb5166af149f349bf9611ee65`)
parsed 507,045 rows, with 38,683 blank DeepSeek fields, 3,209 blank Qwen
fields, and exact human/AI hash overlaps of 2/20/38 by model. The source has
no row-level generation timestamp, prompt/output hash ledger, exact model
revision, upstream repository path/commit, or per-record license chain; its
human side is a lossy CodeSearchNet/HMCorp reconstruction. It remains
quarantine-only, with proposal self-hash
`e76a14dea3691a5b301dab1db20f1e3f1b222e492f0e3c987d294f453d0faf39`, unchanged
source register, and authoritative counts `verified_ai=0`, `verified_human=0`,
`eligible_gold=0`. The blocker is provenance/rights/lineage, not volume.

## Bounded acquisition-round diagnostic receipt — 2026-07-14

The cumulative receipt is
`/Users/cheng/corpus-expansion/v10.3/review/acquisition-round-diagnostic-2026-07-14.json`
(file SHA-256
`ed6865550f8750594b62f22613498be73bc8c5b65ee2e339e46424db13d35ac9`, embedded
self-hash `f711406daa4819bf9f92d685b7bbe769a901c723354bbee7daa6e0aecf916d7b`).
It proves two sources and 1,158,877,925 total payload bytes, within the
two-source/5-GiB bound. The source register is unchanged at SHA-256
`1bed3c36f105413411b52a62b8a8543115da5c11c3c4c25ecc8025dfb0ba5deb`; no
register delta, manifest, selection, run, or calibration output exists. Both
sources remain static-only and non-promotable, with authoritative counts
`verified_ai=0`, `verified_human=0`, and `eligible_gold=0`. No third download is
part of this round.

## Hardened clean artifact and recursive gate rebind — 2026-07-16

The final local package boundary is bound to commit
`a7d3e0c18dd8b10a4f70094ad2b3b634c92ae7ab`. A disposable clean checkout
rebuilt and packed `slopbrick@0.44.0`; the prepack guard passed and the artifact
is **2,464,850 bytes** with SHA-256
`f8ec4417e0fd38e15d6ffbca852b6f0c44ad5bcb413f6a36ab976f8902a06ef7`. The
builder was clean before and after packing (`dirty=false`, zero status entries,
status SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`).

The same external tarball passes the packed-consumer contract **9/9** on Node
22.22.3 and **9/9** on Node 24.15.0, including the paired-receipt assertion.
The two receipts are preserved in the ignored
`.superpowers/sdd/task-6-receipts/` directory. The clean checkout also passes
recursive typecheck and the serial build → test gate: Core **33/241**, Website
**11/47**, Engine **5/59**, and SlopBrick **333/3,749**, with the expected
**5 skipped files / 9 skipped tests**. The only build notices are the known
non-fatal Zod declaration warnings.

This closes the local commit-bound package/workspace prerequisite only. It does
not resolve missing v10.3 static-ledger or witness authority, admit corpus
units, authorize calibration, create a release/tag, publish to npm, deploy, or
mutate remote state.

## Package self-scan diagnostic — 2026-07-16

The package-local CLI completed a direct no-telemetry JSON scan of the current
workspace with **248/248** files analyzed and zero parse, timeout, crash, or
internal failures. The report is valid and complete. Its policy gate failed
only `meanSlop` (**17.9540 > 15**); `p90Slop=1` and `peakScore=8.2` passed.
The output is preserved at
`/private/tmp/slopbrick-selfscan-current-direct.json` with SHA-256
`b18340d58362e8c9890df8a04bf4ca6491b00cf6c75ad6d6c0428ce0d58f71f4`.

This confirms the scanner is operational and the result is actionable, while
also showing a UX distinction that must remain explicit: `failed=0` is the
file-scan failure count, whereas `failedThresholds=["meanSlop"]` is the
policy outcome. The stale baseline was ignored after a config-hash mismatch;
no baseline was regenerated and no repository, corpus, authority, release, or
remote state changed.

## Exact finite-sample KS engine diagnostic — 2026-07-16

`@usebrick/engine` now selects a bounded exact pooled-label KS distribution
when integer sample sizes satisfy `n + m <= 40`; the existing asymptotic
formula remains the large-sample fallback. The TDD red proof showed the old
`0.329104789...` approximation for `n=m=5, D=0.6`; the green implementation
returns the exact `0.357142857...`. Engine passes **5 files / 60 tests** and
typecheck, and SlopBrick's KS contract passes **21/21**. Cross-checks against
independent SciPy exact results agree for balanced and unequal sample sizes.

No SlopBrick rule, threshold, corpus, authority, release, or remote state was
changed. The package artifact must be rebuilt and rebound after this code
slice is committed.

## Exact-KS clean release-boundary replay — 2026-07-16

The clean detached replay of `e29c203baca5b806b3bf7b5708ec913cdd6a150c`
passed build → typecheck → serial test: Core **33/241**, Website **11/47**,
Engine **5/60**, and SlopBrick **333/3,749**, with 5 skipped files / 9 skipped
tests. The prepack guard passed. The resulting `slopbrick-0.44.0.tgz` is
**2,465,764 bytes** with SHA-256
`83ed688db8199193e1df9b85f488a60fb455d6f15a548547a5c726470c2969b0`.

The builder was clean before and after packing (`dirty=false`, zero status
entries, status SHA
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`). The
same external tarball passed the 9-test packed-consumer contract on Node
22.22.3 and Node 24.15.0; the receipt files are the ignored pair
`task-6-packed-consumer-node-v22-22-3.json` and
`task-6-packed-consumer-node-v24-15-0.json` (SHA-256
`8ec8d20934429306c689a817e2a9be073097f3b26d9b3cddf51ac7203ec00b1c` and
`a2ccd095e72c32e2d2b3aa28e1cf904238ee22de62fd0d57ce4656c1e70fc894`).

This is the final local package/workspace prerequisite for the exact-KS code
commit. It is not release authorization. Census remains blocked by absent
static-ledger and witness authority; no corpus or remote state changed.

## Policy-gate-first-line UX verification — 2026-07-16

`packages/slopbrick/src/report/pretty.ts` now includes the policy decision in
the first human verdict line. The regression test in
`tests/report/v0.14.5i-ux.test.ts` was run red before the change and green
after it. Focused evidence: **60/60** UX tests and **25/25** renderer/CLI/score
contract tests; package typecheck and build pass.

The rebuilt package-local command was run as:

```text
node ./bin/slopbrick.js scan --workspace . --threads 1 --no-telemetry
```

It completed **248/248** selected files with zero parse, timeout, crash, or
internal failures and exited 1 only for the configured policy result. The
first verdict line is now:

```text
Repo's AI-slop score is low (18.0/100) with 170 active issues. Policy gate fails (AI Slop Score 18.0 > 15).
```

The underlying result is unchanged: `meanSlop=17.9540 > 15`, while `p90Slop=1`
and `peakScore=8.2` pass. A restricted serial full-suite replay passed
329/338 files and 3,743/3,759 tests; the seven failures were sandbox/host
boundary cases. The four affected files were rerun with host-capable
permissions and passed **169/169**. The current exact-KS artifact remains
bound to commit `e29c203b` until this UX change is committed and the clean
package/consumer receipts are rebound.

## UX commit-bound package and consumer rebind — 2026-07-16

The renderer change is committed at
`d5e1e4e3790fa1375de9fcd0e9e25a95f7b9054b`. From a disposable clean checkout,
build and recursive typecheck pass; Core **33/241**, Website **11/47**, and
Engine **5/60** tests pass. The restricted SlopBrick run reports **329/338**
files and **3,743/3,759** tests, with only the previously observed localhost,
setuid, and consumer-subprocess boundary cases failing. Host-capable reruns of
all four affected files pass **169/169**.

The clean prepack artifact is `slopbrick-0.44.0.tgz`, **2,466,120 bytes**,
SHA-256
`bfa3e9b104590e7af3ffa0ac15bf126f8f7eb5192bea5e94f99076d1f7bd8510`. Builder
status is empty and stable (`dirty=false`, zero entries, status SHA
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`). The
external tarball passes **9/9** under Node **22.22.3** and **9/9** under Node
**24.15.0**, with paired receipts whose SHA-256 values are
`52fe85ee52644c070e631777175e8e322cb1284479da976ce62eb6ddb9ed43f4` and
`d8910739e8d795389bfadcdc0f9ff6043a6d5bf8469f3aaeb85540f04b740b10`.

No release/tag/publish/deploy or corpus/authority state changed. v10.3 census
remains `ready=false`, `authorityEligible=false` because static-ledger and
witness authority trees are still absent.

## Host-capable full SlopBrick suite — 2026-07-17

The clean `d5e1e4e37` checkout replayed the complete SlopBrick suite with one
worker and host-capable permissions. Result: **333 passed test files**, **5
skipped files**, **3,750 passed tests**, and **9 skipped tests**. A temporary
root dependency symlink was used for source-side zod resolution and removed
afterward; the clean checkout again has zero status entries. This is the final
local test-gate evidence for the policy-verdict UX commit; no package,
corpus, authority, or remote state changed.

## Website browser/UX evidence — 2026-07-17

Real-browser review of the local Astro site found and corrected two issues:
the generated `v10.1 historical` label was duplicated when callers added
`Historical`, and the desktop/mobile hero accent could overflow at 320px
because a later CSS rule forced `white-space: nowrap`. The generated label is
now `v10.1`; display copy supplies the historical qualifier, interpolated
counts have explicit spaces, and the accent is constrained to the title column
and allowed to wrap. The homepage browser snapshot confirms the corrected
strings and no longer matches `Historical Historical`, `covers103`, or
`119 rules,27`.

The final website gates are: Playwright **17/17**; Astro typecheck **47 files,
0 errors/warnings/hints**; unit **11 files / 47 tests**; and static build **4
routes**. The added mobile regression checks the 320px title/accent geometry;
the existing axe, keyboard, route, terminal, and mobile-overflow checks remain
green. This is local website evidence only. SlopBrick's clean package remains
bound to `d5e1e4e37`; the website/docs changes are committed as `157a139ea`.
Release, publish, deploy, corpus admission, and remote state are unchanged.

## Fresh v10.3 census recheck — 2026-07-17

The read-only `census:preview` command was rerun against the current corpus
with `admission-context-v1` and invocation intent
`ea082ec3c7de8c02c89031cfc93afcbc42fc9b96a1481dce4bb94aa6dbe3cfbf`. Its
canonical JSON is byte-identical to the prior receipt, SHA-256
`95ed5b57a4ccab0bd3eb21aef150088a34632b60f843f0200c204dc65e50ffda`.
Result: `ok=true`, `ready=false`, `authorityEligible=false`, 329 registered
and reviewed sources, 452,382 additive/quarantine units, and 0 eligible.
Blockers are unchanged: `static_authority_unavailable` and
`witness_authority_unavailable`.

Filesystem inspection confirms the expected `authority/`,
`review/admission/sources/`, `review/admission/static-ledgers/`, and
`review/admission/witnesses/` trees are absent. The existing acquisition and
tool-authority trees cannot be promoted as admission authority. This recheck
was read-only; no corpus, label, manifest, release, or remote state changed.

## Focused authority-boundary replay — 2026-07-17

Serial Vitest replay of the six local handoff suites passed **6 files / 56
tests**: `v103-admission-static-ledger-stream`, `v103-admission-static-ledgers`,
`v103-admission-authority-materializer`,
`v103-admission-authority-cli`,
`v103-admission-authority-rebuild-adapter`, and
`v103-admission-allocation-preview`. These tests cover explicit graph
selectors, byte-backed stream/overlap joins, partition and semantic hashes,
publication recovery, and provenance conservation. They are bounded fixture
proof, not live authority; the current census remains 0 eligible and no
corpus, label, manifest, release, or remote state changed.

## Task 9A Core dependency-receipt evidence — 2026-07-17

Commit `ef83ef3ec` adds the four Task 9A schemas, generated peers, pure Core
validators/hash helpers, schema-index entries, codegen support, and contract
fixtures. The exact contract test was first run red because the new module was
absent; after implementation it passed **16/16**. The post-commit gates are:

- codegen freshness: pass;
- schema fixture validation: pass;
- Core typecheck: pass;
- Core test: **34 files / 257 tests passed**;
- Core build: pass.

The tests include valid, schema-invalid, semantic-invalid, field-mutation, and
cross-kind cases, plus both run lifecycle kinds and Node 22/24 packed-runtime
variants. This receipt-contract slice is independent of corpus authority and
does not create a manifest, alter corpus bytes or labels, bump SlopBrick, or
authorize release/publish/deploy. Task 9B is the next code slice, but it cannot
start its live manifest path until the owner authority bundle makes the census
ready.

## Task 9B Core manifest-contract foundation — 2026-07-17

The first Task 9B contract slice is green locally. It adds the twelve
prerequisite/publication and manifest lifecycle schemas and generated peers,
the closed artifact mapping and self-hash validators, fixed tuple codegen,
schema-index and fixture-harness wiring, and the optional v10.3.2
`admissionBinding`/per-file identity contract. The focused contract test is
**25/25**; the Core corpus/schema contract set is **82/82**; schema validation
and Core typecheck pass.

This receipt is contract-foundation evidence only. It does not assert that the
v10.3 corpus is ready, does not publish a prerequisite bundle or manifest, and
does not authorize a package bump, release, npm publish, website deployment,
or remote mutation. The live census remains fail-closed until the missing
static-ledger and witness authority bundle is supplied.
