# v0.45.0 Recovery Execution Evidence

**Recorded:** 2026-07-09
**Scope:** recovery tasks 0–5 plus explicitly recorded v10.3 implementation
slices; this is not release evidence.

This report records the fresh verification available after the scanner recovery
work. It does not promote calibration results, close the broader continuation
plan, or authorize a version tag, publish, or deployment.

## Verified implementation tasks

| Task | Evidence | Result |
| --- | --- | --- |
| 0 — source/ESM/CommonJS parity | Commit `e8b7a2d4f`; source help, `tsc`, `tsup`, package-worker and artifact tests | APPROVE |
| 1 — config/monorepo discovery | Commit `83e1d2894`; 14 focused tests and `tsc` | APPROVE |
| 2 — empty/partial outcomes | Commits `160e149bc`, `9dbb02afe`, `e388b4a0a`; final review approved | APPROVE |
| 3 — authoritative CI outcome | Commits `f94805af8`, `f5d87528c`, `0da9150f5`; 26 focused tests and `tsc` | APPROVE |
| 4 — Dart rule contracts | Commit `158ee8011`; `vitest run tests/engine/rule-hints.test.ts tests/engine/signal-strength-guardrails.test.ts tests/rules/dart` — 3 files, 17 tests passed; `tsc --noEmit` passed; independent review approved | APPROVE |

The four Dart rules remain `defaultOff: true`, `DORMANT`, and zero-valued in
signal metadata. No calibration evidence is claimed for them.

## Fresh self-scan evidence

Commands were run against the built CLI with one worker thread, JSON output,
telemetry disabled, and color disabled. Both scans returned exit code **1**
because the current `meanSlop` threshold failed. Both reports were complete:
no files failed or were skipped.

| Workspace | Status | Requested | Analyzed | Failed | Skipped | AI slop | Hygiene | Security | Repository health | Failed thresholds |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `packages/slopbrick/src` | complete | 328 | 328 | 0 | 0 | 25 | 99.9006 | 100 | 75 | `meanSlop` |
| repository root | complete | 388 | 388 | 0 | 0 | 25 | 99.7989 | 83.3333 | 75 | `meanSlop` |

These scans establish discovery and completion accounting, not rule
correctness or release quality. Findings still require the stratified manual
correctness/usefulness review specified by Gate 2, and the failing threshold is
intentionally recorded rather than hidden.

## Remaining gates

Gate 0 (scope/version decision), the score and broader CLI/MCP/core/engine
contracts, v10.3 calibration/provenance, full-suite environment repair, site
and operations hardening, and release-candidate/publication gates remain open.
The historical v10.2a artifacts and current model files remain exploratory and
must not drive signal changes or a release.

## Post-reconciliation verification (2026-07-09)

Follow-up commit `ea9c7c1d1` corrected the Dart signal metadata `aiSpecific`
drift; its review is APPROVE. The follow-up guardrail run covered four files
and 19 tests, all passing.

A full Vitest rerun was then started and stopped after exposing additional
non-green gates:

- structural-clone performance exceeded its 2,000 ms budget (2,748.9 ms)
- calibration-db corpus fixture scanned 0 files where more than 50 were expected
- category-separation exceeded the 60-second timeout
- parser-kotlin setup remained blocked by the local native-addon/install environment

Follow-up verification after the pinned dependency repair changed the status:

- structural-clone now completes within budget (1,971 ms in the combined run;
  533 ms isolated)
- category-separation passes with 1.93 points of best-bucket separation
- parser-kotlin passes after compiling the Node 24 native addon (5/5 tests)
- the DB calibration fixture still scans 0 files and remains an open corpus
  provenance/availability blocker
- stricter core schema validation required updating an invalid SlopBrick test
  fingerprint fixture; the structure test now passes (37/37)

The DB corpus blocker and remaining Gate 0 cleanup/calibration/release gates
still prevent a full-suite or release claim.

## Verification update — 2026-07-10

Using the repository-pinned `pnpm@9.15.0`, recursive typecheck passed for
core, engine, SlopBrick, and website. The SlopBrick build emits the v10.3 CJS
and ESM calibration control-plane entries. Its serial full suite passed:
247 test files and 2,418 tests; five fixture/integration files and nine tests
were intentionally skipped.

The default parallel recursive test invocation remains non-green under local
resource contention: worker-pool handshake timing assertions and the
structural-clone 2-second performance budget failed only in that concurrent
run, while both focused tests and the serial full suite passed. This is not a
release pass for the default parallel CI configuration; it is evidence that
the remaining issue is test-execution resource sensitivity rather than a
reproduced functional failure.

## v10.3 immutable release materialization — Task 1 (2026-07-11)

Release-materialization Task 1 is implemented in commit `ca72d0b15`
(`feat(core): model immutable calibration release assets`). This opens the
single private Core `0.3.0` calibration-schema tranche and adds the optional
`release_archive` manifest/check-out binding without changing
`STRUCTURE_SCHEMA_VERSION` (`5`) or the existing Git-tree fixture bytes.

TDD and review evidence:

- pre-change focused baseline: 21/21 passed;
- test-only RED: six expected missing-feature failures, followed by a separate
  RED that caught the absent 4,096-character runtime boundary;
- specification review found two identity-test gaps; mutation runs proved the
  corrected tests fail when repository ID or commit is omitted from checkout
  identity, when repositories are keyed by commit, or when duplicate
  repository-ID rejection is removed;
- code-quality review found three contract defects; a second test-only RED
  produced eight failures for non-string coercion, `.` root segments, and the
  inconsistent 512-character source-ID ceiling;
- final specification and code-quality rereviews both returned APPROVE with no
  remaining Critical, Important, or Minor findings.

Fresh pre-commit verification:

- exact Core codegen and generated-code freshness gate: pass;
- exact Core schema fixture validation: pass;
- focused Core contracts: 67/67 pass;
- full Core suite: 116/116 pass;
- Core typecheck and exact build (including prebuild/codegen/contracts): pass;
- SlopBrick typecheck: pass;
- tracked and staged diff checks: pass;
- existing v10.3.0 Git fixture SHA-256 remains
  `89d9eaf198702f2e28bc6270efedc0d1a08d73988ab4c61c79ff0704a7724225`;
- frozen admission/release plan hashes remain
  `233ede7262dd408318230f32ab4cc5d2103caf8b5662ae4fb228088531f70e75`
  and `0115a2c73dcfd0a086c70b1e3fe6519558bf7c34ff4942d97d52c3dfa92c585d`
  respectively.

The commit hook's staged self-scan completed 5/5 files with no parse failures:
AI Slop 7.2 (threshold 15 passed), engineering hygiene 100, security 100, and
repository health 97.1. It reported local `.slopbrick/` persistence, but the
post-commit status showed no tracked or untracked `.slopbrick/` change. This
small staged scan is diagnostic Task 1 evidence, not a whole-project self-scan,
corpus-quality, calibration, or release pass.

No external corpus byte, source register, admission decision, selection, run,
signal/verdict, remote, tag, publication, or deployment changed in Task 1.

## v10.3 immutable release materialization — Task 2 (2026-07-11)

Release-materialization Task 2 is implemented in commit `704abef0e`
(`feat(slopbrick): bind release assets into corpus selection`). File identity
now conditionally binds the normalized release-archive kind, byte digest,
root prefix, and extraction policy while preserving the exact pre-existing Git
payload. Selection records copy only the local checkout binding
`{kind, assetSha256, extractionPolicy}`; URLs, acquisition metadata, archive
size/format, checkout/cache paths, and host details are not serialized there.

TDD, review, and compatibility evidence:

- pre-change Git selection and run-manifest baselines passed;
- test-only RED produced four intended feature failures for missing release
  identity, selection binding, mixed-manifest propagation, and ledger drift;
- the frozen Git file ID remains
  `sbf_16358504671821d0e9643d831764052abd7cfadf3d1cc989074612cd94d64b58`;
- frozen Git-only JSONL, canonical-ledger, and pretty-ledger SHA-256 values
  remain `caefdfa7467d74e09193875d6f29ed213ac2958069a1f3dc22ada7c4b12ee52f`,
  `b01883c955f357fb04b6a87ed104bd4ed36c86d297d48a6d4d34a81451f97a18`,
  and `92c2407b9b2d61f844661b80b5e86c0765d95a4f78f53ea549daaf9edb18e430`;
- identity mutations cover digest, policy, root prefix, commit, repository,
  family, and normalized path; a URL-only change preserves `fileId` while
  changing the manifest hash;
- mixed Git/release order is deterministic, removed/changed selection bindings
  fail ledger verification, and a release binding under method `v10.3.0` is
  rejected;
- independent specification and code-quality reviewers both returned APPROVE
  with no findings;
- the exact focused boundary suite passed 16/16, including the spawned CLI
  refusal cases, and SlopBrick typecheck passed;
- staged diff checks passed.

The commit hook scanned the two changed production files with no active issues
or parse failures: AI Slop 0.0 (threshold 15 passed), engineering hygiene 100,
security 100, and repository health 100. This is a scoped staged diagnostic,
not a whole-project self-scan or corpus/calibration/release verdict.

No external corpus byte, source register, admission decision, real selection,
run, signal/verdict, remote, tag, publication, or deployment changed in Task 2.
