---
// StructureDemo — renders a real `health.json` (the artifact the
// tool produces) as if it's being written to disk, one field at a time.
// The animation is driven by the parent's scroll position via
// `animation-timeline: view()`. Each field fades in as the user
// scrolls past it. No JS required.
//
// This is the only section that has narrative tension: the page is
// showing you what the tool builds.
---

<section class="structure-demo" aria-label="Example health.json output">
  <div class="structure-demo__panel">
    <div class="structure-demo__bar">
      <span class="structure-demo__dot" aria-hidden="true"></span>
      <span class="structure-demo__dot" aria-hidden="true"></span>
      <span class="structure-demo__dot" aria-hidden="true"></span>
      <span class="structure-demo__path">/your-repo/.slopbrick/health.json</span>
    </div>
    <pre class="structure-demo__body"><code><span class="sd-key">&#123;</span><span class="sd-line" style="--i: 0">  <span class="sd-key">"version"</span>: <span class="sd-str">"5"</span>,</span><span class="sd-line" style="--i: 1">  <span class="sd-key">"generatedAt"</span>: <span class="sd-str">"2026-07-06T15:33:18.987Z"</span>,</span><span class="sd-line" style="--i: 2">  <span class="sd-key">"workspace"</span>: <span class="sd-str">"/your-repo"</span>,</span><span class="sd-line" style="--i: 3">  <span class="sd-key">"aiSlopScore"</span>: <span class="sd-num">25</span>,</span><span class="sd-line" style="--i: 4">  <span class="sd-key">"engineeringHygiene"</span>: <span class="sd-num">100</span>,</span><span class="sd-line" style="--i: 5">  <span class="sd-key">"security"</span>: <span class="sd-num">100</span>,</span><span class="sd-line" style="--i: 6">  <span class="sd-key">"repositoryHealth"</span>: <span class="sd-num">57</span>,</span><span class="sd-line" style="--i: 7">  <span class="sd-key">"issueCounts"</span>: &#123;</span><span class="sd-line" style="--i: 8; --indent: 1">    <span class="sd-key">"high"</span>: <span class="sd-num">3</span>,</span><span class="sd-line" style="--i: 9; --indent: 1">    <span class="sd-key">"medium"</span>: <span class="sd-num">12</span>,</span><span class="sd-line" style="--i: 10; --indent: 1">    <span class="sd-key">"low"</span>: <span class="sd-num">47</span></span><span class="sd-line" style="--i: 11">  &#125;</span><span class="sd-key">&#125;</span></code></pre>
  </div>
</section>
