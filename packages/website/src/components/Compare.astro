---
// Compare — slopbrick vs "everything else" (pre-2026 status quo).
// Side-by-side courses of bricks separated by a centered VS divider.
// The yay bricks are warm-toned (orange check); the nay bricks are
// cold-toned with line-through (struck-through old ways). Each
// section is a "course" in the wall — the masonry aesthetic carries
// the visual metaphor.

import BrickLayout from './BrickLayout.astro';

// "What slopbrick does" — 5 concrete capabilities you can verify
// in the docs and the CLI output.
const yay = [
  'v0.43.0, 103 rules, 22 categories, the 4-score model',
  'Writes .slopbrick/{inventory,constitution,structure,health}.json — every agent reads the same shape',
  'MCP server: Claude Code, Cursor, Copilot all consume the structure',
  'Constitution engine — declare your stack once, drift exits 1',
  'v10-calibrated against 576,750 real files, per-rule precision/recall on disk',
];

// "What the alternative looks like" — the situation slopbrick replaces.
const nay = [
  'Linters that score every commit against the same stale rules',
  'CI gates that ship without a Constitution to enforce',
  'AI agents that retrain on each session — forgetting your patterns',
  'Scan-results dashboards that nobody on the team opens',
  'MCP servers that read but never write the structure files',
];
---

<section class="section compare-section" id="compare">
  <BrickLayout as="header" tone="raised" class="compare-section__head" span={2}>
    <span class="compare-section__label">the difference</span>
    <h2 class="compare-section__title">
      Four scores, one Constitution, every <span class="compare-section__accent">writeable artifact</span>.
    </h2>
    <p class="compare-section__body">
      576,750 files of v10 calibration. 103 rules across 22 categories. The 4-score
      model (AI Slop Score, Engineering Hygiene, Security, Repository Health). A Constitution engine
      you declare. usebrick.dev writes <code>.slopbrick/</code> artifacts that agents read
      instead of re-parsing the AST.
    </p>
  </BrickLayout>

  <div class="compare">
    <div class="compare__col compare__col--yay">
      <div class="compare__heading compare__heading--yay">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <polyline points="20 6 9 17 4 12" />
        </svg>
        <span>usebrick</span>
      </div>
      <div class="compare__items">
        {yay.map((line) => (
          <div class="compare__item">
            <span class="compare__check">
              <svg viewBox="0 0 24 24" width="100%" height="100%" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </span>
            <span class="compare__item-text">{line}</span>
          </div>
        ))}
      </div>
    </div>

    <div class="compare__divider" aria-hidden="true">
      <span>vs</span>
    </div>

    <div class="compare__col compare__col--nay">
      <div class="compare__heading compare__heading--nay">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <line x1="18" y1="6" x2="6" y2="18" />
          <line x1="6" y1="6" x2="18" y2="18" />
        </svg>
        <span>everything else</span>
      </div>
      <div class="compare__items">
        {nay.map((line) => (
          <div class="compare__item compare__item--struck">
            <span class="compare__cross">
              <svg viewBox="0 0 24 24" width="100%" height="100%" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <line x1="18" y1="6" x2="6" y2="18" />
                <line x1="6" y1="6" x2="18" y2="18" />
              </svg>
            </span>
            <span class="compare__item-text">{line}</span>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
