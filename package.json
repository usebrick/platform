{
  "name": "usebrick-platform",
  "version": "0.0.0",
  "private": true,
  "description": "UseBrick is the repository-owned quality, coherence, and verification layer for agent-built software; SlopBrick is the shipped AI-slop scanner and CLI.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "security:audit": "node scripts/audit-npm-bulk.mjs --audit-level high",
    "plans:validate": "node scripts/validate-execution-docs.mjs",
    "positioning:validate": "node --test scripts/validate-positioning-docs.test.mjs",
    "scan": "node packages/slopbrick/bin/slopbrick.js scan",
    "bench:scan": "pnpm --filter slopbrick bench:scan",
    "changeset": "changeset",
    "version-packages": "changeset version && node scripts/sync-website-version.js"
  },
  "engines": {
    "node": "^22.0.0 || ^24.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "pnpm": {
    "overrides": {
      "vitest>vite": "6.4.3",
      "vite-node>vite": "6.4.3",
      "yaml-language-server>yaml": "2.8.3",
      "tsup>esbuild": "0.28.1"
    }
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  }
}
