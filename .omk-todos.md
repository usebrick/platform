# v0.42.0 work tracker

## Pre-flight
- [ ] Inspect the 3 Sprint-3b stashes (config.ts CompositeRule<Context> stashes 0/1/2 + .gitignore stash 3)
- [ ] Decide: carry stashes forward as working diff, or replace with a clean write from the plan

## 3b.1 — CompositeRule type
- [ ] src/types/config.ts: CompositeRule<Context> parallel to Rule<Context>
- [ ] ruleIds: string[]; minMatch: number

## 3b.2 — Clustering engine (5-step algorithm from §3b plan)
- [ ] src/engine/cluster.ts (new): in-tree single-linkage with stability pruning
  - STEP 1: rule pre-filter by support (adaptive minSupport)
  - STEP 2: pairwise NPMI + Fisher's exact
  - STEP 3: single-linkage clustering (HDBSCAN-style, no k to pick)
  - STEP 4: per-cluster calibration (F1 × recall/FP ≥ 1.5×)
  - STEP 5: emit synthetic composite entries (composites.json)
- [ ] Pure functions only — no I/O

## 3b.3 — Cluster emitter
- [ ] Write composites.json next to signal-strength.json
- [ ] Provenance field per entry

## 3b.4 — Loader + registry
- [ ] Read composites.json (auto) + slopbrick.config.mjs#compositeRules (user)
- [ ] Merge into RuleRegistry
- [ ] Delete dead 2-arg register overload (F6)

## 3b.5 — Engine wiring
- [ ] src/engine/worker.ts: composites run after per-file RuleRegistry
- [ ] Emit synthetic issue per composite that matches minMatch

## 3b.6 — CLI surface
- [ ] src/cli/commands/composite.ts (new)
  - `slopbrick composite discover` — run clusterer, print candidates
  - `slopbrick composite enable <id>` — opt a specific composite in
- [ ] Both opt-in only (no auto-emission)

## 3b.7 — Tests
- [ ] tests/engine/cluster.test.ts — pure-function tests for the 5-step pipeline
- [ ] tests/rules/composite.test.ts — registry integration
- [ ] Calibration gate test (one cluster passes, one fails recall/FP ≥ 1.5×)

## 3b.8 — Hand-curated fallback
- [ ] src/rules/builtins.ts: seed composites.json with one hand-curated composite
- [ ] Tagged "hand-curated-by-brief", defaultOff: true
- [ ] Removed once auto-discovered clusters exist

## 3a.1 — Marker-block constant
- [ ] src/snippet/generators.ts: <!-- slopbrick:begin:v3 --> / <!-- slopbrick:end:v3 -->
- [ ] Emitter wraps output in markers

## 3a.2 — Safe rewriter
- [ ] src/snippet/render.ts: safeRewrite(path, newContent)
- [ ] Both markers → replace inner; neither → fail closed; one → warn + skip

## 3a.3 — Hook fire site
- [ ] src/cli/report/persistRun.ts: after health.json + inventory.json, call refreshSnippets
- [ ] Sequence atomically (rewrite only after both writes succeed)

## 3a.4 — Opt-in flag
- [ ] src/cli/types.ts: autoRefreshSnippets: boolean (default false)
- [ ] slopbrick.config.mjs#autoRefreshSnippets OR --refresh-snippets

## 3a.5 — Tests
- [ ] tests/snippet/refresh.test.ts
  - Full rewrite of marker block
  - User-edited content outside block preserved
  - Conflicting markers handled

## 3c.1 — --diff <file> mode
- [ ] src/cli/pr.ts: accept unified diff path
- [ ] Score only added lines (--diff ref semantics from scan)
- [ ] Output via formatPrMarkdown for Action consumption

## 3c.2 — CLI dispatch
- [ ] src/cli/commands/pr.ts: --diff <file> option

## 3c.3 — GitHub Action
- [ ] .github/workflows/slopbrick-review.yml (new)
- [ ] On pull_request opened
- [ ] permissions: pull-requests: write, actions: read
- [ ] Posts single summary comment via gh api

## 3c.4 — Tests
- [ ] tests/cli/pr-diff.test.ts
  - --diff <file> parsing
  - added-line-only scoring
  - markdown output for sample diff

## Pre-release for v0.42.0
- [ ] packages/slopbrick/package.json: 0.41.0 → 0.42.0
- [ ] packages/slopbrick/CHANGELOG.md ## [0.42.0] header
- [ ] .changeset/v0.42.0-*.md
- [ ] packages/website/src/data/version.json
- [ ] pnpm -r typecheck (full)
- [ ] scoped vitest
- [ ] pnpm -r build
- [ ] self-scan
- [ ] commit + push
- [ ] tag v0.42.0
- [ ] gh release create
- [ ] approve deployment
- [ ] verify npm view
