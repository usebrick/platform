# v0.45 calibration continuation

The authoritative execution plan is the package-level
[v0.45.0 continuation plan](../../packages/slopbrick/docs/calibration/v0.45.0-continuation-plan.md).
This note records only the current ordering; green checks below are evidence
snapshots, not authorization to admit corpus data or release artifacts.

## Current state pointer — 2026-07-16

Use the package-level continuation plan and its latest follow-up sections as
the sole current evidence. The latest RAM-capped SlopBrick gate is **329 test
files / 3,716 tests passed** (5 skipped files / 9 skipped tests); Core is
**33 files / 240 tests passed**. Recursive typecheck and build pass for Core,
Engine, Website, and SlopBrick, with only the known non-fatal Zod declaration
warnings. The current branch is `codex/v0.45-recovery`; the only unrelated
working-tree paths are `.astro/`, `TODO.md`, and `src/`.

The Task 3B witness-review graph, transactional publisher/recovery path, Core
contracts, CLI boundary, and plan/hash checkpoints are committed locally. No
corpus bytes, labels, manifest, remote, push, tag, release, package
publication, or deployment state changed. The remaining open execution slice
is real static/witness authority materialization; the census is still
diagnostic-only with `verified_ai=0`, `verified_human=0`, zero candidate/eligible
units, and 452,382 quarantined/unrepresented units.

The external review-artifact validator remains diagnostic-only; it does not
prove source-level admission, manifests, selections, runs, or release
readiness.

Verification is intentionally risk-scaled: one integrated gate owns each
implementation slice. Parser, filesystem, hash, collision, crash-recovery,
symlink, and resource-limit edge cases are automated and do not need repeated
human confirmations. Human review is reserved for external provenance,
authorship, rights/lineage, privacy/quality, and release-authorization claims.
Older review entries are historical evidence, not pending duplicate approvals.

The dependency-security gate has the same risk-scaled shape. The legacy
`pnpm audit --prod --audit-level high` command reaches npm's retired audits
endpoint (HTTP 410), so one repository-owned read-only adapter now posts the
deterministic production dependency payload to npm's current bulk advisory
endpoint. Its helper tests pass **3/3** and the current live result covers
**522** external production packages with **0** high-or-critical advisories.
This is direct dependency-advisory evidence only; it does not advance corpus
authority or release authorization.

## Current verification checkpoint — 2026-07-15

The current checkout was reverified with one risk-scaled gate rather than
parallel duplicate confirmations. The RAM-capped SlopBrick package gate
passed **329 test files / 3,716 tests**, with **5 skipped files / 9 skipped
tests**; Core passed **33 files / 240 tests**. Workspace typecheck passed for
Core, Website, Engine, and SlopBrick;
the Website check reported 0 errors, warnings, and hints. The recursive build
passed for all four packages, with only the existing non-fatal Zod declaration
warnings. `git diff --check` is clean.

The worktree still contains only the unrelated untracked `.astro/`, `TODO.md`,
and `src/` paths. The implementation and documentation checkpoints are
committed locally; no corpus, remote, package, release, publish, or deployment
state changed.

The live v10.3 diagnostic census remains **329/329** registered/reviewed
sources, **452,382** additive units, **0** candidate units, **0** eligible
units, and **452,382** quarantined/unrepresented units. Its blockers are
`static_authority_unavailable` and `witness_authority_unavailable`. This is
the single real blocker; lock-only nonce wording and verification-scope
details remain non-blocking edge-case follow-ups and do not require another
full-corpus confirmation.

The fresh built-CLI `source:census` replay remains byte-stable at **113,469
bytes**, SHA-256
`de0cd1879d14365b919c09bdd21bd5760a6804a0e3626d87abf37b78ad948857`, with
context SHA-256
`9b3f8a4adcbb82ef4d97685be51264a1feb44ebf46584697df93511dad2f3089` and
bundle SHA-256
`b5d8894f485ca58c3dc6d7285bdb46215578fa98003c329419de2098a6f48a60`.
`evidence:verify` returned zero verified and unavailable evidence IDs.

The next execution slice is therefore authority materialization (source
rights/authorship/lineage, static ledgers, overlap resource receipt, and
witness authority), followed by the bounded **100+100** smoke and
**5,000+5,000** canary. No additional public-repository pull is justified
unless that authority-backed allocation reports a measured deficit.

## Current dependency sequence

### Historical execution snapshot — 2026-07-13 (superseded)

- The bounded full SlopBrick suite now passes 285 files (5 skipped), 3,316
  tests (9 skipped) with one worker and a 2 GiB heap cap. Core, Engine, and
  Website tests pass 127/127, 59/59, and 37/37; recursive typechecks pass;
  SlopBrick and Website builds pass. The recursive build remains blocked by
  Core's codegen-freshness guard on the uncommitted generated
  `packages/core/src/generated/calibration-observation.ts` and
  `packages/core/src/generated/health.ts` changes.
- The current same-tarball Task 6 pair passes 9/9 on Node 22.22.3 and 9/9 on
  Node 24.15.0 with strict offline installation. The tarball SHA is
  `61d3b9e9c3a8d8d01b19425431f3dca56d395e30b719461f1c0e34ad9c398846`; both
  receipts agree on dirty commit `81e79b9ec47f46f70575ae650beccd3a278b60e5`,
  179 status entries, and status SHA
  `0142d40f2cca9afa8f713d7e5bf4ed9fc1f9e5e6895f73771fa2764e2574fe6f`.
Those receipts remain bound to the 179-entry snapshot; the current worktree
is 613 status entries with status SHA
  `f5b4404c0a72c4b81766767ad93d06687d35a81b45f0538aeb9473cd72e051df` after
  the score-contract, provenance, CLI regression, workflow, website
  accessibility, v10.3 run-artifact, and current verification slices.
- The latest locally available built-CLI self-scan receipts are complete/valid:
  package 208/208 with 119 active and 274 suppressed; repository root 439/439
  with 212 active and 550 suppressed. Diagnostic usefulness is closed after
  the stratified review and renderer/message corrections; these dirty-byte
  receipts remain non-release context and should be refreshed after a clean
  builder boundary.
- The legacy corpus is centralized under `/Users/cheng/corpus-expansion`:
  224,903 positive/AI and 227,479 negative/human filelist paths have zero exact
  overlap. Baseline trees contain 5,833 and 52,280 immediate regular source
  files; selected lists contain 5,809 and 52,280, while generated `.slop-audit`
  artifacts are quarantined. The receipt is
  `v10.3/review/corpus-centralization-receipt-2026-07-13.json`; labels remain
  quarantine inventory until admission review.
- The read-only Gate 4 census-prep audit (2026-07-13) confirms that these are
  still quarantine inventories: 224,903 declared-AI rows and 227,479
  declared-human rows, 317 repository reviews pending, and all 12
  source-register entries quarantine/pending. The controlled HumanEval audit
  has 100/100 pairs but zero eligible after the source contract; EvalPlus has
  zero eligible files. The truthful reviewed baseline is
  `verified_ai=0`, `verified_human=0`, with no admission/census/witness/ledger/
  manifest/selection/run artifacts. Tasks 1A–3B must establish the offline
  authority before any deficit calculation or public repository acquisition;
  no network or corpus mutation occurred.
- A bounded Task 1A Core contract slice now passes 6/6 focused tests, Core
  typecheck, and schema validation for the five durable admission policy,
  witness, tool-profile, invocation-intent, and tool-receipt contracts. The
  evidence CAS/context brand, authority publication, `evidence:verify` CLI,
  remaining schemas, and independent review remain open.
- The bounded Task 1B acquisition-round Core contract is independently
  approved in `.superpowers/sdd/task-1B-acquisition-round-rereview.md`:
  Core passes 17 files / 163 tests, the focused acquisition/schema run passes
  6/6, and codegen, schema validation, typecheck, and diff-check pass. The
  contract covers one/two-source authorization, receipts, locks, transactions,
  explicit authorization-order joins, distinct tool receipt ID/hash fields,
  child/orchestrator authority projections, strict network observations,
  release caps/redirects, and staged recovery-state bindings. The Core
  `test:contract` freshness failure remains limited to tracked generated
  observation/health drift. This is offline contract evidence only; runtime
  acquisition, filesystem crash/race proof, corpus admission, Gate 4/5, and
  release remain open, with no network or corpus mutation.
- Incremental cache contracts now cover malformed caches, workspace-relative
  paths, unwritable destinations, all-cache-hit messaging, byte-stable health
  persistence, and the fail-closed project-rule policy. Hash-only cached files
  are not hydrated into project findings; a full scan is required for
  project-wide parity. No corpus admission, tag, push, publication, or
  deployment has occurred.
- The bounded v10.3 metrics reducer now computes deterministic denominator-aware
  point estimates only from an explicit eligible-file census and complete rule
  catalog; its focused suite is 11/11 and includes Wilson 95% intervals plus
  deterministic family-cluster/language macro estimates, diagnostic F1, and
  seeded family-cluster bootstrap intervals. Admission-backed report wiring
  remains open. The
  incomplete projection contract now suppresses canonical score aggregates
  and opt-in explanations in JSON/SARIF/MCP while retaining non-gating
diagnostics (44 focused projection tests plus 28 selected partial-output
checks). The lossless calibration-evidence contract carries sanitized,
deterministic per-rule counts through observations (8 SlopBrick files/31
tests plus 3 Core contract tests). Neither slice creates a v10.3 cohort.

### Task 1B temporal/adjudicator authority checkpoint — 2026-07-13

The temporal-attestation and dedicated-adjudication contracts are now wired
through SlopBrick's admission ledger projection. Core rejects orphan or
malformed temporal receipts, maps attestations to explicit sources, binds
dedicated assignment/receipt JSONL plus hashes and IDs, and normalizes
hash-sorted prior IDs to canonical peer order. After the reduction refactor,
evidence is Core 19 files/172 tests and the SlopBrick admission/source-census/
temporal matrix 30/30 with one
worker; the final RAM-bounded serial SlopBrick suite is green at 293 files/
3,406 tests with 5 skipped files and 9 skipped tests; typechecks and schema
validation are green. The result is still
diagnostic and fail-closed (`ready=false`, `eligibleUnits=0`), with no corpus
admission or release state. Reduction/reconciliation must happen before Task
2A: consolidate validators, classify generated artifacts, split historical
evidence, and rerun recursive gates. The current dirty snapshot is 624 status
entries with SHA-256
`8aeaff54a6612c3ddad819bc52e1c0b8a61cc45ad4edd6480741bcc644ba6c0a`; it is not
a clean-boundary receipt.

### Reduction/reconciliation checkpoint — 2026-07-13

The dirty-boundary reduction pass is complete: seven Core admission validators
share one pure primitive module, reducing 48 repeated guards to seven shared
primitives plus 11 specialized wrappers. Core passes 19 files/172 tests and
typecheck; schema validation, SlopBrick typecheck, and diff-check pass. The
artifact classification documents the intentional generated contract tranche,
fixture policy, and historical/research partitions, and contradictory old
Task 1B sections are marked superseded. No files were deleted, moved, or
acquired. The reduction snapshot was 627 status entries with SHA-256
`a0b7502b5dd57367216ba6bfb75c66f4f6b56056b68c5ebe0877bb31b77a0e48`; Task 2A
has since started at a bounded Core contract boundary and its runtime and
recursive gates remain open.

### Task 2A Core overlap-contract checkpoint — 2026-07-14

The independent review in `.superpowers/sdd/task-2-contracts-review-final.md`
approves the first four-contract Core slice: normalizer registry,
overlap-universe row/summary, and frozen overlap-policy schemas with generated
peers, fixtures, semantic validators, exact rational similarity/size helpers,
and canonical stream checks. Review fixes reject contradictory
registered-unassigned labels, locator control bytes, omitted/substituted
normalizer registries, and the wrong heap bound; malformed unknown rows now
fail closed without throwing. Bounded one-worker gates pass: focused 1/6,
full Core 20/178, typecheck, schema validation, and diff-check. SlopBrick
normalizers/reader, disk-bounded overlap, resource receipt,
persistence/recovery, CLI, and real-corpus acceptance remain open. No corpus
or remote release state changed.

### Task 2A external-sort/shard fixture checkpoint — 2026-07-14

The independent review in `.superpowers/sdd/task-2-external-sort-review-final.md`
approves the fixture/diagnostic writer only. Canonical duplicate-preserving
JSONL sorting, contained no-clobber `generation_local` shard paths, lowered
shard/open-file/work budget enforcement, incomplete receipts, and unknown-file
preservation pass in 1 file/6 tests; SlopBrick typecheck and diff-check pass.
This is not the production spill/merge path: incremental verification,
heap/RSS/unit/wall telemetry, Core shard/resource schemas, adjacency/clusters,
checkpoint/recovery, CLI, and 452,382-unit acceptance remain open; no corpus or
remote release state changed.

### Task 2A SlopBrick normalizer/universe fixture checkpoint — 2026-07-14

The independent review in
`.superpowers/sdd/task-2-normalizers-universe-review-final.md` approves the
bounded diagnostic slice only. Strict UTF-8 normalization, explicit
covered/unsupported/unreadable outcomes, runtime implementation/fixture-hash
allow-listing, deterministic full SHA-256 five-token shingles, and canonical
chunked JSONL validation pass in 2 files/10 tests; SlopBrick typecheck and
diff-check pass. The reader intentionally buffers fixture input and is not the
452,382-unit production path. Incremental verification, resource limits,
malformed-language fixtures, collision evidence, external sort, shards,
persistence/CLI, and real-corpus acceptance remain open; no corpus or remote
release state changed.

### Task 2A exact-similarity fixture checkpoint — 2026-07-14

The independent review in `.superpowers/sdd/task-2-similarity-review-final.md`
approves the pure fixture slice only. Same-language prefix candidates, global
exact-hash duplicates, integer size filters, exact inclusive rational 0.80
confirmation, both polarity bindings/sides, explicit AI↔human cross-side
derivation, and duplicate-shingle cardinality pass in 1 file/8 tests; the Core
overlap contract remains 1 file/6 and SlopBrick typecheck passes. Incremental
stream verification, external sort/shards, adjacency/clusters, resource
receipts, persistence/recovery, CLI, and 452,382-unit acceptance remain open;
no corpus or remote release state changed.

### Task 2A artifact-contract/runtime checkpoint — 2026-07-14

The independent review in
`.superpowers/sdd/task-2-artifact-runtime-review-final.md` approves the nine
overlap artifact schemas, generated peers, nine valid/nine invalid fixtures,
and pure semantic validators. Core passes 21 files/181 tests; focused artifact
tests pass 3/3; typecheck, schema validation, and diff-check pass with one
worker and a 2 GiB heap cap. Production incremental overlap, telemetry,
persistence/recovery, publication, CLI, and real-corpus acceptance remain
open; no corpus or remote release state changed.

### Task 2A incremental stream checkpoint — 2026-07-14

The independent review in
`.superpowers/sdd/task-2-incremental-reader-review-final.md` approves the
bounded single-consumer `AsyncIterable` universe reader. Strict UTF-8,
exact-byte hashing, canonical JSONL/final-newline, the 32 MiB unit/content
bound, Core row/registry/order/count/status/unresolved/hash checks, and early
consumer termination failure are covered by the focused overlap-universe/stream
suite (**2 files / 9 tests**); SlopBrick typecheck and diff-check pass. The
review fix permits registered-language `unreadable` rows while still rejecting
contradictory `unsupported` bindings. Production spill/merge, shards,
telemetry, persistence/recovery, CLI, and real-corpus acceptance remain open;
no corpus or remote release state changed.

### Task 2A bounded overlap computation checkpoint — 2026-07-14

The independent review in
`.superpowers/sdd/task-2-overlap-builder-review-final.md` approves the
computation-only builder. Canonical stream hashing and normalizer/status
binding, bounded multi-pass postings/pair merges, deduplicated candidate pairs,
exact/inclusive-near edges, generation-local edge/adjacency/cluster shards,
resource accounting, and output rehash/schema-checks pass in the focused
overlap suite (**5 files / 25 tests**); SlopBrick typecheck and diff-check pass.
Checkpoint/resume, publication/recovery, CLI, cross-artifact
range/relationship verification, and real 452,382-row acceptance remain open;
no corpus or remote release state changed.

## Task 2A overlap authority publication/CLI checkpoint — 2026-07-14

The bounded local authority slice is independently approved for fixture/local
use. Core has four
overlap generation/current/lock/transaction contracts with generated peers and
semantic self-hash validators (22 files/183 tests plus schema validation).
SlopBrick stages descriptor and primary leaves before transaction intent,
fsyncs journal/output boundaries, publishes immutable generation trees before
the current pointer, and recovers only after lock/transaction bindings,
generation-number chaining, complete trees, and current-pointer anchoring prove
one transaction. The authority overlap CLI exposes build/recover/verify; the
initial publication/CLI gate was 2 files/14 tests (before the follow-on
fault/status hardening), with typecheck and diff-check green under one worker
and a 2 GiB heap cap.

This remains fixture/local evidence, not corpus admission or release readiness.
Real 452,382-unit acceptance, external tool-authority receipt indexing, full
fault/two-writer coverage, checkpoint/resume, cross-artifact relationship
verification, clean recursive gates, and commit-bound release evidence remain
open. No corpus bytes or remote release state changed.

### Historical Task 2A fault/CAS and CLI-E2E update — 2026-07-14 (superseded)

The bounded publication hardening now covers six recoverable durable hooks
(including the real `cleanup-fsynced` boundary), a post-completion
`lock-unlinked` boundary, and a deterministic two-writer stale-parent race;
the publication test passes **20/20** and repeated race checks remain stable.
The CLI test passes **5/5** for child-process publish→verify, contention
status, and library-created pending transaction→CLI recover→verify. The
combined publication/CLI focus is **25/25**, and the seven-file overlap focus
is **51/51** under one worker and a 2 GiB heap cap. The post-hardening full
SlopBrick suite passes **301 files / 3,462 tests** (5 skipped files / 9 skipped
tests), and recursive typecheck is green with zero errors. Recursive build
still stops at Core's known codegen-freshness guard for uncommitted generated
observation/health peers. Real corpus, external tool authority,
checkpoint/resume, admission, clean receipts, and release remain open.

### Historical execution status — 2026-07-12

- Release-materialization Tasks 1–6 are implemented and bounded-contract
  reviewed. A bounded serial SlopBrick rerun after the default-off renderer
  fix passed 265 files (5 skipped) and 3,157 tests (9 skipped); the subsequent
  pretty-detail/resource-budget edits have focused green tests. Task 6 contract
  hardening and a fresh full rerun after all current edits remain pending. The
  clean builder commit and commit-bound receipt rerun remain open before
  admission can begin.
- Gate 2 scanner-contract remediation is complete for the implemented slice,
  with focused green evidence for parser-backed comments, AST-proven CORS,
  project-level identical-block coordination/incremental parity, effective
  diagnostics, lexical security boundaries, and large JSON/beacon completion.
- SlopBrick and website typechecks are green. The final serialized recursive
  gate is 264 SlopBrick files passed/5 skipped with 3,151 tests passed/9
  skipped; Core 116, engine 59, and website 37 also pass. Release gates remain
  explicit rather than inferred from this result.
- The working registry is 119 rules/27 categories in an unreleased candidate;
  npm's latest verified public release is v0.43.0 at 103 rules/22 categories.
  v10.1 evidence has separate analyzed (576,750) and sampled (581,550)
  denominators and covers 103 rules, not every candidate rule.
- The platform-root self-scan's security false positives from rule comments and
  advice strings were removed by the bounded lexical fix; its current result
  is 435/435 complete with Security 83.3 and 208 active findings. The remaining
  compression flood is still a calibration/UX decision, not a release pass.
- No tag, push, GitHub release, npm publication, deployment, or v10.3 corpus
  admission has occurred.

- Runtime/release truth is now centralized: Node.js 22/24 is enforced in
  package engines, doctor, docs, and CI; PR review captures threshold/runtime
  exits safely; and publishing verifies/uploads one checksum-bound tarball
  before the gated OIDC publish job. Focused runtime/packed-consumer tests are
  11/11. This is readiness evidence, not a release authorization.

- Immutable release-asset Task 5 now validates the v10.3 manifest, materializes
  pinned archives, verifies every declared file, and emits a local checkout map
  only. Its final focused matrix is 16/16 with package typecheck/build green;
  corpus admission and release state remain unchanged. Task 6 now adds exact
  resolver binding, packed valid/invalid schema checks, and durable Node 22/24
   receipts; those receipts are dirty-worktree evidence, not release approval.
- The self-scan review found a real Markdown taxonomy defect: non-AI rules with
  `USEFUL`/`OK` calibration verdicts were shown under AI Findings. The focused
  renderer fix uses the issue polarity for the AI-versus-hygiene bucket and
  passes 31 focused renderer tests plus package typecheck/build. The wider
  diagnostic context/compression decision is now closed; release-signal use
  remains gated by provenance and clean-boundary evidence.
- The portability audit at `.superpowers/sdd/path-portability-audit.md`
  confirms the v10.3 control-plane entrypoint is not tied to this machine, but
  historical calibration scripts still embed local clone/binary fallbacks;
  those scripts remain outside Gate 3 evidence until repaired and tested.

1. **Gate 3 — lossless pipeline:** finish versioned calibration artifacts,
   stable per-file observations, fail-closed accounting, deterministic
   selection/run metadata, retry/bisection, coverage checks, and schema-backed
   machine output.
2. **Gate 4 — provenance corpus:** create and review the versioned corpus
   manifest; quarantine unproven positives; record pinned origin, license,
   label evidence, file hash, family/duplicate cluster, stratum, and split
   eligibility; then derive file lists only from that manifest.
3. **Gate 3 + Gate 4 integration:** use the reviewed manifest as the only
   selection input. A run must account for every selected file exactly once and
   fail closed on stale, missing, duplicate, corrupt, or mismatched records.
4. **Gate 5 onward:** run smoke, canary, and full calibration only after the
   preceding gates have their required evidence. ML research remains downstream
   of valid per-file data and leakage-safe splits.

## Implemented v10.3 control-plane foundation

The following is implementation progress, not calibration or release evidence:

- versioned corpus, run, checkout-map, observation, failure, and coverage
  contracts; semantic provenance/leakage validation;
- deterministic manifest selection, fail-closed selection verification, and
  frozen input hashes;
- resolver containment/content-hash checks; durable no-clobber attempts;
  timeout/crash bisection with one singleton retry;
- packaged worker invocation with frozen rule filters, path-free terminal
  evidence, artifact persistence, and `cal:scan` input validation.

The bounded Gate 3 implementation now also has an explicit `cal:report`
boundary: verified scans can emit four deterministic, path-free
`status: unavailable` receipts (and exit 1) without fabricating metrics. The
The implemented Gate 3 synthetic matrix is now complete: the 28-file/553-test
calibration glob covers the listed runtime cells, and the full cross-command
slice passes 1/1 with deterministic path-free artifacts and fail-closed report
handling. Broader worker/checkout production parity and actual metric
production remain open; the persisted-schema, calibration-sidecar-isolation,
and bundled-worker slice is covered by 8/8 focused tests. Gate 4
remains independent: no v5 file may be used as evaluation evidence until it has
reviewed v10.3 provenance and labels.

## Immutable release-asset prerequisite — 2026-07-11

The canonical plan now requires release-materialization Tasks 1-6 before the
admission program consumes immutable release archives. Tasks 1-3 are complete
at `ca72d0b15`, `704abef0e`, and `14998b539`. The corrected Task 4 plan is
independently approved at SHA-256
`08d30d9dca5b177764c5c631d98d570d75b2702a556438eaf8c10dadf292c26f`;
its rereview is `.superpowers/sdd/v103-release-task4-plan-rereview.md`.

Task 4A is complete at `521c0e888`: it extracted the trusted POSIX cache
boundary without changing the byte-frozen 184-test Task 3 contract, added 43
direct tests, passed typecheck/build, and received independent specification
plus code/security approval. Its evidence is
`.superpowers/sdd/v103-release-task4a-review.md`.

Task 4B is complete at `6ce6259da`: it pins the exact ZIP graph and implements
the raw ZIP/CRC/owned-stream/receipt/reference foundation. The combined Task
3/4A/4B boundary passes 301/301 focused tests, package typecheck/build, direct
strict test compilation, and independent specification plus security review.
Its evidence is `.superpowers/sdd/v103-release-task4b-review.md`.

The staged-score blocker found during Task 4B was fixed separately at
`efb069b90`: Git-scoped verification is now flywheel-independent and read-only,
and sequential/concurrent library scans no longer leak process-scoped duplicate
state. This closes only staged determinism/state writes; type-only-use,
not-applicable-axis, suppressed-count, baseline-migration, and installed-hook
binary issues remain open Gate 2 work.

Task 4C is complete at `04697806e`. It adds bounded extraction, verified private
trees, durable no-replace publication, complete reuse checks, and identity-safe
cleanup.

Independent specification, code-quality, and archive-security rereviews returned
APPROVE after fixes for recycled-FD close retry, ambiguous link cleanup, and
`fs.Dir` lifecycle.

The focused matrix passed 425/425 under Node 20.19.6, 22.22.3, and 24.15.0.
SlopBrick passed 2,956 tests with 9 skipped. Repository suites passed core 116,
website 37, engine 59, and SlopBrick 2,956.

Typecheck, build, and frozen-install gates are green. Test repairs are recorded
at `93cbc36eb`; generated website package metadata was refreshed at `81e79b9ec`.

The pre-remediation audit baseline was 17 production and 20 full-graph
advisories; no path traversed `yauzl`, `yazl`, `pend`, or `buffer-crc32`.
After the bounded 2026-07-12 migration (Vitest 3.2.7, Vite 6.4.3, Astro
7.0.7, YAML 2.8.3, and scoped esbuild 0.28.1), both moderate-level audits
return zero advisories. The migration is still dirty and needs independent
review plus a clean commit.

The partial staged precommit self-scan scored 7.2/100 with one low
compression-profile finding. The historical package/root self-scans were
complete and parseable, but remain diagnostic. The current override records
208/208 package files and 439/439 platform-root files; the remaining
compression-profile flood awaits manual UX and calibration review.

Release-materialization Tasks 1–6 are now the bounded-complete tranche. The
next release-materialization action is not another implementation slice: it is
an authorized clean builder commit followed by a commit-bound Node 22/24
receipt rerun. No push, tag, release, publication, or deployment has occurred.
The external corpus remains read-only, non-canonical, and provenance-gated.

The detailed dependency evidence is split deliberately: the historical
pre-remediation counts remain in `.superpowers/sdd/dependency-gate-audit.md`,
while the live zero-advisory migration and its recursive verification are in
`.superpowers/sdd/dependency-remediation-report.md`.

`/Users/cheng/corpus-expansion/` is the confirmed local exploratory corpus
root. It is not, by itself, reviewed provenance or canonical manifest data;
folder names and legacy file lists cannot establish labels or release evidence.

## Gate 4 evidence snapshot — 2026-07-10

The external v10.3 workspace is now centralized at
`/Users/cheng/corpus-expansion/v10.3/`. Its legacy intake declaration is
`positive → AI` and `negative → human`; that mapping remains **declared
polarity**, not a verified binary label.

- 452,382 legacy-selected paths have per-file review inventory records:
  224,903 declared AI and 227,479 declared human.
- 317 legacy Git repositories have pinned origins and commits. Seventeen have
  no root license and are ineligible pending review.
- 14,804 exact-hash and 11,637 origin-family cross-polarity records are
  quarantined. Near-duplicate clustering remains required before selection.
- The central registry, inventories, and quarantine JSON/JSONL artifacts pass
  `node /Users/cheng/corpus-expansion/v10.3/review/validate-review-artifacts.mjs`.
- The first labeling procedure is
  `/Users/cheng/corpus-expansion/v10.3/review/labeling-protocol-v1.md`:
  two independent reviewers, pinned evidence, license decision, content hash,
  scope, and edit status are mandatory for a verified label.
- AIGCodeSet was independently reviewed and remains quarantine-only. Although
  its numeric labels and model metadata are explicit, its CodeNet lineage has
  unresolved per-submission redistribution evidence and its classes share every
  problem group plus exact cross-label duplicates. The audit is recorded at
  `/Users/cheng/corpus-expansion/v10.3/review/aigcodeset-provenance-review.md`.

After release-materialization Tasks 4-6 and the preceding admission-authority
slices are complete, the next Gate 4 data task is to turn an approved, licensed
paired source into a small reviewed manifest fixture, then run its repository/
family/cluster split through the already-implemented selector and scanner.
Additional public data must enter `sources/benchmarks/` as quarantine first; it
must not bypass this process merely because it has a machine/human field.

For existing evidence and open conditions, see the
[execution evidence](../../packages/slopbrick/docs/calibration/v0.45.0-execution-evidence.md)
and the plan's Gate 3, Gate 4, and release-gate sections.

## Historical Task 1B structured record/decision consumption checkpoint — superseded 2026-07-13

> Historical snapshot only. The superseding temporal/adjudicator checkpoint in
> the package handoff and continuation plan is the current execution state.

The bounded offline structured-authority slice is independently approved in
`.superpowers/sdd/task-1B-structured-records-rereview.md`. SlopBrick now
consumes the record stream, samples, blind assignments, decisions, receipts,
and source ledgers as one graph. Representation is record-derived rather than
inventory-derived: zero records means zero represented units and an explicit
unrepresented remainder. Orphan bytes, partial/alias-conflicting graphs,
duplicate/cross-source/unledgered ledger objects, missing decision joins,
missing source-rights evidence, and non-canonical decision order fail closed.

Fresh gates: SlopBrick focused review/census **22/22**, Core focused authority
**13/13**, full Core **17 files / 165 tests**, both typechecks, schema
validation, and diff-check. This remains non-authoritative with
`ready=false`, `authorityEligible=false`, and `eligibleUnits=0`; Gate 4 is
still `verified_ai=0` / `verified_human=0` with no corpus mutation. The
current two-decision receipt contract rejects a one-decision third adjudicator
until a dedicated receipt contract is implemented. Evidence-ID resolution
against the verified index and temporal-attestation consumption are deferred.
The worktree snapshot is 613 status entries with SHA-256
`f5b4404c0a72c4b81766767ad93d06687d35a81b45f0538aeb9473cd72e051df`.

## Historical Task 2A envelope-relation checkpoint — 2026-07-14 (superseded)

The bounded 301-file/3,466-test counts in this section are retained for
provenance. The package-level continuation plan's later Live verification
follow-up supersedes them with the current 307-file/3,540-test evidence.

`verifyOverlapArtifactRelations` now fail-closes the three overlap envelopes
when their Core-valid self-hashes or their cross-file authority joins disagree.
It checks the generation receipt binding, universe/policy/normalizer/tool
receipt identities, index-to-ledger receipt linkage, coverage/count/completion
flags, and required envelope paths. Publication, recovery, and read-only
verification use this immutable-tree check.

Latest bounded evidence: publication **24/24**, CLI **5/5**, combined **29/29**,
seven-file overlap **55/55**, full SlopBrick **301 files / 3,466 tests**
(5 skipped files / 9 skipped tests), Core **22 files / 183 tests**, schema
validation, recursive typecheck, package build, and diff-check green. The
recursive build remains blocked by the known uncommitted Core generated
observation/health peers. This remains fixture/local evidence only: no real
corpus admission, external tool receipt, checkpoint/resume, clean commit-bound
receipt, release, or deployment has occurred.

## Task 2A terminal checkpoint/resume and sidecar-integrity follow-up — 2026-07-14

The overlap builder now persists four hash-bound local checkpoints in phase
order (`postings`, `candidate_pairs`, `exact_edges`, `clusters`) under a
task-scoped invocation intent. A terminal `clusters` checkpoint resumes
without reading the universe stream or source bytes; intent mismatches,
altered checkpoint bytes, substituted shards, and non-terminal resume requests
fail closed. Terminal recovery also validates Core index, ledger, and resource
receipt self-hashes and authority/completion joins, closing the local-sidecar
tamper gap found during review.

The checkpoint/resume suite passes **4/4**; the overlap/builder/scale/
publication/CLI/resume and health-structure focus passes **90/90**; SlopBrick
typecheck and `git diff --check` pass. Only terminal resume is implemented;
earlier-phase continuation remains open. This is recovery infrastructure, not
corpus admission or release evidence; no corpus or remote state changed.

## Disposable source-bound self-scan refresh — 2026-07-14

The rebuilt workspace binary was rerun in a disposable copy with project
memory and telemetry disabled, one worker, and no repository mutation. The
current JSON artifact is `/tmp/slopbrick-self-scan-current-20260714-final.json`
(585,972 bytes, SHA-256
`20cb0bcad4b02925e974c7f61585ef683e0b798b5e62be680e51099203f8f623`). It
analyzed **227/227** files with zero failure/skip/parse/timeout/crash/internal
outcomes, **157 active / 531 total** findings, **374 default-off suppressed**,
AI Slop **17.1909**, hygiene **99.4702**, security **100.0000**, health
**92.9647**, and `failedThresholds=["meanSlop"]`. Exit 1 is the expected
policy result, not a crash; this remains diagnostic rather than release
evidence. The brief terminal output is useful and internally coherent. The
headline now names the metric (`Repo's AI-slop score is low`) so the
lower-is-cleaner direction is not ambiguous; the focused UX regression passes.

The follow-up scan after that correction retains the same valid 227/227
coverage, 157 active / 531 total findings, and expected meanSlop threshold
failure. The new disposable JSON receipt is
`/tmp/slopbrick-self-scan-current-20260714-ux.json` with SHA-256
`4e52627940f6d4d5a6419387ed42a26fa866f15895804723cee56fdecaf30593`.

## Final full package and health-wire matrix gate — 2026-07-14

The latest RAM-capped serial SlopBrick package gate passes **307 test files /
3,540 tests** (5 skipped files / 9 skipped tests), including the genesis,
persisted-health, heatmap, and doctor validity coverage. Core remains **23/185**
and Engine **5/59**; recursive typecheck remains green. The focused validity and
renderer matrix is **121/121**. The package build has only the known non-fatal
Zod declaration warnings. The clean codegen boundary, real 452,382-unit
admission/calibration, and release gates remain open; no corpus or remote state
changed.

## Current synthetic clean-boundary consumer rerun — 2026-07-14

The current bytes were copied to `/tmp/platform-clean-current-AAX30H`, omitting
only the unrelated root artifacts `TODO.md`, `.astro/types.d.ts`, and
`src/env.d.ts`, then committed locally at synthetic commit
`0118fdeb02fe712b482729f1ea612aa8496989a0`. The snapshot has zero status
entries; the empty-status SHA-256 is
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
The snapshot predates later documentation-only plan/evidence edits, so its
implementation commit is not byte-identical to today's full documentation.
The dedicated recursive typecheck log is
`/tmp/platform-clean-current-AAX30H-typecheck.log` (SHA-256
`77a8ba5d727e74962cdb7fb9f4dd04f7db7e699e54f3e03eb09e82b86e17b93f`).

Recursive serial typecheck (0 errors; three non-blocking Website hints), build,
and tests pass. The package result is **307 files / 3,540 tests passed** with
5 skipped files / 9 skipped tests; build output has only the known non-fatal
Zod declaration warnings.

The packed `slopbrick@0.44.0` artifact at
`/tmp/task6-clean-current-AAX30H/slopbrick-0.44.0.tgz` has SHA-256
`c048fe0ac0e4d7fb1a988e7899c3a6765b87f8cc6e09c13d63521ce548beb826`.
The packed-consumer flow passes **9/9** under Node 22.22.3 and **9/9** under
Node 24.15.0 using strict offline installation and local-process-only CLI
execution. The current receipt directory is
`/tmp/task6-clean-current-AAX30H/receipts-v2/`; the external packer is recorded
as Node 24.15.0, npm 11.12.1, and pnpm 10.34.5. Receipt hashes are
`0858f8f9125fc3303071ffcd9e10dac83900004174f964c673e1bbbacef12919`
(Node 22) and `f393f8dc7ca5fee719fd2561c9b48ff249c3ae837529b03ced355936675dacf5`
(Node 24). Both receipts agree on the same tarball, synthetic commit, and
clean status and report successful install, ESM import, CJS require,
materialize, select, and verify stages.

This is diagnostic clean-snapshot evidence only. It is not a branch commit,
release receipt, npm publication, deployment, or corpus-admission authority;
the real clean-boundary and zero-authority corpus gates remain open.

## Current Task 3A handoff — 2026-07-15

The deterministic witness/census contract slice is committed locally at
`f69dd6bc5`. Core schemas and validators now cover witnesses, infeasibility
certificates, search/result bundles, witness-review receipts/bundles, and the
census. SlopBrick has deterministic candidate projection/search and a
diagnostic `census` boundary. The single integrated gate is green: Core
**32/237**, recursive typecheck/build green, and SlopBrick **326 test files /
3,702 tests** with 5 skipped files / 9 skipped tests under a 2 GiB heap cap.

The contract explicitly rejects unsorted streams and incomplete search
authority, and requires an approved witness review before readiness. It remains
non-authoritative: live static/witness authority is missing, the review-graph
publisher is still the next implementation slice, and no corpus labels,
release, remote, package publication, or deployment changed. Do not pull more
repositories or manually inspect all 452,382 rows; finish authority
materialization first, then run the 100+100 smoke and 5,000+5,000 canary gates.

## Task 3B review-graph checkpoint — 2026-07-15

The pure witness-review graph builder is now present locally. One integrated
focused suite (**3/3**) covers the complete acyclic bundle plus receipt/output
binding, disagreement, and non-witness-target failures. It remains no-I/O and
diagnostic: no publication lock/transaction, completion marker, routing
reference, corpus mutation, label promotion, remote, release, or deployment
changed. The next implementation slice is one transactional publisher with a
single crash/no-clobber gate; the prior three-way confirmation pattern is
retired for this edge-case layer.

### Task 3B transactional witness-publication checkpoint — 2026-07-15

Task 3B now has one implementation owner and one automated fault matrix. Core
commits `d065c191e` and SlopBrick commit `fc9859a72` (with cleanup `d2e04c0e4`)
add four publication contracts, generated types, semantic lock/transaction
binding, and a transaction-owned publisher/recovery path. The fixed topology
is `review/admission/witnesses/<gate>`: bundle, completion, and routing
projection are staged, fsynced, promoted without clobbering same-path bytes,
and journaled. Recovery is nonce- and no-live-writer-gated, including a
lock-only journal; existing unrelated files and symlink-escape attempts remain
protected.

Focused evidence is **9/9 Core contract tests**, **6/6 publisher/recovery
tests**, and **2/2 CLI boundary tests**. The full RAM-capped SlopBrick gate is
**329 test files / 3,713 tests passed**, with 5 skipped files / 9 skipped
tests, plus Core/SlopBrick typecheck and `git diff --check`. The CLI emits one
machine-readable error for incomplete
publication requests and does not mutate the root. The routing-promotion
recovery test covers the subtle CAS-state rebinding failure that would
otherwise justify another manual confirmation.

This is still diagnostic-only: no real static/witness authority, corpus label,
manifest, remote, release, package publication, or deployment changed. The
live census remains 329/329 sources reviewed, 452,382 additive units, 0
candidate/eligible, all quarantined, blocked by
`static_authority_unavailable` and `witness_authority_unavailable`. Next is
materialize those authorities and run the bounded smoke/canary gates; no more
three-way confirmation of these edge cases is planned.

## 2026-07-15 source-byte binding checkpoint

The bounded source-level audit at
`/Users/cheng/corpus-expansion/v10.3/review/source-byte-binding-audit-2026-07-15.json`
resolves all **452,382** selected inventory rows against the centralized
filesystem: **452,382 regular files, 0 missing, 0 non-regular, and 0 recorded-
size mismatches**. It used streaming inventory reads and `lstat`; it did not
load or execute the corpus. This narrows the blocker to provenance/rights and
generation-backed authority, not missing bytes or insufficient repository
volume. No additional public-repository pull is warranted. The one remaining
project gate is real static/overlap authority, then the bounded witness
smoke/canary; automated edge-case coverage remains the sole confirmation for
filesystem/parser/recovery behavior.

The same audit identifies the next concrete authority dependency: the current
configured registry covers **228,456 TypeScript rows** and leaves **223,926**
rows in other languages. The shared lexical runtime now has a separately
reviewed generic binding, but the real registry still needs explicit,
hash-bound entries (or an excluded partition) before a complete overlap ledger
can be published. This is a coverage contract, not a reason for manual review
of every file.

### Resource probe boundary — 2026-07-15

The resource probe in
`/Users/cheng/corpus-expansion/v10.3/review/overlap-materialization-resource-probe-2026-07-15.json`
normalized 1,000 rows successfully, then stopped the existing overlap builder
when its temporary work reached about **601 MiB** (RSS about **557 MiB**) on a
host with roughly **12 GiB** free. No authority was published. A full run must
therefore be sharded or use a bounded index/work strategy; it must not be
launched in parallel on this checkout.

### Explicit language registry and coverage checkpoint — 2026-07-15

The normalizer boundary now has a deterministic registry builder with an
explicit hash-bound entry for each observed supported language (Astro, C, C++,
C#, Dart, Go, Java, JavaScript, Kotlin, PHP, Python, Ruby, Rust, SQL, Svelte,
Swift, and TypeScript). The registry is persisted at
`/Users/cheng/corpus-expansion/v10.3/review/admission/normalizer-registry.json`;
its self-hash is
`2a9b52f03203276008c083ec38f3ab8f0abd983b7877eda3ac3d7eb0ebf4e43e` and its
canonical file hash is
`b723bc4d5ac5091bae396c251f913df6e951f5d7280d87dc3126ede84264c9ae`.

The one-worker coverage audit is recorded at
`/Users/cheng/corpus-expansion/v10.3/review/normalizer-coverage-audit-2026-07-15.json`.
It resolved and normalized **452,364/452,382** rows, with **0 missing files,
0 non-regular files, 0 size mismatches, and 0 content-hash mismatches**. The
remaining **16 `other` rows** are explicitly unsupported and **2 rows** are
strict-UTF-8 unreadable; neither is silently treated as “no overlap.” The pass
read 2,876,862,876 bytes, observed a maximum RSS of about 373 MiB, and did not
promote labels or publish authority.

This is a real coverage improvement, not a readiness claim. The unresolved
18 rows still keep a complete overlap receipt closed, and the existing full
overlap materializer remains unsafe at this host's available disk budget; the
next implementation is a bounded/sharded join, not another human confirmation.

#### Coverage delta correction — 2026-07-15

The 16 `other` rows were then verified as `.m` Objective-C sources and bound
through an explicit `normalizer-objective-c-v1` registry entry. The updated
registry self-hash is
`376f5d05f3ec64d970c7c5b10daa40e89b6615e0151381e270306a865c916375` and its
canonical file hash is
`6989a41169f64e717d6f3372b1f6d6fbe8b1f6231af99449f0dc74513045bf91`.
The delta receipt is
`/Users/cheng/corpus-expansion/v10.3/review/normalizer-coverage-audit-2026-07-15-r2.json`.
Combined with the prior full stream, it leaves **452,380 covered**, **0
unsupported**, and exactly **2 strict-UTF-8 unreadable** rows. Those two rows
remain explicit unresolved inputs; no label or authority state changed.

### Source allocation/provenance checkpoint — 2026-07-15

The corpus does not need a second manual pass over all 452,382 rows just to
answer where they came from. The streaming allocation audit at
`/Users/cheng/corpus-expansion/v10.3/review/source-allocation-audit-2026-07-15.json`
joins each inventory row's namespaced `sourceId` and declared polarity to the
329-entry genesis register and its 329 source reviews, while retaining the
origin URL/local source, pinned commit where available, source decision, rights
status, language, and quarantine status. It reconciles **452,382/452,382**
rows: **224,903 declared-AI** and **227,479 declared-human**, comprising
**58,089** baseline rows and **394,293** repository rows. **298** registered
sources have selected rows; the remaining registered sources have zero selected
rows in this inventory.

The receipt is reproducible (`allocationSha256`
`16a45c496339e645fa28cc3f917771f6c94bd101edaac179b9e1977dd9bb4960`; file
SHA-256 `2d3ec6099fc3dae24cd5580e489bc79d375413d6b555826a0f0ca768a911b177`;
producer script SHA-256
`be9af4ea929a799a3b07adcad927e86ed3dcc5bc8ce510f192cab2687c6c9cd5`).
All 329 source decisions remain `source_quarantine`; the audit is explicitly
`allocation_diagnostic_only`, with `authorityPublished=false`. This allocates
the work into source-level provenance/rights review queues and avoids wasting
RAM/time on duplicate row-by-row confirmation. Human review is still required
for the external claims that can change admission (authorship, rights,
lineage, privacy/quality), not for repeating deterministic allocation or
filesystem edge-case checks.

### Scratch-reclamation/resource checkpoint — 2026-07-15

The overlap builder now removes consumed external-sort runs and phase-local
raw/sorted intermediates after durable output is emitted; deletion is
symlink-contained. The focused builder/resume/scale gate is **14/14**, and the
builder test asserts that all seven scratch run directories are empty after a
successful build. A representative 1,000-row probe (500 positive + 500
negative legacy-baseline rows) completed with **1,000/1,000 covered**, **161
edges**, `coverageComplete=true`, and `withinAllLimits=true`. Its receipt is
`/Users/cheng/corpus-expansion/v10.3/review/overlap-materialization-resource-probe-2026-07-15-r2.json`:
maximum retained work was **565,103,061 bytes (~539 MiB)**, compared with the
prior ~601 MiB probe, and no scratch run files remained. This is a bounded
resource improvement, not full-corpus authority evidence; the 452,382-row run
still requires a sharded/join canary before any authority publication. The
edge cases are now one automated confirmation gate, not three human approvals.
The refreshed serial package gate is **329 test files / 3,716 tests passed**
with 5 skipped files / 9 skipped tests.

## 2026-07-16 language-sharded overlap checkpoint

The overlap materializer now processes the shingle/postings join one
registered language at a time. Scratch slots are full SHA-256 language
identifiers, unit offsets remain byte-bound per language, and the exact-content
pass remains global so identical bytes in different languages still produce an
exact edge. The builder tool receipt is bound to
`disk-bounded-language-sharded-postings-prefix-filter-v1`; consumed language
scratch runs and unit files are removed only after their durable outputs are
written.

The focused overlap matrix is **66/66 tests** across eight files, including the
new cross-language exact-edge and language-scoped-postings case; SlopBrick
typecheck and `git diff --check` pass. A 1,000-row representative probe (500
declared-AI + 500 declared-human, TypeScript/JavaScript) completed with
**1,000/1,000 covered**, **161 edges**, `coverageComplete=true`, and
`withinAllLimits=true`. Its receipt is
`/Users/cheng/corpus-expansion/v10.3/review/overlap-materialization-resource-probe-2026-07-16-sharded.json`
(file SHA-256 `cff4f40cc19b43455db00fcdcb8245bfa5486ae52560cffe8838cedb26fee4a9`);
observed maximum heap was **783,413,768 bytes**, RSS **986,513,408 bytes**,
retained work **776,960,449 bytes**, and wall time **35,205 ms**. This is a
resource-bound implementation/probe improvement, not authority or label
evidence: the real census remains 0 candidate/eligible with static and witness
authority unavailable. No new human confirmation or public-repository pull is
required for this edge-case/resource slice.

## Execution-control rule — 2026-07-16

The calibration work is owner-led. One active execution path owns the next
bounded slice and one integrated automated gate owns deterministic
filesystem/parser/resource edge cases. Existing receipts and passing tests are
reused; they are not re-confirmed by multiple agents. A second reviewer is
reserved for a genuinely independent, high-risk authority, rights, release, or
security decision and must review a different evidence question. The current
static/witness-authority slice therefore has no duplicate confirmation queue.

## FormAI-v1 bounded source intake — 2026-07-16

Because the existing GPT-5 coding-examples source has only 67 conservative code
files, one bounded additional source was acquired for diagnostic cohort
coverage: the official FormAI-Dataset repository at commit
`0056a748b8a087e533be94a3ad2b2135065b0102`. Its v1 archive is pinned by
SHA-256 `fc458020ad0e9b0999882d4bfdd27edfdee2f0ff5de22848e11352d64230ca47`,
the repository license by
`633eadfd5d8bff94251c18bde927d76d712b063e621a67c592c825895d31561b`, and the
README by
`07ab29ef7627aabe6eed3e4fc1522ca84f6712b7e39b2778b2d770ea295c5d62`.

The bounded extraction selected the first 100 `DATASET/FormAI_*.c` members in
the immutable archive order: 100 files, 220,368 bytes, selection hash
`4461d0512af07314df8f241536a3e92e844281b2450375d368a25561a28335d0`
and content-tree hash
`9c229be47050df2e2cd448baba8997d34e9c43a393ae3f9a1aa43b2a3c12edb7`.
The machine audit is
`/Users/cheng/corpus-expansion/v10.3/review/formai-v1-gpt35-smoke-provenance-audit-2026-07-16.json`
(SHA-256 `deba3c3fd85458adac5fe4ac9bd70e33266e8b2ed17a28662b7863480111f6ec`).

The upstream README claims 112,000 compilable C programs generated by
GPT-3.5-turbo and the repository carries MIT licensing; those are retained as
source evidence, not silently applied to every derived row. This intake is
`source_quarantine`, diagnostic-only, and did not change the v10.3 register,
labels, manifest, static authority, witness authority, or smoke selection.
No sample was compiled or executed. The remaining blockers are the source
register delta, record-level prompt/edit/lineage policy, and the documented
independent source decisions; no full-corpus rereview is required.

## Bounded FormAI/HumanEval scan UX review — 2026-07-16

The pre-fix serial diagnostic scan with explicit C/Python include globs
analyzed **200/200** files (100 FormAI C + 100 HumanEval Python), with **0**
parse failures, **0** crashes, a valid complete report, AI Slop Score **8.56**,
and no failed thresholds. The machine review is
`/Users/cheng/corpus-expansion/v10.3/review/formai-v1-gpt35-smoke-scan-review-2026-07-16.json`
(SHA-256 `335d11bc17ad4b1241bd055f699580d9356adf8ca7206beeb4a0f5e35ebe6203`);
the JSON report SHA-256 is
`9ec30723cc088aab14e30500701e63647a0896839d68ea6fd2fdf6ccac17c997`.

The default-config control initially selected only the 100 Python files because
C/C++ was omitted from the default include globs. That boundary is now fixed:
the default policy includes `.c`, `.h`, `.cc`, `.cpp`, `.cxx`, `.hpp`, and
`.hxx`. A rebuilt default scan selected and analyzed **200/200** files (100 C +
100 Python), with **0** parse/internal failures, valid completion, the same
diagnostic scores, and no failed thresholds. The before/after audit is
`/Users/cheng/corpus-expansion/v10.3/review/formai-v1-gpt35-smoke-scan-default-cpp-fix-2026-07-16.json`
(SHA-256 `f51af6144cab991f65adf06719b9a6ccacc228b5bf93c4f901d4f68b5d5c2ea5`).
This improves discoverability only; the scan remains diagnostic and does not
establish authorship or change labels.

## Default C/C++ discovery implementation and gate — 2026-07-16

The default discovery policy now includes the reviewed C/C++ extensions
(`.c`, `.h`, `.cc`, `.cpp`, `.cxx`, `.hpp`, `.hxx`). The focused discovery and
language-contract tests are **22/22**, the package typecheck and rebuild pass,
and the rebuilt default FormAI/HumanEval control selects **200/200** files
(100 C + 100 Python) with zero parse/internal failures. The RAM-capped
package-wide gate then passed **329 test files / 3,717 tests**, with 5 skipped
files / 9 skipped tests. This is a product discoverability fix only; the live
census remains **0 represented / 0 eligible** and overlap verification still
fails closed with `overlap_current_missing`.

## Final recursive test gate — 2026-07-16

The RAM-capped recursive test gate completed successfully once after the
package gate: Core **33 files / 240 tests**, Website **7 files / 38 tests**,
Engine **5 files / 59 tests**, and SlopBrick **329 files / 3,717 tests**. The
workspace total is **374 test files / 4,054 passing tests**, with 5 skipped
SlopBrick files / 9 skipped tests. This is the single integrated deterministic
test confirmation for this slice; no parallel re-confirmation is needed. The
authority state is unchanged: the live census remains **0 represented / 0
eligible**, and overlap verification still fails closed with
`overlap_current_missing`.

## Explicit smoke-input boundary — 2026-07-16

The next bounded slice is implemented in the package plan as the
library-only `materializeAdmissionSmokeInputGeneration` adapter. It requires
explicit, hash-bound source generations, semantic sidecars, an acquisition-
bound register delta, canonical record/overlap streams, and a strict 100 AI /
100 human cohort policy before writing anything. It stages and atomically
renames a diagnostic bundle and always marks it non-authoritative. Focused
coverage is **2/2** and SlopBrick typecheck passes. This does not change the
live census: **0 represented / 0 eligible** and static/witness authority are
still unavailable. The next gate requires the missing reviewed authority
artifacts; no additional repository pull is justified.

The post-build read-only `census:preview` remained unchanged: `ready=false`,
`authorityEligible=false`, **329/329** sources reviewed, **452,382** additive
units, **0** represented/candidate/eligible, and **452,382** quarantined. The
blockers are still `static_authority_unavailable` and
`witness_authority_unavailable`; no corpus or label state changed.

## Smoke materializer integrity hardening — 2026-07-16

Staging cleanup is now ownership-safe: an existing transaction stage is never
removed by a replay, and a cohort must represent every declared source. The
focused adapter gate is **4/4**, with SlopBrick typecheck, rebuild, and diff
checks green. The P0 release/schema audit remains isolated on its own branch;
no release, remote, deployment, corpus, labels, or authority state changed.

Next: obtain the pinned reviewed source-generation/approval, semantic-authority,
register-delta, overlap/normalizer, and static/witness receipts. Then run the
bounded 100-positive/100-negative smoke gate. Until those inputs exist, v10.3
stays at **0 represented / 0 eligible** and the two authority blockers above
remain explicit.

## Smoke overlap and hostile-input hardening — 2026-07-16

The smoke boundary now validates the supplied overlap universe/JSONL against a
Core normalizer registry, binds that registry hash into its diagnostic receipt,
rejects non-canonical source-review bytes, and converts malformed runtime
requests into fail-closed results. Focused coverage is **7/7**; typecheck,
rebuild, and diff checks pass. No corpus, labels, authority, release, remote,
or deployment state changed. The registry is intentionally caller-owned and
the adapter remains diagnostic-only; real authority still requires row-to-
record binding, complete source approvals, and static/witness receipts.

## Smoke overlap-to-record join — 2026-07-16

Overlap rows must now bind one of the 200 admission records and match its
source, content, language, aggregate sources, and proposed polarity. A
Core-valid wrong-record substitution is rejected before staging. Focused
coverage is **8/8**; typecheck, rebuild, and diff checks pass. The adapter
remains diagnostic-only and the v10.3 census is unchanged at **0 represented /
0 eligible**.

## Smoke source-approval byte binding — 2026-07-16

The smoke input now carries and persists canonical independent-review approval
bytes, bound to each source generation and proposal. A substituted approval is
rejected before staging. Focused coverage is **9/9**; typecheck, rebuild, and
diff checks pass. The bundle remains diagnostic-only and the census is
unchanged.

## Complete source-generation graph validation — 2026-07-16

The smoke materializer now invokes Core's complete source-generation graph
validator. Candidate sources must carry matching source review, proposal,
generation, independent approval, blind assignment, two typed decisions, blind
review receipt, and acquired snapshot/materialization receipt; partial or
ID-only semantic sidecars fail closed before staging. Focused materializer and
CLI coverage is **14/14** (serial), with SlopBrick typecheck and build green
(only the existing non-fatal Zod declaration warnings). The result remains
diagnostic-only; no corpus, labels, authority, release, remote, or deployment
state changed. The live census remains **0 represented / 0 eligible**, blocked by
`static_authority_unavailable` and `witness_authority_unavailable`.

## Static-ledger stream hostile-runtime and receipt hardening — 2026-07-16

The disk-backed static-ledger stream now fails closed for hostile JavaScript
requests before dereferencing request fields, returning a typed diagnostic
receipt for null and invalid-kind inputs. Successful stream receipts now require
an explicit empty error list, an absolute output directory, and exact
generation-local projection paths for the selected ledger kind; traversal,
absolute, and cross-kind paths are rejected. The focused stream plus outer
materializer gate is **14/14**, with SlopBrick typecheck, build, and
`git diff --check` green (only the existing non-fatal Zod declaration warnings).
This is local safety hardening only: no corpus, label, authority, release,
remote, or deployment state changed, and the live census remains **0
represented / 0 eligible** with the two authority blockers above.

## Smoke-input boundary hardening and exploratory-role clarification — 2026-07-16

The explicit `admission:smoke-input` loader now accepts only canonical
root-relative manifest selectors and manifest paths: dot/dot-dot or empty
segments, repeated separators, backslashes, control bytes, and overlong paths
fail before input resolution. The contained loader also rejects directories and
other non-regular files before reading bytes, preventing a caller-supplied FIFO
or device path from blocking the diagnostic command. The materializer rejects
admission JSONL that is not strictly ordered by immutable `recordId`, matching
the downstream static-authority stream contract. Focused CLI/materializer
coverage is **19/19** serial, and the combined smoke/ledger suite is **25/25**;
SlopBrick typecheck, build, and `git diff --check` are green (with only the
existing non-fatal Zod declaration warnings during build).

This adapter remains intentionally exploratory and diagnostic-only: it enforces
the explicit 100/100 record shape, source/language/family/pair mechanics, byte
bindings, and overlap joins, but it does not infer `eligible_gold` or claim a
verified `AdmissionCohortWitnessV1`. Gold evidence, unique-cluster authority,
complete static ledgers, witness approval, and release readiness remain
downstream gates; every materialized result is `diagnosticOnly=true`,
`authorityEligible=false`, and `ready=false`. No corpus, labels, authority,
remote, release, or deployment state changed.
